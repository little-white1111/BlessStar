{
  "id": "C-KERNEL-POOL-1",
  "type": "kernel",
  "version": "v1",
  "priority": "kernel",
  "status": "active",
  "scope": [
    "kernel/runtime",
    "adapter/orchestration"
  ],
  "stage": "ci",
  "rule": "KernelPool must use the day21 A''' parameters: steady=3, max=10, FIFO unbounded wait, no execute timeout, no slot quarantine on exec failure, per-batch parallel exec enabled, and IrSnapshot latest+pin retention.",
  "gate_refs": [
    "GATE-KERNEL-POOL-CONFIG"
  ],
  "blocking": true,
  "owner": "subtask-b-eng",
  "evidence": [
    "架构方案选择记录.md",
    "kernel/runtime/include/bs/kernel/runtime/kernel_pool.h",
    "kernel/runtime/src/kernel_pool.c"
  ],
  "deprecate": null,
  "parameters": {
    "steady_count": 3,
    "max_instances": 10,
    "dynamic_idle_ttl_ms": 1000,
    "inline_depth_max": 8,
    "fifo_wait_unbounded": true,
    "execute_timeout_ms": 0,
    "per_batch_parallel_exec": true,
    "slot_quarantine_on_failure": false,
    "retain_latest_per_path": true,
    "max_pinned_revisions_per_path": 8
  },
  "implementations": [
    {
      "gate_id": "GATE-KERNEL-POOL-CONFIG",
      "runner": "python",
      "entry": "tools/scripts/gates/check_kernel_pool_config.py",
      "entry_kind": "script"
    }
  ]
}
