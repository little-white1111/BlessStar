{
  "id": "C-CF-1",
  "type": "configuration",
  "version": "v1",
  "priority": "configuration",
  "status": "active",
  "scope": [
    "adapter",
    "parser"
  ],
  "stage": "parse",
  "rule": "Configuration parse failures must surface deterministic error semantics before persistence.",
  "gate_refs": [
    "GATE-INTEGRATION",
    "GATE-REGRESSION"
  ],
  "blocking": true,
  "owner": "subtask-b-eng",
  "evidence": [
    "docs/reports/contract-gate-report.json"
  ],
  "deprecate": null,
  "implementations": [
    {
      "gate_id": "GATE-INTEGRATION",
      "runner": "ctest",
      "entry": "ctest --test-dir build_ci_test -C Release -L integration -j 1 --output-on-failure",
      "entry_kind": "command"
    },
    {
      "gate_id": "GATE-REGRESSION",
      "runner": "ctest",
      "entry": "ctest --test-dir build_ci_test -C Release -L regression -LE day17 --output-on-failure",
      "entry_kind": "command"
    }
  ]
}
