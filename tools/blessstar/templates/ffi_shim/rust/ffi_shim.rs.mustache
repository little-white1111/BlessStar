// BlessStar FFI Shim — {{biz_id}} / {{display_name}}
// Auto-generated by blessstar spec --lang=rust
// Build: cargo build --release (produces .dll/.so/.dylib)

#![allow(unused_variables)]

use std::ffi::CStr;
use std::os::raw::c_char;

{{#gates}}
/// {{description}}
/// Rule: {{rule}}
/// Returns 0 = accept, -1 = reject
#[no_mangle]
pub extern "C" fn gate_{{id}}(
    ctx: *mut std::ffi::c_void,
    uri: *const c_char,
    read_result: *const std::ffi::c_void,
    detail_out: *mut c_char,
    detail_cap: usize,
) -> i32 {
    // TODO: Implement gate logic for {{field}} — {{rule}}
    0 // default: accept
}

{{/gates}}
{{#effects}}
/// Effect: {{trigger}} → {{action}}
#[no_mangle]
pub extern "C" fn effect_{{trigger_dot_to_underscore}}(
    ctx: *mut std::ffi::c_void,
    key: *const c_char,
    data: *const u8,
    len: usize,
    epoch: u64,
) -> i32 {
    // TODO: Notify {{action}} when {{trigger}} changes
    0
}

{{/effects}}
