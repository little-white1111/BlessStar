// BlessStar FFI Shim — {{biz_id}} / {{display_name}}
// Auto-generated by blessstar spec --lang=cs
// Build: dotnet build (produces FfiShim.dll)
// Kernel loads via LoadLibrary + GetProcAddress with UnmanagedCallersOnly

using System;
using System.Runtime.InteropServices;

/// <summary>
/// BlessStar FFI Shim: {{display_name}}
/// </summary>
public static class FfiShim_{{biz_id_underscore}}
{
{{#gates}}
    /// <summary>
    /// {{description}}
    /// Rule: {{rule}}
    /// </summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    public static int Gate_{{id}}(IntPtr ctx, IntPtr uri, IntPtr readResult, IntPtr detailOut, int detailCap)
    {
        // TODO: Implement gate logic for {{field}} — {{rule}}
        return 0; // default: accept
    }

{{/gates}}
{{#effects}}
    /// <summary>
    /// Effect: {{trigger}} → {{action}}
    /// </summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    public static int Effect_{{trigger_dot_to_underscore}}(IntPtr ctx, IntPtr key, IntPtr data, int length, ulong epoch)
    {
        // TODO: Notify {{action}} when {{trigger}} changes
        return 0;
    }

{{/effects}}
}
