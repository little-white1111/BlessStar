#include <stdio.h>
#include <string.h>
#include <stdint.h>

/* BlessStar FFI Shim — {{biz_id}} / {{display_name}}
 * Auto-generated by blessstar spec --lang=c
 * Implement the body of each gate_{{id}} function.
 */

{{#gates}}
/* {{description}}
 * Rule: {{rule}}
 * Layer: {{layer}}, Sub-category: {{sub_category}}
 * Returns 0 = accept, -1 = reject (write error to detail_out)
 */
int gate_{{id}}(void* ctx, const char* uri, const void* read_result,
                char* detail_out, size_t detail_cap) {
    /* TODO: Implement gate logic for {{field}} — {{rule}} */
    (void)ctx; (void)uri; (void)read_result; (void)detail_out; (void)detail_cap;
    return 0; /* default: accept */
}

{{/gates}}
{{#effects}}
/* Effect: {{trigger}} → {{action}}
 * Triggered when {{trigger}} changes.
 * Returns 0 on success.
 */
int effect_{{trigger_dot_to_underscore}}(void* ctx, const char* key,
                                          const uint8_t* data, size_t len,
                                          uint64_t epoch) {
    /* TODO: Notify {{action}} when {{trigger}} changes */
    (void)ctx; (void)key; (void)data; (void)len; (void)epoch;
    return 0;
}

{{/effects}}
