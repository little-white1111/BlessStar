// BlessStar FFI Shim — {{biz_id}} / {{display_name}}
// Auto-generated by blessstar spec --lang=ts
//
// napi-rs based FFI shim for Node.js native addon.
// Build: npx napi build --platform --release
// Kernel loads the resulting .node file via dlopen.

{{#gates}}
// {{description}}
// Rule: {{rule}}
export function gate{{id}}(ctx: Buffer, uri: string, readResult: Buffer): number {
    // TODO: Implement gate logic for {{field}} — {{rule}}
    return 0; // default: accept
}

{{/gates}}
{{#effects}}
// Effect: {{trigger}} → {{action}}
export function effect{{trigger_dot_to_underscore}}(ctx: Buffer, key: string, data: Buffer, epoch: number): number {
    // TODO: Notify {{action}} when {{trigger}} changes
    return 0;
}

{{/effects}}
