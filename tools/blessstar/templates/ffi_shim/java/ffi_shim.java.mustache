// BlessStar FFI Shim — {{biz_id}} / {{display_name}}
// Auto-generated by blessstar spec --lang=java
//
// This file defines JNI native methods.
// The companion ffi_shim_jni.c provides extern "C" implementations.
// Build: javac FfiShim.java && gcc -shared -o libffi_shim.so ffi_shim_jni.c

public class FfiShim_{{biz_id_underscore}} {
    static {
        System.loadLibrary("ffi_shim_{{biz_id_underscore}}");
    }

{{#gates}}
    // {{description}}
    // Rule: {{rule}}
    public static native int gate_{{id}}(String uri, String fieldValue);

{{/gates}}
{{#effects}}
    // Effect: {{trigger}} → {{action}}
    public static native int effect_{{trigger_dot_to_underscore}}(String key, byte[] data, long epoch);

{{/effects}}
}
