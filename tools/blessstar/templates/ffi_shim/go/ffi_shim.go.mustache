package main

// BlessStar FFI Shim — {{biz_id}} / {{display_name}}
// Auto-generated by blessstar spec --lang=go
// Build: go build -buildmode=c-shared -o ffi_shim.dll

/*
#include <stdlib.h>
*/
import "C"
import "unsafe"

{{#gates}}
//export gate_{{id}}
func gate_{{id}}(ctx unsafe.Pointer, uri *C.char, readResult unsafe.Pointer,
                 detailOut *C.char, detailCap C.size_t) C.int {
    // TODO: Implement gate logic for {{field}} — {{rule}}
    return 0 // default: accept
}

{{/gates}}
{{#effects}}
//export effect_{{trigger_dot_to_underscore}}
func effect_{{trigger_dot_to_underscore}}(ctx unsafe.Pointer, key *C.char,
                                           data unsafe.Pointer, length C.size_t,
                                           epoch C.uint64_t) C.int {
    // TODO: Notify {{action}} when {{trigger}} changes
    return 0
}

{{/effects}}

func main() {}
