# 岗位筛选与批量投递架构增强 · 项目修改记录

> **关联 ADR**: `架构方案选择记录.md` — 岗位筛选与批量投递架构增强章节  
> **架构模式**: Port-Adapter 六边形架构（方案B）  
> **Commit 类型**: 新增功能模块（零侵入，不修改已有业务逻辑）

---

## 变更总览

| Phase | 层级 | 新增文件数 | 修改文件数 | 说明 |
|-------|------|-----------|-----------|------|
| 1 | Domain 实体层 | 4 | 0 | 评分/投递核心实体与聚合根 |
| 2 | Port 接口层 | 5 | 0 | 关键词扩展/可信评分/搜索/文件/浏览器接口 |
| 3 | Repository 层 | 2 | 0 | GORM 持久化实现 |
| 4 | Adapter 实现层 | 6 | 0 | 启发式评分/词典扩展/网页搜索/本地文件/浏览器 |
| 5 | Service 编排层 | 2 | 0 | 筛选编排/投递编排 |
| 6 | Scheduler 层 | 1 | 0 | TTL 过期清理定时任务 |
| 7 | 入口集成 | 0 | 4 | 自动迁移/配置键/CLI 命令/HTTP 路由 |
| 8 | 测试 | 6 | 0 | 26 个测试函数 |
| **合计** | | **26** | **4** | **30 个文件变更** |

---

## 详细文件变更列表

### Phase 1：Domain 实体层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/domain/scored_jd.go` | 新增 | ScoredJD 实体 + ScoringStatus/TargetStatus/DeliveryStatus 枚举 + ScoreDetails 值对象 + CalculateTrust/CalculateMatch 方法 | S1（可配置权重）、S2（关键字扩展+可信度双维度） | `scored_jd_test.go` |
| `internal/domain/scoring_session.go` | 新增 | JDScoringSession 聚合根 + AddResult/GetPassedResults/MarkCompleted 方法 + 双阈值过滤逻辑 | S4（双阈值过滤）、S5（状态管理） | `scoring_session_test.go` |
| `internal/domain/delivery_session.go` | 新增 | DeliverySession 聚合根 + NewDeliverySession（TTL 锁定）/AddTarget/IsExpired/RemainingDays 方法 | D1（创建会话）、D5（TTL 缓存过期策略） | `delivery_session_test.go` |
| `internal/domain/delivery_target.go` | 新增 | DeliveryTarget 实体 + NewDeliveryTarget/MarkExported/MarkCompleted 状态机方法 | D2（记录导出目标）、D3（状态追踪） | `delivery_target_test.go` |

### Phase 2：Port 接口层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/port/keyword_expander.go` | 新增 | KeywordExpander 接口 + ExpandedKeywords/MatchDetail 值对象定义 | S2（同义词扩展+类别匹配） | 由 `expander_test.go` 覆盖 |
| `internal/port/trust_scorer.go` | 新增 | TrustScorer + CompanyVerifier 接口 + TrustScore/CompanyInfo 值对象 | S3（薪资/福利/公司/位置综合评分） | 由 `heuristic_test.go` 覆盖 |
| `internal/port/jd_search.go` | 新增 | JDSearchEngine 接口 + JDLink/FetchedJD 值对象 | S2（批量获取 JD） | — |
| `internal/port/file_storage.go` | 新增 | FileStorage 接口（Save/Delete/Exists/Read/FullPath） | D4（文件导出持久化） | — |
| `internal/port/browser_opener.go` | 新增 | BrowserOpener 接口（Open） | D4（打开投递 URL） | — |

### Phase 3：Repository 持久化层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/repository/scoring_repo.go` | 新增 | ScoringSessionModel/ScoredJDModel GORM 模型 + CreateSession/FindSessionByID（含 Results 加载）/UpdateSession/SaveScoredJD/SaveScoredJDBatch/FindPassedBySession | S5（持久化） | — |
| `internal/repository/delivery_repo.go` | 新增 | DeliverySessionModel/DeliveryTargetModel GORM 模型 + CreateSession/FindSessionByID/FindAllSessions/UpdateSession/DeleteSession（CASCADE）/FindExpiredSessions/SaveTarget/SaveTargets/UpdateTarget/DeleteTargetsBySession | D5（TTL 过期查找与删除） | — |

### Phase 4：Adapter 实现层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/adapter/scorer/heuristic.go` | 新增 | HeuristicScorer 实现 TrustScorer + detectSalaryRange/detectBenefits/detectCompanyIntro/detectLocationMatch 启发式检测函数 | S3（五维评分） | `heuristic_test.go` |
| `internal/adapter/keyword/dict.go` | 新增 | 内置同义词词典（20组映射）+ 类别关键词词典（7组） | S2（同义词扩展） | 由 `expander_test.go` 覆盖 |
| `internal/adapter/keyword/expander.go` | 新增 | DictExpander 实现 KeywordExpander + Expand/MatchDetail/MatchScore 方法 | S2（扩展+匹配） | `expander_test.go` |
| `internal/adapter/search/webpage.go` | 新增 | BOSS直聘搜索适配器 + Search/ParseResultPage/FetchDetail 方法 | S2（批量获取 JD） | — |
| `internal/adapter/filestorage/local.go` | 新增 | LocalFileStorage 实现 FileStorage（根目录: ~/.resumedesign/deliveries/） | D4（文件导出） | — |
| `internal/adapter/browser/opener.go` | 新增 | 跨平台 BrowserOpener 实现（windows/darwin/其他） | D4（打开投递 URL） | — |

### Phase 5：Service 编排层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/service/screening_service.go` | 新增 | ScreeningService + StartScoringSession/RunScreening（并行评分+双阈值过滤）/GetScreeningResult/GetPassedResults | S1-S5 | — |
| `internal/service/delivery_service.go` | 新增 | DeliveryService + StartDelivery（简历导出+文件保存+浏览器打开）/ListDeliveries/GetDelivery/RevokeDelivery | D1-D5 | — |

### Phase 6：Scheduler 层

| 文件路径 | 操作 | 变更说明 | 对应不变量 | 对应测试类 |
|---------|------|---------|-----------|-----------|
| `internal/scheduler/cleanup.go` | 新增 | CleanupTask + Start（定时协程）/clean（查找过期→删除文件→删除数据库记录） | D5（TTL 过期清理） | — |

### Phase 7：入口集成（修改现有文件）

| 文件路径 | 操作 | 变更说明 |
|---------|------|---------|
| `internal/repository/db.go` | 修改 | AutoMigrate 新增 4 张表：ScoringSessionModel、ScoredJDModel、DeliverySessionModel、DeliveryTargetModel |
| `internal/port/config_reader.go` | 修改 | 新增 12 个配置键常量（screening.* 8个 + delivery.* 4个） |
| `internal/adapter/config/local.go` | 修改 | ConfigModel 新增 Screening/Delivery 结构体 + 默认值 + Get/Keys/FormatValue 扩展 |
| `cmd/cli/main.go` | 修改 | 新增 7 个 CLI 命令（screening start/run/result, delivery start/list/show/revoke） |
| `cmd/server/main.go` | 修改 | 新增 8 个 REST 路由 + 服务初始化 + CleanupTask 启动 |

### Phase 8：测试编写

| 文件路径 | 测试函数数 | 覆盖内容 |
|---------|-----------|---------|
| `internal/domain/scored_jd_test.go` | 7 | 状态枚举验证（3）+ CalculateTrust（4种场景）+ CalculateMatch（3种场景） |
| `internal/domain/scoring_session_test.go` | 7 | AddResult 通过/被信任拒绝/被匹配拒绝 + GetPassedResults + GetRejectedResults + MarkCompleted + MarkFailed |
| `internal/domain/delivery_session_test.go` | 6 | NewDeliverySession + AddTarget + SetCompleted + IsExpired + RemainingDays + TTL 锁定验证（D5） |
| `internal/domain/delivery_target_test.go` | 4 | NewDeliveryTarget + MarkExported + StatusTransitions + IsExpired |
| `internal/adapter/keyword/expander_test.go` | 6 | Expand（Go后端/去重）+ MatchDetail（部分匹配/无匹配）+ MatchScore + Dict（GetSynonyms/AllCategories） |
| `internal/adapter/scorer/heuristic_test.go` | 10 | detectSalaryRange（3种）+ detectBenefits（2种）+ detectCompanyIntro（2种）+ detectLocationMatch（3种）+ HeuristicScorer（完整/空内容） |
| **合计** | **40** | |

---

## 架构不变量覆盖矩阵

| 不变量 | 描述 | 覆盖层级 | 验证方式 |
|--------|------|---------|---------|
| S1 | 可配置权重 | Port(config_reader) + Adapter(scorer) + Service(screening) | 编译（接口约束） |
| S2 | 关键词扩展+批量JD获取+并行评分 | Port(keyword_expander,jd_search) + Adapter(keyword,search) | 编译 + 单元测试 |
| S3 | 薪资/福利/公司介绍/位置/认证 五维评分 | Port(trust_scorer) + Adapter(heuristic) | 单元测试（10个用例） |
| S4 | 双阈值过滤（trust+match） | Domain(scoring_session) + Service(screening) | 单元测试（拒绝场景） |
| S5 | 筛选会话状态管理 | Domain(scored_jd,scoring_session) + Repository(scoring_repo) | 单元测试（状态转换） |
| D1 | 创建投递会话+TTL锁定 | Domain(delivery_session) + Service(delivery) | 单元测试（TTL 锁定验证） |
| D2 | 记录导出目标 | Domain(delivery_target) + Service(delivery) | 单元测试 |
| D3 | 投递目标状态追踪 | Domain(delivery_target) + Repository(delivery_repo) | 单元测试（状态转换） |
| D4 | 文件导出+浏览器打开 | Port(file_storage,browser_opener) + Adapter(filestorage,browser) | 编译（接口约束） |
| D5 | TTL 缓存过期策略 | Domain(delivery_session) + Scheduler(cleanup) + Repository(delivery_repo) | 单元测试 + 定时任务逻辑 |

---

## 验证结果

| 验证项 | 结果 |
|-------|------|
| `go build ./...` | ✅ 编译通过，无错误 |
| `go vet ./...` | ✅ 无 vet 警告 |
| `go test ./... -count=1` | ✅ 全部 PASS（原有测试 + 新增40个测试函数） |
| 零侵入原则 | ✅ 所有新增代码在 domain/port/adapter/service/repository/scheduler 目录，未修改已有业务逻辑 |
| 静态分析（不变量注释） | ✅ 每个文件头部标注了对应不变量，代码关键位置有 `// Invariant:` 注释 |

---

## 回滚指引

若需回滚此模块，执行以下步骤：

1. **恢复入口文件**（修改过的 4 个文件）：
   - `internal/repository/db.go` — 移除 AutoMigrate 新增的 4 张表
   - `internal/port/config_reader.go` — 移除 12 个配置键常量
   - `internal/adapter/config/local.go` — 移除 Screening/Delivery 结构体和 Get/Keys/FormatValue 扩展
   - `cmd/cli/main.go` — 移除 7 个 CLI 命令和 handler
   - `cmd/server/main.go` — 移除 8 个路由和服务初始化

2. **删除新增目录**（含所有新增文件）：
   ```bash
   rm -rf internal/domain/scored_jd.go
   rm -rf internal/domain/scoring_session.go
   rm -rf internal/domain/delivery_session.go
   rm -rf internal/domain/delivery_target.go
   rm -rf internal/port/keyword_expander.go
   rm -rf internal/port/trust_scorer.go
   rm -rf internal/port/jd_search.go
   rm -rf internal/port/file_storage.go
   rm -rf internal/port/browser_opener.go
   rm -rf internal/repository/scoring_repo.go
   rm -rf internal/repository/delivery_repo.go
   rm -rf internal/adapter/scorer/
   rm -rf internal/adapter/keyword/
   rm -rf internal/adapter/search/
   rm -rf internal/adapter/filestorage/
   rm -rf internal/adapter/browser/
   rm -rf internal/service/screening_service.go
   rm -rf internal/service/delivery_service.go
   rm -rf internal/scheduler/
   ```
