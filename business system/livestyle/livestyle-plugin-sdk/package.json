{
  "name": "livestyle-plugin-sdk",
  "version": "0.1.0",
  "description": "livestyle 组件插件 SDK — 零依赖，仅 TypeScript + Web API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
