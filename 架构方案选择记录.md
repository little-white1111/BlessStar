# 🏛️ 架构方案选择记录 (ADR) — 全链路接通

## 1. 需求背景

打通 **Editor（Electron 前端）↔ 配置文件（JSON）↔ 业务系统** 的完整链路。当前瓶颈：

* AI 管线 1400+ 行 `pipelineManager.ts` 硬编码 BlessStar 领域逻辑，无法复用到其他配置领域

* 配置编辑只有"内部管理"模式，缺少项目级别的导入/导出/分发工作流

* 没有统一的工作区概念来管理多环境、多格式的配置文件

* Editor 侧 AI 操作无法直接映射到业务系统可消费的配置文件变更

**核心目标**：先实现一条完整的「AI 编辑 → 配置文件生成 → 业务系统消费」链路，验证架构可行性，再逐步扩展。

***

## 2. 三大候选方案对比

| 维度            | 方案A (极简运维)                                                | 方案B (性能优先)                                   | 方案C (高扩展) ⬅️ **已选中**                                                                |
| :------------ | :-------------------------------------------------------- | :------------------------------------------- | :---------------------------------------------------------------------------------- |
| **核心思路**      | 原地扩展 `pipelineManager` + 扁平文件直接读写                         | 轻量 Plugin 层 + 内存缓存工作区                        | **完整 Domain Plugin 体系 + Project Workspace 模型**                                      |
| **AI 管线策略**   | `pipelineManager.ts` 中加 `if-else` 分支判断领域                  | 抽取 `ToolRegistry` + `PromptRegistry` 两个轻量注册表 | **完整** **`IDomainPlugin`** **接口**：tools/prompts/concepts/persistence/commands/hooks |
| **导入/导出策略**   | 单文件的"打开→编辑→另存为"，无项目管理                                     | 内存工作区（LRU 缓存活跃配置，进程退出丢失）                     | **磁盘 Project Workspace**：`.blessstar/` 元数据目录 + `src/` + `dist/`                     |
| **编辑→业务系统链路** | Editor 生成 JSON → 业务系统轮询文件变更（inotify/ReadDirectoryChanges） | Editor → SHM 双缓冲 → ConfigConsumer 实时推送       | **Editor → Workspace Build → 格式转换 → 导出 JSON → 业务系统 Import**                         |
| **优点**        | 改动最小，2-3 周可出原型                                            | 编辑体验流畅，SHM 推送延迟 <1ms                         | 领域完全解耦，项目可管理，多环境/多格式原生支持，链路可观测                                                      |
| **缺点**        | `pipelineManager` 继续膨胀到不可维护，无项目管理能力                       | Plugin 接口不够灵活，后续重构成本高                        | 前期开发量大（6-8 周），有过度设计风险                                                               |
| **预估端到端延迟**   | 编辑→保存 50ms，业务感知 500ms（轮询间隔）                               | 编辑→SHM 推送 <1ms                               | 编辑→Build→导出 200ms（含格式转换）                                                            |
| **维护成本**      | ⭐（短期低，长期高 — 技术债累积）                                        | ⭐⭐                                           | ⭐⭐⭐（前期高，但长期分摊）                                                                      |
| **领域可扩展数**    | 1 个（BlessStar 自身，硬编码）                                     | 2-3 个（注册表模式上限）                               | **N 个**（Plugin 注册制，无上限）                                                             |
| **回滚/版本管理**   | ❌ 无                                                       | ❌ 无                                          | ✅ Workspace history/ 支持                                                             |

***

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案 C — 完整 Domain Plugin + Project Workspace
>
> * **AI 管线**：方案 A（Pipeline Engine + Domain Plugin 架构）
>
> * **导入/导出**：方案 B（Project Workspace 模型）
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-01

***

### 📐 架构拓扑图（ASCII 必填）

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           Editor (Electron 进程)                                      │
│                                                                                       │
│  ┌──────────────────────────────┐    ┌───────────────────────────────────────────┐   │
│  │     AI Pipeline Engine        │    │          Project Workspace Frontend        │   │
│  │  ┌────────────────────────┐  │    │                                           │   │
│  │  │  StageOrchestrator      │  │    │  workspaceManager.ts                      │   │
│  │  │  ├─ Router (分句路由)    │  │    │  ├─ create(project_root)                 │   │
│  │  │  ├─ Intent (意图解析)    │──┼────┼─>│  ├─ open(project_root)                 │   │
│  │  │  ├─ Execute (工具执行)   │  │    │  │  ├─ addSource(file, format)            │   │
│  │  │  └─ Render (结果渲染)    │  │    │  │  ├─ readConfig(src_name)               │   │
│  │  └───────────┬────────────┘  │    │  │  ├─ writeConfig(src_name, data)          │   │
│  │               │               │    │  │  ├─ build(target_format)                │   │
│  │  ┌────────────▼────────────┐  │    │  │  └─ export(src, fmt, output_path)       │   │
│  │  │    IDomainPlugin[*]     │  │    │  └───────────┬───────────────────────────┘   │
│  │  │  ┌──────────────────┐  │  │    │               │                              │
│  │  │  │ blessstar/       │  │  │    │               ▼                              │
│  │  │  │ ├─ tools.ts      │  │  │    │  ┌───────────────────────────────────────┐   │
│  │  │  │ ├─ prompts.ts    │  │  │    │  │   FormatDetect                       │   │
│  │  │  │ ├─ concepts.ts   │  │  │    │  │  ├─ detectByMagic(bytes) → format    │   │
│  │  │  │ ├─ persistence.ts│  │  │    │  │  └─ detectByExt(filename) → format   │   │
│  │  │  │ └─ commands.ts   │  │  │    │  └───────────────┬───────────────────────┘   │
│  │  │  └──────────────────┘  │  │    │                   │                          │
│  │  └────────────────────────┘  │    │                   │                          │
│  └──────────────┬───────────────┘    └───────────────────┼──────────────────────────┘
│                 │                                        │
│                 │           IPC (contextBridge)          │
│                 │  window.blessstar.* → ipcRenderer      │
│                 ▼                                        ▼
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                    Electron Main Process (main.ts)                            │   │
│  │                                                                               │   │
│  │  ipcMain.handle('workspace:*', ...)  →  Rust Native Addon (napi-rs)          │   │
│  │  ipcMain.handle('ai:*', ...)         →  Rust Native Addon (napi-rs)          │   │
│  └───────────────────────────────────┬──────────────────────────────────────────┘   │
└──────────────────────────────────────┼──────────────────────────────────────────────┘
                                       │ C ABI (ffi)
                                       ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                     C++ Runtime (Adapter Layer)                                       │
│                                                                                        │
│  ┌─────────────────────────────┐    ┌─────────────────────────────────────────────┐   │
│  │   Workspace Service          │    │     Format Converter                       │   │
│  │   (adapter/workspace/)       │    │     (adapter/parser/config_format/)         │   │
│  │                              │    │                                             │   │
│  │  bs_workspace_create()       │    │  bs_format_convert(                         │   │
│  │  bs_workspace_open()         │────┼─>  v1_json, target_fmt, &out)              │   │
│  │  bs_workspace_add_source()   │    │  ├─ to_json()                              │   │
│  │  bs_workspace_build()        │    │  ├─ to_yaml()                              │   │
│  │  bs_workspace_export()       │    │  ├─ to_toml()                              │   │
│  │                              │    │  └─ to_ini()                               │   │
│  │  .blessstar/ 元数据管理       │    └──────────────────┬──────────────────────────┘   │
│  │  ├─ schema/{schema_id}.json  │                       │                             │
│  │  ├─ history/{timestamp}.json │                       │                             │
│  │  └─ workspace.yaml           │                       │                             │
│  └─────────────────────────────┘    ┌───────────────────▼──────────────────────────┐   │
│                                      │   Schema Derivation + Import/Export         │   │
│  ┌─────────────────────────────┐    │   (adapter/parser/schema_import_export/)      │   │
│  │   DomainPlugin Host          │    │                                               │   │
│  │   (adapter/plugin_host/)     │    │  bs_schema_derive(config_tree) → schema      │   │
│  │                              │    │  bs_schema_import(json_schema) → bs_schema   │   │
│  │  PluginLoader                │    │  bs_schema_export(bs_schema) → json_schema   │   │
│  │  ToolExecutor                │    └─────────────────────┬────────────────────────┘   │
│  │  HookDispatcher              │                          │                            │
│  └─────────────────────────────┘                          │                            │
│                                                            ▼                            │
│                              ┌──────────────────────────────────────────────────────┐   │
│                              │    Kernel Layer (format-agnostic)                    │   │
│                              │                                                      │   │
│                              │  schema/ → gate_chain/ → state/ → pipeline/         │   │
│                              └────────────────────────┬─────────────────────────────┘   │
│                                                       │                                 │
│                              ┌────────────────────────▼─────────────────────────────┐   │
│                              │    WAL Persistence + SHM Publisher                   │   │
│                              │                                                      │   │
│                              │  shm_manager → double_buffer_writer → eventfd        │   │
│                              └────────────────────────┬─────────────────────────────┘   │
└───────────────────────────────────────────────────────┼──────────────────────────────────┘
                                                        │ SHM (共享内存) + eventfd
                                                        ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                     Business System (SDK Consumer)                                    │
│                                                                                        │
│  ┌───────────────────────────────────────────────────────────────────────────────┐    │
│  │  ConfigConsumer / AppSession                                                  │    │
│  │                                                                                │    │
│  │  方式 A (实时推送):                                                             │    │
│  │    consumer = ConfigConsumer(shm_path, efd)                                    │    │
│  │    consumer.subscribe(callback)  →  wait_and_read()                            │    │
│  │                                                                                │    │
│  │  方式 B (文件导入):                                                             │    │
│  │    session = AppSession(manifest.json)                                          │    │
│  │    reload = ConfigReloadSession(session)                                        │    │
│  │    reload.AddFilePath("dist/prod.yaml")                                        │    │
│  │    r = reload.Commit()  → gate_validate → sync_kernel_state                    │    │
│  │                                                                                │    │
│  │  方式 C (供应商格式):                                                            │    │
│  │    NormalizeVendorConfig(RawYaml, "legacy.yml", &result)                       │    │
│  │    → result.v1_bytes → ConfigReloadSession                                     │    │
│  └───────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

> **模块位置说明**：架构图为概念拓扑，实际目录如下
>
> * `plugin-host` 功能由 TypeScript 前端实现（`app/editor/src/ai/engine/toolSystem.ts`、`hookManager.ts` + `app/editor/src/ai/domain-plugin/registry.ts`），C++ 侧无独立模块
>
> * WAL 持久化位于 `adapter/persistence/attach_wal.c`（Adapter 层），非独立顶层模块
>
> * SHM 发布器位于 `app/sdk/src/shm/`（SDK 层），非 Kernel 层

### 核心数据流（端到端）

```
用户输入 NL: "把生产环境的 timeout 改成 30 秒"
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│ AI Pipeline Engine                                               │
│                                                                   │
│  Stage 1: Router                                                  │
│    → 分句: ["把生产环境的 timeout 改成 30 秒"]                     │
│    → 检测到: domain=production, intent=modify, field=timeout       │
│                                                                   │
│  Stage 2: Intent (LLM + DomainPlugin)                             │
│    → IDomainPlugin.getSystemPrompt() → 注入领域知识                │
│    → LLM 输出: {subject: "timeout", intent: "MODIFY",             │
│                 target: "src/prod.yaml", new_value: 30}           │
│                                                                   │
│  Stage 3: Execute (ToolSystem)                                    │
│    → IDomainPlugin.getTools() → 查找 write_config tool             │
│    → tool.execute("src/prod.yaml", "timeout", 30)                 │
│    → WorkspaceManager.writeConfig("src/prod.yaml", ...)            │
│      → IPC → Rust Addon → bs_workspace_write()                    │
│        → format_detect → parse → validate → write_back             │
│    → ExecutionTrace.record({tool, duration, status})              │
│                                                                   │
│  Stage 4: Render                                                   │
│    → 用 DomainPlugin 的 resultFormatter 渲染结果                   │
│    → 显示: "已将 prod.yaml 的 timeout 更新为 30"                   │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│ Workspace Build (手动触发或自动)                                   │
│  bs_workspace_build("project", "json")                            │
│    → 读取 src/prod.yaml → parse → v1_json                          │
│    → gate_validate → 校验通过                                      │
│    → 写入 dist/prod.json (格式转换)                                 │
│    → 写入 .blessstar/history/{timestamp}.json                      │
│    → SHM Publisher → 通知业务系统                                  │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│ Business System (ConfigConsumer)                                  │
│  consumer.wait_and_read(5000) → 收到新数据                        │
│  callback(data, len, version) →                                   │
│    parse_json(data) → reload_config(new_config)                   │
│  → 系统配置生效                                                   │
└─────────────────────────────────────────────────────────────────┘
```

***

### ⚙️ 架构不变量（审查红线）

1. **双向依赖禁止**：`ai-engine-core` → `IDomainPlugin`（引擎调用接口），禁止 DomainPlugin 反向 import 引擎内部状态或直接操作 DOM
2. **Workspace 数据主权**：所有配置文件的读写必须经过 `bs_workspace_*` C ABI，禁止 Editor 直接调用 `fs.writeFile` 写入 `src/` 目录
3. **Plugin 隔离性**：每个 DomainPlugin 运行在自己的命名空间下，禁止跨 Plugin 直接访问对方注册的 tool 或 state
4. **全链路 TraceID**：从 Editor 的 AI 操作到 Kernel 状态变更，每次调用链必须携带 `traceId`（UUID v4），记录到 ExecutionTrace 和 WAL audit log
5. **格式转换双向幂等**：`v1_json ↔ yaml ↔ toml ↔ ini` 转换必须是 round-trip 安全的 — 任意转换两次回到原格式后，语义等价（允许格式化差异，不允许字段丢失）
6. **Workspace 元数据一致性**：`.blessstar/` 目录必须时刻与 `src/` 内容一致。如果检测到 `src/` 文件被外部修改，必须重新扫描并更新 `.blessstar/schema/` 和 `.blessstar/workspace.yaml`
7. **AI 管线执行可观测**：每次 Pipeline 执行必须在 `ExecutionTrace` 中记录 `{traceId, stageName, duration, toolCalls[], status}`，可通过 `window.blessstar.getTrace(traceId)` 查询
8. **门控不可绕过**：从 Workspace 写入的任何配置变更，在写入 Kernel state 前必须经过 Gate Chain 校验。绕开门控的写入路径（如直接写 SHM）视为违规
9. **Plugin 版本契约**：`IDomainPlugin.version` 必须与 `PipelineEngine` 声明的 `supportedPluginVersion` 匹配，不匹配的 Plugin 拒绝加载

***

### 📋 落地建议（给 subtask-b 的指令）

#### 模块拆分

| 模块                        | 路径                                                             | 职责                                                 | 关键接口                                                |
| ------------------------- | -------------------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------- |
| **`ai-engine-core`**      | `app/editor/src/ai/engine/`                                    | 通用 Pipeline Engine，不包含任何领域逻辑                       | `IPipelineStage`, `PipelineContext`, `ToolSystem`   |
| **`ai-domain-plugin`**    | `app/editor/src/ai/domain-plugin/`                             | DomainPlugin 接口定义 + Registry                       | `IDomainPlugin`, `BusinessAdapterRegistry`          |
| **`ai-domain-blessstar`** | `app/editor/src/ai/domains/blessstar/`                         | BlessStar 领域插件实现                                   | `implements IDomainPlugin`                          |
| **`workspace-frontend`**  | `app/editor/src/workspace/`                                    | Workspace 前端服务 + Electron IPC 调用                   | `WorkspaceManager`, `FormatDetect`                  |
| **`workspace-service`**   | `adapter/workspace/`                                           | Workspace 后端 C 服务                                  | `bs_workspace_*` C ABI                              |
| **`format-convert`**      | `adapter/parser/config_format/`                                | 格式双向转换器                                            | `bs_format_convert()` 统一入口                          |
| **`schema-io`**           | `adapter/parser/schema_import_export/`                         | Schema 外部导入/推导                                     | `bs_schema_derive()`, `bs_schema_import()`          |
| **`plugin-host`**         | `app/editor/src/ai/engine/` `app/editor/src/ai/domain-plugin/` | 后端 Plugin 加载 + Hook 分发（TypeScript 前端实现，C++ 侧无独立模块） | `ToolSystem`, `HookManager`, `DomainPluginRegistry` |

#### 架构关键设计代码（核心接口）

**AI Pipeline 侧（TypeScript）：**

```typescript
// ─────────────────── ai/engine/types.ts ───────────────────

interface IPipelineStage {
  id: string;
  execute(ctx: PipelineContext): Promise<StageResult>;
}

interface PipelineContext {
  traceId: string;
  userMessage: string;
  clauses: Clause[];
  currentIntent: Intent | null;
  toolResults: ToolResult[];
  domainPlugin: IDomainPlugin;
  abortSignal: AbortSignal;
  metadata: Record<string, unknown>;  // 附加上下文元数据
}

interface ToolSystem {
  registerTool(tool: IToolDeclaration): void;
  executeTool(name: string, args: Record<string, unknown>,
              ctx: PipelineContext): Promise<ToolResult>;
  getTools(): IToolDeclaration[];
}

// ─────────────────── ai/domain-plugin/types.ts ───────────────────

interface IDomainPlugin {
  id: string;                   // "blessstar" | "myapp"
  version: string;              // semver, e.g. "1.0.0"
  supportedPipelineVersion: string;

  // 领域数据
  tools: IToolDeclaration[];
  getSystemPrompt(): string;
  getConceptKnowledge(): IConceptEntry[];
  getTrieDictionary(): ITrieDict;
  getCommandRegistry(): ICommandEntry[];

  // 持久化
  getPersistenceProvider(): IPersistenceProvider;

  // 生命周期 Hook（可选）
  hooks?: {
    onBeforeStage?: (stageId: string, ctx: PipelineContext) => Promise<void>;
    onAfterStage?: (stageId: string, result: StageResult, ctx: PipelineContext) => Promise<void>;
    onToolResult?: (toolName: string, result: ToolResult, ctx: PipelineContext) => Promise<void>;
  };
}

// ─────────────────── ai/domain-plugin/registry.ts ───────────────────

// 单例实例模式
class DomainPluginRegistryImpl {
  register(plugin: IDomainPlugin): boolean;  // false 表示版本不匹配或 ID 重复被拒绝
  get(id: string): IDomainPlugin | undefined;
  getAll(): IDomainPlugin[];
  getDefault(): IDomainPlugin | undefined;   // 未注册 Plugin 时返回 undefined
}

export const DomainPluginRegistry = new DomainPluginRegistryImpl();
```

**Workspace 侧（C ABI）：**

```c
// ─────────────────── adapter/workspace/include/bs/adapter/workspace/workspace.h ───────────────────

typedef struct bs_workspace_t bs_workspace_t;

/** 工作区生命周期 */
bs_workspace_t* bs_workspace_create(const char* project_root);
int             bs_workspace_open(bs_workspace_t* ws);
int             bs_workspace_add_source(bs_workspace_t* ws,
                                        const char* rel_path,
                                        const char* format);   // "auto" | "yaml" | "json" | "toml" | "ini"
int             bs_workspace_remove_source(bs_workspace_t* ws,
                                           const char* rel_path);

/** 配置读写（走 workspace 管线：detect → parse → validate → cache） */
int bs_workspace_read(bs_workspace_t* ws, const char* src_name,
                      uint8_t** out_data, size_t* out_len);
int bs_workspace_write(bs_workspace_t* ws, const char* src_name,
                       const uint8_t* data, size_t len);

/** 构建 + 导出 */
int bs_workspace_build(bs_workspace_t* ws, const char* target_format);
int bs_workspace_export(bs_workspace_t* ws, const char* src_name,
                        const char* target_format, const char* output_path);

/** 历史管理 */
int  bs_workspace_list_history(bs_workspace_t* ws, const char* src_name,
                               bs_workspace_history_entry_t* entries,
                               size_t* count);
int  bs_workspace_rollback(bs_workspace_t* ws, const char* src_name,
                           uint64_t timestamp);

/** 元数据 */
const char* bs_workspace_get_project_root(bs_workspace_t* ws);
int         bs_workspace_get_source_count(bs_workspace_t* ws);

void bs_workspace_destroy(bs_workspace_t* ws);

// ─────────────────── adapter/parser/config_format/include/bs/adapter/parser/config_format/format_convert.h ───────────────────

typedef enum {
  BS_FORMAT_JSON = 0,
  BS_FORMAT_YAML,
  BS_FORMAT_TOML,
  BS_FORMAT_INI,
  BS_FORMAT_XML,
  BS_FORMAT_AUTO,   // 自动检测
  BS_FORMAT_COUNT,  // 哨兵值 — 内部使用，非公开格式
} bs_format_t;

/**
 * 将 v1 JSON bytes 转换为目标格式。
 * @param v1_json    输入 v1 JSON bytes
 * @param v1_len     输入长度
 * @param target_fmt 目标格式
 * @param out        输出 bytes（调用者 free()）
 * @param out_len    输出长度
 * @return 0=成功, <0=错误码
 */
int bs_format_convert(const uint8_t* v1_json, size_t v1_len,
                      bs_format_t target_fmt,
                      uint8_t** out, size_t* out_len);

/**
 * 自动检测输入格式（基于魔术字/扩展名）。
 * 返回结果结构体，包含检测到的格式和置信度分数。
 */
bs_format_detect_result_t bs_format_detect(const uint8_t* data, size_t len,
                                           const char* filename_hint);
```

#### 测试要求

| 测试层级              | 覆盖范围                                                 | 工具/框架                                                                  | 方法                                                                                |
| ----------------- | ---------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **单元测试**          | `ai-engine-core` 每个 Stage 独立逻辑；`format_convert` 单个格式 | Vitest (TS) + CTest (C++)                                              | Mock `IDomainPlugin`，验证 StageOrchestrator 编排顺序是否正确；对每种格式的 serializer/parser 做正交测试 |
| **契约测试**          | BlessStar DomainPlugin 是否符合 `IDomainPlugin` 接口       | Vitest + `ts-interface-checker`                                        | 编译时 type-check + 运行时验证所有必需字段非空、版本兼容                                               |
| **集成测试**          | `bs_workspace_*` C ABI 的完整生命周期                       | CTest + 临时文件系统                                                         | create → addSource (yaml+json+toml) → write → build → export → 验证 dist/ 输出正确      |
| **Round-trip 测试** | 格式转换双向幂等性                                            | 属性测试（Property-Based Testing），使用 `rapidcheck` (C++) 或 `fast-check` (TS) | 随机生成配置树 → v1\_json → to\_yaml → to\_json → 与原 v1\_json 语义对比；重复 1000 次             |
| **全链路 e2e**       | Editor AI → Workspace → SHM → ConfigConsumer         | Playwright (Electron) + CTest                                          | 启动 Editor → 输入 NL 指令 → 等待 Workspace build → 启动 Consumer 进程 → 验证数据一致               |
| **回归测试**          | 现有 BlessStar 功能不受影响                                  | 现有 CTest 套件                                                            | 完整运行 `ctest --output-on-failure`                                                  |
| **并发测试**          | 多 Editor 窗口同时操作同一 Workspace                          | CTest（多进程）                                                             | 两个进程同时 `bs_workspace_write` 同一文件，验证 WAL 和 CAS 版本控制正确                              |
| **Plugin 隔离测试**   | 两个 DomainPlugin 同时注册                                 | Vitest                                                                 | 注册 pluginA 和 pluginB，验证 getTools() 返回各自的工具集，互不污染                                  |

#### 涉及改动/新增文件清单

**新增文件（AI 管线解耦 — TypeScript）：**

```
app/editor/src/ai/engine/pipelineEngine.ts          ← Pipeline 编排引擎
app/editor/src/ai/engine/stage.ts                    ← Stage 基类 + IPipelineStage
app/editor/src/ai/engine/toolSystem.ts               ← 工具注册/路由/执行
app/editor/src/ai/engine/hookManager.ts              ← Hook 注册/分发
app/editor/src/ai/engine/types.ts                    ← 核心类型定义
app/editor/src/ai/domain-plugin/types.ts             ← IDomainPlugin 接口
app/editor/src/ai/domain-plugin/registry.ts          ← DomainPluginRegistry
app/editor/src/ai/domains/blessstar/index.ts         ← Plugin 入口
app/editor/src/ai/domains/blessstar/tools.ts         ← BlessStar 工具集
app/editor/src/ai/domains/blessstar/prompts.ts       ← BlessStar 领域 Prompt
app/editor/src/ai/domains/blessstar/concepts.ts      ← BlessStar 概念知识
app/editor/src/ai/domains/blessstar/persistence.ts   ← BlessStar 持久化实现
app/editor/src/ai/domains/blessstar/commands.ts      ← BlessStar 命令注册
```

**新增文件（Workspace — TypeScript）：**

```
app/editor/src/workspace/workspaceManager.ts         ← Workspace 前端管理
app/editor/src/workspace/fileWatcher.ts              ← 文件变更监听
app/editor/src/workspace/formatDetect.ts             ← 格式自动检测
```

**新增文件（Workspace — C++）：**

```
adapter/workspace/include/bs/adapter/workspace/workspace.h
adapter/workspace/src/workspace.cpp                   ← 生命周期管理
adapter/workspace/src/workspace_build.cpp              ← Build 管线
adapter/workspace/src/workspace_export.cpp             ← Export 管线
adapter/workspace/src/workspace_history.cpp            ← 历史管理
adapter/CMakeLists.txt                                 ← 新增 workspace 子目录
```

**新增文件（格式转换 — C，通过** **`adapter/CMakeLists.txt`** **直接引用）：**

```
adapter/parser/config_format/include/bs/adapter/parser/config_format/format_convert.h
adapter/parser/config_format/include/bs/adapter/parser/config_format/format_types.h
adapter/parser/config_format/src/format_convert.c      ← 统一入口
adapter/parser/config_format/src/format_detect.c       ← 格式检测
adapter/parser/config_format/src/to_json.c             ← v1_json → JSON 序列化
adapter/parser/config_format/src/to_yaml.c             ← v1_json → YAML 序列化
adapter/parser/config_format/src/to_toml.c             ← v1_json → TOML 序列化
adapter/parser/config_format/src/to_ini.c              ← v1_json → INI 序列化
adapter/CMakeLists.txt                                   ← 直接引用 config_format + schema_import_export 源文件
```

**新增文件（Schema 推导 — C）：**

```
adapter/parser/schema_import_export/include/bs/adapter/parser/schema_import_export/schema_derive.h
adapter/parser/schema_import_export/include/bs/adapter/parser/schema_import_export/schema_import.h
adapter/parser/schema_import_export/src/schema_derive.c
adapter/parser/schema_import_export/src/schema_import.c
```

**修改文件：**

```
app/editor/src/ai/pipeline/pipelineManager.ts  → 拆解重构，领域逻辑迁移到 domains/blessstar/
app/editor/src/ai/context-manager/*.ts         → 改为从 IDomainPlugin 动态获取知识数据
app/editor/src/ai/tools/index.ts               → 工具注册从硬编码改为 DomainPluginRegistry
app/editor/electron/main.ts                    → 新增 workspace:* IPC handler
app/editor/electron/preload.ts                 → 暴露 window.blessstar.workspace.* API
app/editor/src/main.tsx                        → 初始化时加载 DomainPlugin
app/editor/package.json                        → 如有新增 npm 依赖
adapter/cli/src/main.cpp                       → 新增 workspace / format 子命令
CMakeLists.txt                                 → 新增 adapter/workspace 子目录
```

***

# 🏛️ 架构方案选择记录 (ADR) — workspace 加固

## 1. 需求背景

当前 workspace 使用**裸文件 I/O**（`std::ifstream`/`std::ofstream`）读写配置文件，存在以下风险：

* **崩溃不安全**：`bs_workspace_write` 在写入中途崩溃 → 磁盘遗留半截文件，无可恢复手段

* **无版本追踪**：每次写入覆盖原文件，无法回退到历史版本

* **无 fsync 策略**：操作系统可能延迟刷盘，断电后数据丢失

* **与 WAL 持久化脱节**：`adapter/persistence/` 已有完整的原子写入 + WAL + 崩溃恢复 + 版本管理能力（`BsAttachStore`），workspace 未利用

## 2. 三大候选方案对比

| 维度           | 方案A (裸 I/O 加固)                          | 方案B (直接路由 Store)                                                | 方案C (可插拔存储后端) ⬅️ **已选中**                                          |
| :----------- | :-------------------------------------- | :-------------------------------------------------------------- | :---------------------------------------------------------------- |
| **核心思路**     | 原地改为 `FlushFileBuffers`/`_commit` fsync | `bs_workspace_read/write` 直接调用 `BsAttachStore::commit_per_path` | 新增 `StorageBackend` vtable 抽象，两个实现：`LocalDirect` + `PersistStore` |
| **崩溃安全**     | ❌ 低级（仅 fsync，无原子写）                      | ✅ 高级（WAL + epoch）                                               | ✅ 按后端选择                                                           |
| **版本追踪**     | ❌ 无                                     | ✅ BsAttachStore 原生版本                                            | ✅ PersistStore 后端支持                                               |
| **历史/回滚**    | ❌ 不可能                                   | ✅ 基于 WAL epoch 可实现                                              | ✅ 按后端可实现                                                          |
| **实现量**      | \~10 行                                  | \~150 行 (新建 `workspace_persist.cpp` 桥接)                         | \~400 行 (interface + 2 backends)                                  |
| **对测试的影响**   | 无（7/7 不变）                               | 需为 store 预建 manifest 文件                                         | 无（测试用 LocalDirect 后端）                                             |
| **沙箱兼容性**    | ✅                                       | ❌ 依赖 io\_worker 线程                                              | ✅ LocalDirect 后端兼容                                                |
| **生产可用性**    | ❌ 不够可靠                                  | ✅ 成熟可靠                                                          | ✅ 生产用 PersistStore 后端                                             |
| **维护成本**     | ⭐                                       | ⭐⭐                                                              | ⭐⭐⭐                                                               |
| **预估写入 QPS** | \~10000 ops/s                           | \~3000 ops/s (队列化 + fsync)                                      | \~8000 ops/s (Local) / \~3000 ops/s (Persist)                     |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案 C — StorageBackend 可插拔存储后端抽象
>
> * **去 Phase 1**：不单独做 fsync 加固（该代码路径在 Phase 2 中被 `PersistStoreStore` 的原子写入完全覆盖）
>
> * **直接 Phase 2**：实现 StorageBackend interface + `LocalDirectStorage` + `PersistStoreStore`
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-02

### 📐 架构拓扑图

```ascii
┌─────────────────────────────────────────────────────────────────────┐
│  bs_workspace_create / open / add_source / remove / destroy          │
│          (workspace 生命周期，不涉及存储)                              │
└─────────────────────────────────────────────────────────────────────┘
                               │ bs_workspace_read / write / build / export
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Workspace I/O Dispatch Layer                                       │
│                                                                      │
│  每次 read/write 通过 ws->storage->read(path, &buf, &len) 调用       │
│  StorageBackend 对工作区文件透明，workspace 上层不感知后端类型         │
└─────────────────────────────────┬───────────────────────────────────┘
                                  │ StorageBackend* ws->storage
                                  │
          ┌───────────────────────┴───────────────────────┐
          │                                               │
          ▼                                               ▼
┌─────────────────────────┐             ┌─────────────────────────────────┐
│ LocalDirectStorage      │             │ PersistStoreStorage              │
│ (bs_storage_local.c)    │             │ (bs_storage_persist.cpp)         │
│                         │             │                                   │
│  read:  fopen → fread   │             │  read:  BsAttachStore::          │
│  write: fopen → fwrite  │             │         read_by_uri(path)        │
│  fsync: _commit/_flush  │             │  write: BsAttachStore::          │
│  exists: _access        │             │         commit_per_path(path)    │
│  remove: _unlink        │             │         → atomic tmp→rename      │
│                         │             │         → WAL + CRC32            │
│  适用: 沙箱/测试/       │             │         → epoch versioning      │
│        轻量单用户部署    │             │                                   │
│                         │             │  适用: 生产环境/多用户/           │
│  HACK(CreateDirectoryA) │             │        需要历史回滚的场景        │
│  沙箱兼容: ✅            │             │                                   │
│  版本追踪: ❌            │             │  沙箱兼容: ❌ (依赖 io_worker)   │
│  历史回滚: ❌            │             │  版本追踪: ✅                    │
└─────────────────────────┘             │  历史回滚: ✅ (基于 WAL epoch)   │
                                        └─────────────────────────────────┘
```

### 选择 StorageBackend 的方式

```ascii
构造时注入 (构造器模式)

   1. bs_workspace_create() 内部:
         #ifdef BS_TESTING
             ws->storage = LocalDirectStorage_new(ws->project_root);
         #else
             ws->storage = PersistStoreStorage_new(ws->project_root);
         #endif

   2. 用户也可以通过环境变量覆盖:
         BSTORAGE=local   → 强制 LocalDirectStorage
         BSTORAGE=persist → 强制 PersistStoreStorage

   3. 两种后端暴露相同错误码:
         0      成功
         -EINVAL 参数错误
         -ENOENT 文件不存在
         -EIO     I/O 错误
         -ENOMEM  内存不足
```

### ⚙️ 架构不变量（审查红线）

1. **存储后端透明性**：`bs_workspace_build` / `bs_workspace_export` 等上层操作**不得感知**当前使用的 StorageBackend 实现；所有存储调用必须通过 `ws->storage->read/write`
2. **错误码统一**：两种后端必须对相同语义条件返回相同错误码（`-ENOENT` 对文件不存在，`-EIO` 对 I/O 失败等）
3. **路径安全**：`StorageBackend` 必须拒绝 `..` 路径穿越（path traversal），禁止访问 workspace root 之外的任何文件
4. **测试无侵入**：`BS_TESTING` 宏或 `BSTORAGE=local` 环境变量自动启用 `LocalDirectStorage`，现有 7 个 workspace 测试无需任何修改
5. **生产默认**：无 `BS_TESTING` 且无 `BSTORAGE` 覆盖时，默认使用 `PersistStoreStorage`
6. **不变量 #2 不削弱**：即使使用 `PersistStoreStorage` 后端，所有配置文件的读写仍然必须经过 `bs_workspace_*` C ABI（不变量 #2），禁止绕过 workspace 直接调用 `BsAttachStore`

### 📋 落地建议（给 subtask-b 的指令）

#### 模块拆分

| 模块                      | 路径                           | 职责                                                | 关键接口                                                    |
| ----------------------- | ---------------------------- | ------------------------------------------------- | ------------------------------------------------------- |
| **`storage-interface`** | `adapter/workspace/include/` | `StorageBackend` vtable 结构体定义                     | `struct StorageBackend { read, write, exists, remove }` |
| **`storage-local`**     | `adapter/workspace/src/`     | `LocalDirectStorage` 实现                           | `LocalDirectStorage_new(project_root)`                  |
| **`storage-persist`**   | `adapter/workspace/src/`     | `PersistStoreStorage` 实现（包装 `BsAttachStore`）      | `PersistStoreStorage_new(project_root, BsAttachStore*)` |
| **`workspace-core`**    | `adapter/workspace/src/`     | 当前 workspace 生命周期 + build/export（改为通过 storage 读写） | `bs_workspace_*` C ABI                                  |

#### 架构关键设计代码（核心接口）

```c
// ──────────── adapter/workspace/include/bs/adapter/workspace/storage_backend.h ────────────
// (新增)

#ifndef BS_STORAGE_BACKEND_H
#define BS_STORAGE_BACKEND_H

#include <stddef.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/** StorageBackend 虚函数表 — 所有文件 I/O 操作通过此接口 */
typedef struct StorageBackend {
    /** 读取文件全部内容。返回 0=成功, -ENOENT, -EIO, -ENOMEM */
    int (*read)(void* ctx, const char* path,
                uint8_t** out_data, size_t* out_len);
    /** 写入文件（覆盖）。返回 0=成功, -EIO, -ENOMEM */
    int (*write)(void* ctx, const char* path,
                 const uint8_t* data, size_t len);
    /** 检查文件是否存在。返回 1=存在, 0=不存在, <0=错误 */
    int (*exists)(void* ctx, const char* path);
    /** 删除文件。返回 0=成功, -ENOENT, -EIO */
    int (*remove)(void* ctx, const char* path);
    /** 销毁后端，释放资源 */
    void (*destroy)(void* ctx);
} StorageBackend;

#ifdef __cplusplus
}
#endif

#endif /* BS_STORAGE_BACKEND_H */


// ──────────── adapter/workspace/include/bs/adapter/workspace/workspace.h ────────────
// (修改 — bs_workspace_t 增加 storage 成员)

typedef struct bs_workspace_t {
    char                      project_root[1024];
    char                      src_dir[1024];
    char                      dist_dir[1024];
    char                      meta_dir[1024];
    bs_workspace_source_entry_t* sources;
    size_t                    source_count;
    size_t                    source_capacity;
    StorageBackend*           storage;    /* ← 新增 */
} bs_workspace_t;


// ──────────── adapter/workspace/include/bs/adapter/workspace/storage_local.h ────────────
// (新增)

StorageBackend* LocalDirectStorage_new(const char* project_root);


// ──────────── adapter/workspace/include/bs/adapter/workspace/storage_persist.h ────────────
// (新增, #ifndef BS_TESTING 下编译)

#include <bs/adapter/persistence/store.h>

StorageBackend* PersistStoreStorage_new(const char* project_root,
                                         BsAttachStore* store);
```

#### 涉及改动/新增文件清单

**新增文件（3 个）：**

```
adapter/workspace/include/bs/adapter/workspace/storage_backend.h   ← StorageBackend vtable 定义
adapter/workspace/include/bs/adapter/workspace/storage_local.h     ← LocalDirectStorage 声明
adapter/workspace/src/storage_local.cpp                            ← LocalDirectStorage 实现（从 workspace.cpp 提取 I/O 逻辑）
```

**条件编译新增（仅非 BS\_TESTING 时）：**

```
adapter/workspace/include/bs/adapter/workspace/storage_persist.h   ← PersistStoreStorage 声明
adapter/workspace/src/storage_persist.cpp                          ← PersistStoreStorage 实现（包装 BsAttachStore）
```

**修改文件（4 个）：**

```
adapter/workspace/include/bs/adapter/workspace/workspace.h         ← bs_workspace_t 增加 StorageBackend* storage
adapter/workspace/src/workspace.cpp                                ← 所有 read/write 改为 ws->storage->read/write；构造时创建 backend
adapter/CMakeLists.txt                                             ← 新增 storage_local.cpp / storage_persist.cpp
cmake/Tests.cmake                                                  ← 链接 bs_adapter_all 后可自动获取 storage
```

#### 测试要求

| 测试层级       | 覆盖范围                                                              | 方法                                                                          |
| ---------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **单元测试**   | `LocalDirectStorage` 4 个方法（read/write/exists/remove）              | 直接调用各方法，验证 tmp 文件创建、内容正确、路径穿越拒绝                                             |
| **集成测试**   | `bs_workspace_create` → `write` → `read` → `build` → `export` 全链路 | 用 `LocalDirectStorage` 后端跑现有 7 个测试（预期全过）                                    |
| **双后端验证**  | 同一测试用 `LocalDirectStorage` 和 `PersistStoreStorage` 各跑一遍           | `BSTORAGE=local ctest -R workspace` + `BSTORAGE=persist ctest -R workspace` |
| **路径穿越测试** | `read("../../etc/passwd")` 返回 `-EINVAL`                           | 构造恶意路径，验证后端拒绝                                                               |
| **回归测试**   | 全部现有测试不受影响                                                        | `npx vitest run` (56 tests) + `ctest --output-on-failure` (33 tests)        |

***

# 🏛️ 架构方案选择记录 (ADR) — 持久化与通信修复

## 1. 需求背景

当前 Editor ↔ 配置文件 ↔ 业务系统链路存在 3 个已确认的架构级问题：

1. **双写分歧**：`commitBatch` 写入 `ConfigManager` (kernel state) 时不更新 `runtime_values`，`AI write_config_value` 同时更新两者，两条写入路径不同步。`bs_config_read(key)` 读到的是旧值。
2. **冷启动值丢失**：`configPersistLoadFfi` 已加载 `configs.json` → `runtime_values`，但 Editor UI 启动时 `formValues = {}`，从未向后端查询实际值。用户重启后表单空白，只显示 Schema 默认值。
3. **三层持久化真相不明**：`configs.json`（persistWrite）、WAL（BsAttachStore）、workspace `runtime.json`（方案A同步桥）三者写入时机不同，无层级契约，崩溃后可能不一致。

核心目标：统一写入路径，确保（冷启动后读取可回放的最新值、修改配置后只重拉变更的单个值、持久化经过 WAL）。

## 2. 三大候选方案对比

| 维度 | 方案A (极简运维) | 方案B (性能优先) | 方案C (高扩展) |
| :--- | :--- | :--- | :--- |
| **核心思路** | commitBatch handler 尾部追加 `writeBlessStarConfig` + 启动时 `config:init` IPC 全量推送 | commitBatch C ABI 内部直接写 `runtime_values`，消除尾部补写；冷启动用增量 `list_configs` 按需拉取 | 定义三层存储层级契约（WAL=真相 → configs.json=缓存 → workspace=导出），引入 epoch 版本校验确保一致性 |
| **Fix 1 范围** | `main.ts` commitBatch handler + `config_commit_batch_c` 末尾各加一行调 `bs_config_write` | `config_reload_session.cpp` 的 `Commit()` 方法内部直接写 `runtime_values`，无需外部补写 | 在 `AttachContext` 中新增 `sync_runtime_values` 回调函数指针，Commit 完成后自动回调 |
| **Fix 2 范围** | `main.ts` `app.whenReady()` 末尾加 `mainWindow.webContents.send('config:init', json)`，全量推送全部 `runtime_values` | Editor.tsx 启动时调 `list_configs` IPC 按需拉取变化值，避免全量 | `useBlessStar.ts` 中 `subscribeWatch` 监听 StateBus，实时增量推送到 form |
| **Fix 3 范围** | 无（明确 `configs.json` = 写时复制缓存，文档注释说明） | `configs.json` 改为从 ConfigManager 快照生成（`config_manager_export_snapshot()`），消除 `runtime_values` 中间层 | 三层引入 epoch 版本号：WAL epoch → configs.json epoch → workspace timestamp；同步时校验版本，不一致时告警 |
| **优点** | 改动 3 处共 ~10 行，2-3 小时可上线 | 彻底消除双写分歧，无需尾部补写；增量拉取减少网络开销 | 持久化层有明确的层级契约和版本校验，适合多进程/多 Editor 并发场景 |
| **缺点** | 尾部补写是人为约定，后续新增写入路径仍可能漏写 | ConfigReloadSession 内部写 runtime_values 引入 cross-layer 依赖 | 实现量大（约 600 行），版本校验逻辑复杂 |
| **写入路径一致性** | ⭐⭐（靠约定，需 code review 兜底） | ✅（架构层面保证） | ✅（架构 + 运行时校验双重保证） |
| **冷启动 UI 正确性** | ✅ 全量推送，简单可靠 | ✅ 增量拉取，按需查询 | ✅ 监听推送，实时更新 |
| **并发安全** | ❌ 无版本校验，后写覆盖先写 | ⚠️ 单线程安全，多 Editor 窗口仍可能冲突 | ✅ epoch CAS 保证写入有序 |
| **预估改动行数** | ~10 行（TypeScript + C++ 各少量） | ~60 行（C++ ConfigReloadSession + TypeScript IPC） | ~600 行（C++ 三层 + 版本校验 + TypeScript 监听） |
| **维护成本** | ⭐ | ⭐⭐ | ⭐⭐⭐ |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案C — 三层层级契约 + epoch 版本校验 + subscribeWatch 实时推送
>
> * **Fix 1**：`AttachContext` 新增 `sync_runtime_values` 回调，Commit 完成后自动写 `runtime_values`
>
> * **Fix 2**：`useBlessStar.ts` 通过 `subscribeWatch` 监听 StateBus，实时增量推送变更到 form
>
> * **Fix 3**：三层引入 epoch 版本号（WAL epoch → configs.json epoch → workspace timestamp），同步时校验版本
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-02

### 📐 架构拓扑图（ASCII 必填）

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              Editor UI (Renderer 进程)                              │
│                                                                                     │
│  ┌───────────────────────────────────────────────┐                                  │
│  │  SchemaForm                                   │                                  │
│  │  ┌─────────────────────────────────────────┐  │                                  │
│  │  │ formValues ← setFormValues()            │  │                                  │
│  │  │  ↑                                        │  │                                  │
│  │  │  │  config:init (全量启动时)              │  │                                  │
│  │  │  │  config:changed (增量推送)             │  │                                  │
│  │  │  │  subscribeWatch (实时监听)             │  │                                  │
│  │  │  │                                        │  │                                  │
│  │  │  │  handleFormChange → queueChange        │  │                                  │
│  │  │  │    → commitBatch IPC                   │  │                                  │
│  │  └─────────────────────────────────────────┘  │                                  │
│  └──────────────────────┬────────────────────────┘                                  │
│                         │ IPC (contextBridge)                                       │
│                         ▼                                                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                        Electron Main Process (main.ts)                              │
│                                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐                   │
│  │  subscribeWatch ←→ addon.watchSubscribeFfi()                 │                   │
│  │    ↓                                                          │                   │
│  │  config:changed IPC → renderer (增量更新 formValues)          │                   │
│  │                                                               │                   │
│  │  commitBatch handler                                          │                   │
│  │    → configCommitBatchFfi (C ABI)                             │                   │
│  │    → configPersistWriteFfi (写 configs.json)                   │                   │
│  │    → syncRuntimeToWorkspace (写 workspace runtime.json)       │                   │
│  │                                                               │                   │
│  │  config:init handler (启动时)                                  │                   │
│  │    → readAllConfigsFfi → 全量推送 runtime_values 到 form      │                   │
│  └──────────────────────────┬───────────────────────────────────┘                   │
└─────────────────────────────┼───────────────────────────────────────────────────────┘
                              │ napi-rs FFI
                              ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         C++ Adapter Layer                                           │
│                                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐                   │
│  │  ConfigReloadSession::Commit()                                │                   │
│  │    → Gate evaluation                                          │                   │
│  │    → bs_adapter_attach_config_sync_path() → ConfigManager     │                   │
│  │    → ┌──────────────────────────────────────────┐             │                   │
│  │      │ [新] AttachContext.sync_runtime_values    │             │                   │
│  │      │ 回调 → bs_config_write(key, value)        │             │                   │
│  │      │ → runtime_values[key] = value             │             │                   │
│  │      │ → write_audit_log                         │             │                   │
│  │      │ → epoch bump                              │             │                   │
│  │      └──────────────────────────────────────────┘             │                   │
│  │    → StateBus.publish(updated_bytes, epoch)                    │                   │
│  └──────────────────────────────────────────────────────────────┘                   │
│                                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐                   │
│  │  [新] 三层持久化层级关系 (epoch 版本校验)                      │                   │
│  │                                                                 │                   │
│  │  层级1: WAL (BsAttachStore)         ← 系统真相 (SOR)           │                   │
│  │    commit_per_path() → WAL append + fsync + epoch +1           │                   │
│  │    ┌──────────────────────────┐                                │                   │
│  │    │ epoch version check:     │                                │                   │
│  │    │ incoming_epoch == cur+1  │ → accept                       │                   │
│  │    │ incoming_epoch != cur+1  │ → reject (EAGAIN/ESTALE)      │                   │
│  │    └──────────────────────────┘                                │                   │
│  │         ↑                                                       │                   │
│  │         │ 写入时校验 epoch                                      │                   │
│  │         │                                                       │                   │
│  │  层级2: configs.json             ← 写时复制缓存 (Cache)        │                   │
│  │    persistWrite() → 从 runtime_values 序列化为 flat JSON       │                   │
│  │    persistLoad() → 反序列化到 runtime_values                    │                   │
│  │    携带 current_epoch 元数据                                     │                   │
│  │                                                                 │                   │
│  │  层级3: workspace runtime.json    ← 用户可读导出 (Export)       │                   │
│  │    syncRuntimeToWorkspace() → 写 workspace 源文件               │                   │
│  │    携带 timestamp 元数据                                         │                   │
│  │                                                                 │                   │
│  │  崩溃恢复策略:                                                   │                   │
│  │    启动时 persistLoad → 读 configs.json → runtime_values       │                   │
│  │    → 调 WAL.recover(epoch) → 回放未刷盘的 WAL 条目             │                   │
│  │    → persistWrite (覆盖 configs.json 为最新)                    │                   │
│  │    → 如果 configs.json.epoch < WAL.epoch → 告警并自动修复      │                   │
│  └──────────────────────────────────────────────────────────────┘                   │
│                                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐                   │
│  │  StateBus ←→ subscribeWatch (eventfd 通知)                    │                   │
│  │    ConfigManager.publish(updated_bytes) → StateBus            │                   │
│  │    → eventfd write → wake subscribeWatch                     │                   │
│  │    → main.ts config:changed IPC → renderer                    │                   │
│  └──────────────────────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### ⚙️ 架构不变量（审查红线）

1. **写入路径唯一性**：所有配置变更必须经过 `config_commit_batch_c()` + `bs_config_write()`，禁止只写其一的分歧路径。`writeBlessStarConfig` 不得单独调用。
2. **冷启动值可回放**：Editor 启动后 `formValues` 必须反映 `runtime_values` 已持久化的最新值，不得为 `{}`。
3. **持久化层级契约**：WAL（`BsAttachStore`）= 系统真相（System of Record），`configs.json` = 写时复制缓存（Cache），workspace `runtime.json` = 用户可读导出（Export）。上层组件不允许跳过 WAL 直接写 `configs.json`。
4. **增量更新原则**：修改单条配置后，只拉取该条配置的当前值，不允许全量重拉。`subscribeWatch` 每次只推送一个 `{key, value, epoch}` 增量事件。
5. **版本有序**：写操作必须携带 epoch 版本号。`ConfigManager` 只接受 `current_epoch + 1` 的写入，拒绝乱序 (`ESTALE`)。`configs.json` 文件头携带 `epoch` 元数据。
6. **崩溃可恢复**：启动时 `configs.json.epoch < WAL.epoch` → 自动从 WAL 重放缺失条目 → 重写 `configs.json`。不允许静默加载过期缓存。
7. **Watch 不阻塞**：`subscribeWatch` 使用 eventfd 异步通知，禁止在 Watch 回调中执行同步 I/O 或长耗时操作。

### 📋 落地建议（给 subtask-b 的指令）

#### 模块拆分

| 模块 | 路径 | 职责 | 关键接口 |
|------|------|------|----------|
| **`runtime-sync`**（新增） | `adapter/src/attach_runtime_sync.cpp` | `AttachContext` 中 `sync_runtime_values` 回调注册 + 执行 | `bs_adapter_attach_set_runtime_sync_fn()`, `bs_adapter_attach_runtime_sync()` |
| **`epoch-manager`**（新增） | `adapter/persistence/attach_epoch.c` | epoch 版本管理：生成/校验/持久化 | `bs_epoch_next()`, `bs_epoch_validate()`, `bs_epoch_persist()` |
| **`watch-bridge`**（新增） | `app/editor/src/hooks/configWatch.ts` | TypeScript 侧 subscribeWatch 封装 | `useConfigWatch()` hook 输出 `{ values, isWatching, error }` |
| **`init-bridge`**（修改） | `app/editor/src/hooks/useBlessStar.ts` | 启动时拉取 `config:init` 全量 + `subscribeWatch` 增量合并到 `formValues` | 修改现有 `useBlessStar` 返回值 |
| **`config-declare`**（修改） | `app/sdk/src/config_declare.cpp` | `bs_config_persist_write_c` / `bs_config_persist_load_c` 增加 epoch 序列化 | 修改现有函数签名（兼容旧格式） |

#### 架构关键设计代码（核心接口）

```c
// ──────────── adapter/src/attach_runtime_sync.h (新增) ────────────

/** 运行时同步回调类型 — Commit 完成后自动调用 */
typedef int (*bs_runtime_sync_fn)(void* ctx, const char* key,
                                   const uint8_t* data, size_t len,
                                   uint64_t epoch);

/** 设置 AttachContext 的运行时同步回调 */
int bs_adapter_attach_set_runtime_sync_fn(
    void* attach_ctx,
    bs_runtime_sync_fn fn,
    void* fn_ctx);

/** 手动触发一次同步（供 ConfigReloadSession::Commit() 调用）*/
int bs_adapter_attach_runtime_sync(
    void* attach_ctx,
    const char* key,
    const uint8_t* data,
    size_t len,
    uint64_t epoch);


// ──────────── adapter/persistence/attach_epoch.h (新增) ────────────

/** epoch 状态 — 持久化到 configs.json 文件头 */
typedef struct bs_epoch_state {
    uint64_t current_epoch;
    uint64_t wal_epoch;      // WAL 最后提交 epoch
    uint64_t cache_epoch;    // configs.json 快照 epoch
} bs_epoch_state_t;

/** 生成下一个 epoch（单调递增）*/
uint64_t bs_epoch_next(bs_epoch_state_t* state);

/** 校验写入 epoch: 必须 == current_epoch + 1，否则返回 -ESTALE */
int bs_epoch_validate(const bs_epoch_state_t* state, uint64_t incoming_epoch);

/** 持久化 epoch 状态到文件（崩溃安全: 先写 tmp 再 rename）*/
int bs_epoch_persist(const char* path, const bs_epoch_state_t* state);

/** 从文件加载 epoch 状态 */
int bs_epoch_load(const char* path, bs_epoch_state_t* out_state);
```

```typescript
// ──────────── app/editor/src/hooks/configWatch.ts (新增) ────────────

/** useConfigWatch — 实时监听配置变更，增量更新 formValues */
export function useConfigWatch() {
  const [values, setValues] = useState<Record<string, string>>({})
  const [isWatching, setIsWatching] = useState(false)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    // 1. 启动时拉取全量
    window.blessstar.getRegisteredSchemas?.().then(schemas => {
      setValues(schemas)
      setIsWatching(true)
    })

    // 2. 监听增量
    const unsub = window.blessstar.onConfigChanged?.((json: string) => {
      try {
        const data = JSON.parse(json)
        // data = { key, value, epoch }
        setValues(prev => ({ ...prev, [data.key]: data.value }))
      } catch { /* ignore malformed */ }
    })

    return () => { unsub?.() }
  }, [])

  return { values, isWatching, error }
}
```

#### 测试要求

| 测试层级 | 覆盖范围 | 方法 |
|---------|---------|------|
| **单元测试** | `bs_epoch_next` 单调递增、`bs_epoch_validate` 正例/反例 | 调用各函数验证返回值 |
| **单元测试** | `bs_adapter_attach_runtime_sync` 回调注册与执行 | Mock 回调函数，验证调用次数和参数 |
| **单元测试** | `useConfigWatch` hook 行为 | Vitest + @testing-library/react-hooks |
| **集成测试** | Commit → runtime_sync → runtime_values 一致性 | ConfigReloadSession::Commit → 验证 bs_config_read 返回值 = 写入值 |
| **集成测试** | epoch CAS 拒绝乱序写入 | 构造旧 epoch，验证返回 -ESTALE |
| **集成测试** | configs.json epoch 持久化 + 加载 | 写入 → 读取 → 验证 epoch 一致 |
| **集成测试** | 崩溃恢复: configs.json 落后于 WAL | 手动降低 configs.json epoch → 启动 → 验证自动重放 |
| **回归测试** | 全部现有 33 CTest + 56 Vitest | `ctest --output-on-failure` + `npx vitest run` |
| **并发测试** | 双 Editor 窗口同时提交 | 两个进程同时 `bs_workspace_write` 同一 path，验证 epoch CAS 拒绝后写 |

#### 涉及改动/新增文件清单

**新增文件（6个）：**

```
adapter/src/attach_runtime_sync.h          ← sync_runtime_values 回调声明
adapter/src/attach_runtime_sync.cpp        ← 回调注册/执行实现
adapter/persistence/attach_epoch.h         ← epoch 版本管理声明
adapter/persistence/attach_epoch.c         ← epoch 生成/校验/持久化实现
app/editor/src/hooks/configWatch.ts        ← useConfigWatch hook
app/editor/src/__tests__/configWatch.spec.ts  ← useConfigWatch 单元测试
```

**修改文件（7个）：**

```
app/sdk/src/config_declare.cpp             ← persist_write/load 增加 epoch 序列化
app/sdk/include/bs/app/sdk/config_declare.h ← 导出 epoch 相关类型
adapter/src/attach_context_internal.h      ← AttachContext 增加 sync_runtime_fn + fn_ctx + epoch_state
adapter/src/attach_context.h               ← 声明 bs_adapter_attach_set_runtime_sync_fn
adapter/orchestration/reload_batch_controller.cpp ← Commit 成功后调 runtime_sync
app/editor/electron/main.ts                ← subscribeWatch IPC 接入 + config:init 全量推送
app/editor/electron/preload.ts             ← onConfigChanged 回调暴露 configWatch 增量事件
app/editor/src/hooks/useBlessStar.ts       ← 集成 useConfigWatch
app/editor/src/routes/Editor.tsx           ← 启动时取 useConfigWatch 的 values 填入 formValues
adapter/CMakeLists.txt                     ← 新增 attach_runtime_sync.cpp 编译
cmake/Tests.cmake                          ← 新增 epoch / runtime_sync 测试目标
```

---

# 🏛️ 架构方案选择记录 (ADR) — 业务系统目录设计与配置注册规范化

## 1. 需求背景

当前 `Bussiness System/` 是外部目录，无固定结构、无自动注册机制。业务系统接入依赖手动调用 `BusinessAdapterRegistry.register()` (TS) 和 `bs_normalizer_register()` (C)，存在以下问题：

* **不可发现**：新人接入需翻阅文档才知道注册入口，无目录级契约感
* **胶水代码**：每个业务系统重复写 `getFieldDeclarations()` / `getAIData()` 等注册调用
* **无统一管理**：字段声明、AI 元数据、归一化器各自注册，无统一入口和结构保障
* **版本兼容不可控**：业务系统依赖的 SDK 版本无声明，升级可能静默破坏

**核心目标**：定义业务系统目录结构契约，实现配置声明和元数据的自动注册，最小化业务接入胶水代码。

## 2. 三大候选方案对比

| 维度 | 方案A (极简主义) | 方案B (性能优先) | 方案C (弹性扩展) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 维持现状。外部目录 + 文档化接口契约。不新增目录，不增构建复杂度 | 新建 `business/` 目录 + 固定层级 + manifest.yaml 声明式字段 + 编译期静态注册 | 新建 `business/` 目录 + manifest.json + 插件加载器。支持静态/动态双模式，版本隔离，热插拔 |
| **目录结构** | (无。业务系统自行维护) | `business/<biz_id>/` ├── `manifest.yaml` (字段+AI元数据) ├── `src/` (归一化器/门禁源码) ├── `include/` (头文件) └── `test/` (业务测试) | 比方案B多：`manifest.json` (含 sdk_version/dependencies/permissions)，可选 `plugin.dll/.so` (动态加载) |
| **注册机制** | 手动 register() | CMake 扫描 `business/*/manifest.yaml` → 编译期生成 C++ 注册表 → 静态链接 | 启动时扫描 `business/*/manifest.json` → 加载静态注册表 + 可选 `dlopen` 动态插件 |
| **优点** | 零改造成本；业务系统完全自主发布节奏 | 编译期确定，零运行时开销；IDE 可导航；新人只用 manifest 声明字段，无需写注册代码 | 业务系统独立构建；版本隔离，运行时可拒绝不兼容版本；插件热更新 |
| **缺点** | 无可发现性；新人需翻阅文档；胶水代码重复 | 业务系统随框架编译，发布节奏耦合；改 manifest 需重编 | 插件加载器增加实现复杂度；manifest 版本兼容需额外维护 |
| **构建耦合** | 无 | 高（业务代码进 CMake 全量编译） | 中（静态部分进 CMake，动态独立构建） |
| **运行时开销** | 无 | 无 | 插件加载 hook ~微秒级 |
| **维护成本** | ☆（无成本） | ☆☆☆ | ☆☆☆☆ |
| **扩展性** | 弱 | 中 | 强 |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案C（弹性扩展）（确认人：用户/Commander，时间：2026-07-03）

### 📐 架构拓扑图（ASCII必填）

```
┌─────────────────────────────────────────────────────────────────────┐
│  BlessStar 业务系统目录契约 (方案C)                                  │
│                                                                     │
│  ┌───────────────────────┐    ┌──────────────────────────┐          │
│  │ business/             │    │ Framework (框架)           │          │
│  │  ├── my-biz/          │    │                           │          │
│  │  │   ├── manifest.json│───→│ BusinessDirectoryScanner  │          │
│  │  │   ├── src/         │    │  - 扫描 business/*/       │          │
│  │  │   │   ├── normalizer.c  │  - 解析 manifest.json     │          │
│  │  │   │   ├── gate.cpp  │    │  - 版本兼容校验           │          │
│  │  │   │   └── ...      │    └─────────┬────────────────┘          │
│  │  │   ├── include/     │              │                          │
│  │  │   └── test/        │              ▼                          │
│  │  └── another-biz/     │    ┌──────────────────────────┐          │
│  │      ├── manifest.json│    │  注册表 (Registry)        │          │
│  │      └── ...          │    │                           │          │
│  └───────────────────────┘    │  BizRegistry              │          │
│                                │  ├─ fields: 字段声明      │          │
│                                │  ├─ ai_meta: AI 元数据    │          │
│                                │  ├─ normalizers: C fn     │          │
│                                │  └─ gates: 门禁工厂       │          │
│                                └─────────┬────────────────┘          │
│                                          │                          │
│          ┌───────────────────────────────┼─────────┐               │
│          ▼                               ▼         ▼               │
│  ┌──────────────┐  ┌──────────────────┐  ┌──────────────┐          │
│  │ ConfigDeclare  │  │ BusinessAdapter  │  │ GateChain    │          │
│  │ configs.json  │  │ Registry (TS)    │  │ 门禁链       │          │
│  │ epoch 管理     │  │                  │  │              │          │
│  └──────────────┘  └──────────────────┘  └──────────────┘          │
│                                                                     │
│  静态注册路径: 编译期 manifest → C++ 注册表                          │
│  动态注册路径: 启动时 manifest → Registry                            │
│  TS 注册路径:  scanner → JS Bridge → BusinessAdapterRegistry        │
└─────────────────────────────────────────────────────────────────────┘
```

### ⚙️ 架构不变量（审查红线）

1. **禁止业务代码侵入框架核心**：`business/` 目录不可反向依赖 `kernel/` 内部符号，仅可依赖 `app/sdk/include/` 公共 API 和 `adapter/include/` 公共接口。
2. **目录结构契约必须自动可发现**：不允许人工维护业务系统注册列表。`business/<biz_id>/manifest.json` 存在即视为注册意图，框架启动/编译时自动发现。注册内容包括：字段声明 (`fields`)、AI 元数据 (`ai`)、归一化器 (`normalizer`)、门禁 (`gate`)。
3. **隔离性**：一个业务系统的编译失败或运行时崩溃不可影响其他注册的业务系统。动态插件模式下，crash 不可波及主进程。
4. **版本兼容声明**：`manifest.json` 必须声明兼容的 SDK 版本 (`sdk_version`)，格式为 semver range (如 `">=1.0.0 <2.0.0"`)。不兼容时启动时拒绝加载并报明确错误。

### 📋 落地建议（给 subtask-b 的指令）

#### 模块拆分

| 模块 | 路径 | 职责 | 关键接口/文件 |
|------|------|------|-------------|
| **`scanner`**（新增） | `adapter/business/scanner.cpp` | 扫描 `business/*/manifest.json`，解析元数据，调用注册表 | `bs_biz_scanner_scan(dir)` 返回注册请求列表 |
| **`registry`**（新增） | `adapter/business/biz_registry.c` | 集中管理业务系统注册：字段/AI/归一化器/门禁 | `bs_biz_registry_register()` `bs_biz_registry_lookup()` |
| **`manifest-loader`**（新增） | `adapter/business/manifest_loader.c` | 解析 `manifest.json` 为内存结构，含 semver 校验 | `bs_manifest_load(path)` → `bs_manifest_t` |
| **`version-check`**（新增） | `adapter/business/version_check.c` | semver range 匹配 | `bs_version_compatible(sdk_ver, required_range)` → bool |
| **`biz-adapter-bridge`**（修改） | `app/editor/src/ai/business-adapter/bridge.ts` | scanner 扫描结果 → `BusinessAdapterRegistry` 自动注册 | 新增 `autoRegisterFromManifest(manifestList)` |

#### 架构关键设计代码（核心接口）

```c
// ── adapter/business/include/bs/adapter/business/manifest.h (新增) ──

/** manifest.json 解析结果 */
typedef struct bs_manifest {
    char     biz_id[64];         // 业务系统 ID
    char     display_name[128];  // 显示名称
    char     sdk_version[64];    // SDK 版本要求 (semver range)

    // 字段声明
    struct {
        char      key[64];
        char      type[16];      // string / integer / boolean / float
        char      default_str[256];
        char      description[512];
        int       required;      // 0/1
    }* fields;
    size_t fields_count;

    // AI 元数据
    struct {
        // config_labels: key=configKey, value=label
        char** config_label_keys;
        char** config_label_values;
        size_t config_labels_count;

        // inverted_index: key=keyword, values=configKey[]
        struct {
            char keyword[128];
            char** config_keys;
            size_t keys_count;
        }* inverted_index;
        size_t inverted_index_count;

        // domain_shards
        struct {
            char domain_name[128];
            char** keywords;
            size_t keywords_count;
            char domain_description[1024];
        }* domain_shards;
        size_t domain_shards_count;

        // skill_routes
        struct {
            char prefix[64];
            char description[512];
            char** tool_chain;
            size_t tool_chain_count;
            int priority;
        }* skill_routes;
        size_t skill_routes_count;
    } ai_data;

    // 归一化器 (C 函数指针, 可选, 从 normalizer.c 编译注册)
    char normalizer_lib_path[256];  // "" 表示无归一化器

} bs_manifest_t;

void bs_manifest_destroy(bs_manifest_t* m);


// ── adapter/business/include/bs/adapter/business/scanner.h (新增) ──

/** 扫描目录下所有业务系统 manifest */
int bs_biz_scanner_scan(const char* base_dir,
                         bs_manifest_t** out_manifests,
                         size_t* out_count);

/** 释放扫描结果 */
void bs_biz_scanner_free_result(bs_manifest_t* manifests, size_t count);


// ── adapter/business/include/bs/adapter/business/registry.h (新增) ──

/** 向全局注册表注册一个业务系统 */
int bs_biz_registry_register(const bs_manifest_t* manifest);

/** 按 biz_id 查询注册信息 */
const bs_manifest_t* bs_biz_registry_lookup(const char* biz_id);

/** 获取所有已注册 biz_id */
size_t bs_biz_registry_list(char*** out_ids);

/** 注册指定业务的归一化器 (由 normalizer.c 编译后的目标文件注册) */
int bs_biz_registry_register_normalizer(const char* biz_id,
                                         BsNormalizerFn fn);

/** 注册指定业务的自定义门禁 (由 gate.cpp 编译后的目标文件注册) */
int bs_biz_registry_register_gate(const char* biz_id,
                                    const CustomGateEntry* gate);


// ── adapter/business/include/bs/adapter/business/version_check.h (新增) ──

/** 检查 sdk_version 是否满足 manifest 要求的 semver range */
int bs_version_compatible(const char* sdk_version,
                           const char* required_range);
```

```typescript
// ── app/editor/src/ai/business-adapter/bridge.ts (新增) ──

/**
 * autoRegisterFromManifest — 将 scanner 扫描结果自动注册到 TS 侧
 * 由 Electron main process 启动时调用:
 *   1. bs_biz_scanner_scan("business/") → manifest[]
 *   2. 每个 manifest → bridge.autoRegister()
 *   3. bridge 内部创建 IBusinessAdapter → BusinessAdapterRegistry.register()
 */
export function autoRegisterFromManifest(manifests: ManifestJson[]): void {
  for (const m of manifests) {
    const adapter: IBusinessAdapter = {
      id: m.biz_id,
      displayName: m.display_name,
      getFieldDeclarations: () => m.fields.map(f => ({
        key: f.key, type: f.type,
        defaultStr: f.default, description: f.description,
        required: f.required,
      })),
      getAIData: () => ({
        configLabels: Object.fromEntries(
          m.ai_data.config_labels.map(([k, v]) => [k, v])),
        invertedIndex: Object.fromEntries(
          m.ai_data.inverted_index.map(({ keyword, configKeys }) =>
            [keyword, configKeys])),
        domainShards: m.ai_data.domain_shards,
        skillRoutes: m.ai_data.skill_routes,
      }),
      getNormalizer: () => null, // C 侧提供, TS 侧无需归一化
      getSystemPromptIdentity: () => m.display_name,
      getConsultationKnowledge: () => '',
    }
    BusinessAdapterRegistry.register(adapter)
  }
}
```

#### manifest.json 示例

```json
{
  "biz_id": "my-biz",
  "display_name": "我的业务系统",
  "sdk_version": ">=1.0.0 <2.0.0",
  "fields": [
    {
      "key": "room_id",
      "type": "string",
      "default": "",
      "description": "房间 ID",
      "required": true
    },
    {
      "key": "max_players",
      "type": "integer",
      "default": "4",
      "description": "最大玩家数",
      "required": false
    }
  ],
  "ai_data": {
    "config_labels": {
      "room_id": "房间 ID",
      "max_players": "最大玩家数"
    },
    "inverted_index": [
      {
        "keyword": "房间",
        "config_keys": ["room_id"]
      },
      {
        "keyword": "游戏",
        "config_keys": ["room_id", "max_players"]
      }
    ],
    "domain_shards": [
      {
        "domain_name": "game",
        "keywords": ["房间", "游戏", "玩家"],
        "domain_description": "游戏基础配置"
      }
    ],
    "skill_routes": [
      {
        "prefix": "/game",
        "description": "游戏配置管理",
        "tool_chain": ["read_config_value", "write_config_value"],
        "priority": 10
      }
    ]
  }
}
```

#### 测试要求

| 测试层级 | 覆盖范围 | 方法 |
|---------|---------|------|
| **单元测试** | `bs_manifest_load` 解析正例/反例 (合法 JSON / 缺字段 / 版本格式错误) | 构建样本 JSON 字符串，调用解析函数校验 |
| **单元测试** | `bs_version_compatible` semver range 匹配 | 正例: ">=1.0.0", "1.0.0 - 2.0.0", "^1.2.3"；反例: 越界版本 |
| **单元测试** | `bs_biz_scanner_scan` 扫描空目录/单业务/多业务 | 创建临时目录结构，扫描验证 count/names |
| **单元测试** | `bs_biz_registry_register` 注册/查询/列表/重复注册 | 注册后 lookup/duplicate register (应返回已存在) |
| **集成测试** | bridge.ts `autoRegisterFromManifest` → BusinessAdapterRegistry | 模拟 scanner 输出, 验证 TS registry 正确注册字段/AI数据 |
| **集成测试** | 完整流程: manifest.json → scanner → registry → ConfigDeclare 可用 | 端到端验证配置字段通过 manifest 注册后可在 Editor form 中渲染 |
| **回归测试** | 全部现有 CTest + Vitest | `ctest --output-on-failure` + `npx vitest run` |
| **版本拒绝** | 不兼容 sdk_version → 加载拒绝 + 明确错误消息 | 构造 manifest sdk_version=">=99.0.0", 验证拒绝注册 |

#### 涉及改动/新增文件清单

**新增文件（10个）：**

```
adapter/business/include/bs/adapter/business/manifest.h     ← manifest 数据结构
adapter/business/src/manifest_loader.c                      ← manifest.json 解析
adapter/business/include/bs/adapter/business/version_check.h ← semver 校验声明
adapter/business/src/version_check.c                         ← semver 校验实现
adapter/business/include/bs/adapter/business/scanner.h       ← 扫描器声明
adapter/business/src/scanner.c                               ← 扫描器实现
adapter/business/include/bs/adapter/business/registry.h      ← 注册表声明
adapter/business/src/biz_registry.c                           ← 注册表实现
app/editor/src/ai/business-adapter/bridge.ts                 ← TS 侧自动注册桥
adapter/business/CMakeLists.txt                               ← 新模块构建
```

**修改文件（5个）：**

```
adapter/CMakeLists.txt                                       ← add_subdirectory(business)
cmake/Tests.cmake                                            ← 注册新测试目标
app/editor/electron/main.ts                                  ← 启动时调用 scanner → bridge
app/editor/src/ai/business-adapter/index.ts                  ← 导出 bridge
app/sdk/include/bs/app/sdk/normalizer_plugin.h               ← 适配 biz_registry 注册 (若需要)
```

**示例 business 目录 (1个)：**

```
business/.gitkeep                                            ← 占位，说明文档
```

---

# 🏛️ 架构方案选择记录 (ADR) — Gate 失败处方笺

## 1. 需求背景

Gate 链拦截非法配置变更时，当前仅返回 `FAIL + error_message`（如 `font_size must be >= 12`）。用户看到报错不知道如何修改，导致反复试错。要求在报错中附带**可执行的修正建议（处方笺）**，复用 AdaptiveIndex 的缓存命中实现毫秒级响应，陌生错误走 AI 管线生成建议并做 Dry-Run 预校验。

## 2. 三大候选方案对比

| 维度 | 方案A (极简适配器层) | 方案B (联动 AI 管线) ⬅️ **已选中** | 方案C (内核级增强) |
| :--- | :--- | :--- | :--- |
| **核心思路** | Adapter 层拦截 gate 失败结果，插桩结构化错误码 + 静态预置建议 | 全链路整合：gate 失败 → Adapter 拦截 → 查 AdaptiveIndex / 调 LLM → 推送建议到前端 | 在 kernel gate_chain 中加 suggestion 字段，统一输出结构 |
| **建议来源** | 仅 `error_fingerprint → 静态修正表`（硬编码 JSON） | `AdaptiveIndex` 缓存 + `LLM` 双源，含 Dry-Run | 随 gate DAG 节点预注册 `suggest_fn` 回调 |
| **优点** | 改动最小，0 额外依赖 | 智能兜底，冷启动走 LLM、热命中走缓存（毫秒级），用户越用越准 | DAG 原生支持，gate 规则编写者能直接内嵌修正逻辑 |
| **缺点** | 无法覆盖未知错误，维护静态表成本高 | 需新增 `SuggestionGenerator` 模块 + LLM 调用 + Dry-Run 管线 | 污染 kernel 纯引擎职责，破坏分层 |
| **适应性学习** | 无 | ✅ 自动：用户手动改对 → 记录指纹 → 下次命中 | 需 gate 编写者手动配 `suggest_fn` |
| **LLM 调用** | ❌ 从不 | ✅ 仅冷启动/新指纹 | ❌ 从不 |
| **新增模块** | 0 | 1 个 `SuggestionGenerator` (C++) + 前端 `SuggestionRenderer` | 扩充 `bs_gate_node_t` |
| **涉及改动层** | Adapter 层 | Adapter + SDK + Editor（前端） | Kernel + Adapter + SDK + Editor |
| **预估延迟(缓存命中)** | <1ms | <2ms | <1ms |
| **预估延迟(LLM 路径)** | N/A | 2-5s（LLM + Dry-Run） | N/A |
| **维护成本** | ⭐ | ⭐⭐⭐ | ⭐⭐ |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案B — 联动 AI 管线（SuggestionGenerator + AdaptiveIndex + LLM + Dry-Run）
>
> * **建议来源**：AdaptiveIndex（历史成功修复缓存） + LLM（陌生错误冷启动）
>
> * **安全前置信道**：Dry-Run 预校验 — AI 建议必须再次过 Gate 才能返回给用户
>
> * **学习机制**：用户手动修复成功后自动记录 `error_fingerprint → 修正方案`
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-03

### 📐 架构拓扑图（ASCII必填）

```
                    ┌─────────────────────────────────────┐
                    │           Editor (Frontend)          │
                    │  ┌─────────────┐   ┌──────────┐    │
                    │  │  Form UI     │   │ Suggestion│    │
                    │  │  (显示建议)  │   │ Renderer  │    │
                    │  └──────┬──────┘   └────┬─────┘    │
                    │         │               │           │
                    │  ┌──────┴───────────────┴─────┐     │
                    │  │   AI Pipeline (已存在)      │     │
                    │  │   · AdaptiveIndex           │     │
                    │  │   · Stage Intent (LLM)      │     │
                    │  │   · Tool System             │     │
                    │  └──────┬──────────────────────┘     │
                    └─────────┼────────────────────────────┘
                              │ FFI (configCommitBatch)
                              ↓
┌────────────────────────────────────────────────────────────┐
│              SDK Layer (config_reload_session)              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              ReloadBatchController (Adapter)          │  │
│  │  READ → GATE ─→ EXECUTE → PERSIST → REPORT          │  │
│  │              │                                        │  │
│  │              ▼ (失败时)                               │  │
│  │  ┌──────────────────────┐                            │  │
│  │  │ SuggestionGenerator  │  ★ 新增模块                │  │
│  │  │ ① 计算 error_fingerprint                          │  │
│  │  │ ② 查本机指纹缓存 (mem 表)                         │  │
│  │  │ ③ 未命中 → 构造 IR 格式修正 prompt               │  │
│  │  │ ④ Dry-Run: 喂回 Gate 预校验                      │  │
│  │  │ ⑤ 返回 Suggestions[]                             │  │
│  │  └──────────────────────┘                            │  │
│  └──────────────────────────────────────────────────────┘  │
│              │                                              │
│              ↓ (AttachIR)                                   │
└──────────────┼──────────────────────────────────────────────┘
               ↓
┌────────────────────────────────────────────────────────────┐
│            Kernel (Pure Engine, 零变动)                     │
│  ┌─────────────────────┐  ┌──────────────────────┐        │
│  │ Gate Chain (DAG)    │  │ Pipeline Executor    │        │
│  │ DFS 评估 → passed/  │  │ 执行 IR → Report     │        │
│  │ failed + err_msg    │  │                      │        │
│  └─────────────────────┘  └──────────────────────┘        │
└────────────────────────────────────────────────────────────┘


处方笺数据流 (失败路径)

  Gate 失败
     │
     ▼
  error_fingerprint = hash(gate_id + field_key + attempted_value)
     │
     ├──→ 查 AdaptiveIndex (key="fix:" + fingerprint)
     │    ├── 命中 (Source: "AdaptiveIndex") ──→ 直接返回
     │    └── 未命中 ──→ LLM 管线
     │                      │
     │                      ▼
     │               Stage Intent 构建修复 Prompt
     │               (强制输出 AttachIR 格式)
     │                      │
     │                      ▼
     │               Dry-Run: AI 建议再次过 Gate
     │                      │
     │               ├── 通过 → Source: "LLM"
     │               └── 失败 → 降级 "无建议"
     │                      │
     └──────────────────────┘
                             │
                             ▼
                   前端渲染 Suggestions + "一键应用"


AdaptiveIndex 学习流程 (用户手动修复成功时)

  用户手动改值 → Gate 通过 → Commit 成功
     │
     ▼
  记录 error_fingerprint → 用户成功的修改
     │
     ▼
  存入 AdaptiveIndex (key="fix:" + fingerprint)
  下次同指纹 → 直接命中，毫秒响应
```

### ⚙️ 架构不变量（审查红线）

1. **Kernel 零侵入**：gate_chain kernel 不可感知 LLM/Suggestion/AdaptiveIndex，其 `bs_gate_eval_result_t` 结构体不可新增 suggestion 字段
2. **SuggestionGenerator 只读原则**：建议生成模块**不可直接写配置**，必须返回 `Suggestion[]` 让用户/前端决定是否应用
3. **Dry-Run 是强制前置信道**：AI 生成的建议在推送给用户前，必须经过 Gate 预校验（复用 GATE 阶段但不 PERSIST）
4. **指纹不可逆**：`error_fingerprint` 仅做匹配 key，不包含配置值明文，避免缓存泄露敏感数据
5. **AdaptiveIndex 双向隔离**：NLU 路由（现有 key 空间）和错误修正建议（新增 `fix:` 前缀 key 空间）共用存储引擎但隔离

### 📋 落地建议（给 subtask-b 的指令）

- **新增模块**：
  - `adapter/orchestration/src/suggestion_generator.cpp` — 拦截 gate 失败，计算指纹，查/写缓存，构造 LLM Prompt，执行 Dry-Run
  - `adapter/orchestration/include/bs/adapter/orchestration/suggestion_generator.h` — 公开 `bs_suggestion_generate()` / `bs_suggestion_learn()`
  - `app/editor/src/components/SuggestionRenderer.tsx` — 前端建议渲染 + "一键应用"按钮

- **架构关键设计**：
  - `SuggestionGenerator` 内部状态机：`FAIL → FINGERPRINT → CACHE_CHECK → (HIT|LLM) → DRY_RUN → RETURN`
  - `error_fingerprint` 计算：`xxh3_64(gate_id + field_key + attempted_value + schema_version)`
  - 建议应用：通过现有 `executeTool("write_config_value", {key, value})` 路径执行，不走 Commit 管线（避免二次 Gate 死循环）
  - Dry-Run：复用 `reload_batch_controller` 的 GATE 阶段，跳过 READ/PERSIST/EXECUTE

- **涉及改动文件清单**：
  - `adapter/orchestration/reload_batch_controller.cpp` — gate 失败处插桩调用 `suggestion_generator`
  - `app/editor/src/ai/context-manager/adaptiveIndex.ts` — 新增 `fix:` 前缀 key 空间
  - `app/editor/src/components/ConfigEditor.tsx` — Gate 报错处渲染 SuggestionRenderer
  - `adapter/orchestration/CMakeLists.txt` — 添加 `suggestion_generator.cpp`

- **测试要求**：
  - 单元：`SuggestionGenerator` 各路径（缓存命中/LLM/Dry-Run通过/降级）
  - 集成：模拟 Gate 失败 → Generator → 返回 Suggestions → 前端应用 → 二次 Gate 通过
  - 回归：确认 kernel gate_chain 测试不受影响（零变动）

---

# 🏛️ 架构方案选择记录 (ADR) — blessstar 契约开发工作流设计

## 1. 需求背景

业务系统接入 BlessStar 当前依赖手写 `bs_config_declare` + 手写 gate 函数 + 手写 manifest.json + 手写注册胶水。新人接入需翻阅 4 份头文件、理解 3 种注册入口，门槛极高。且这些基础设施全部是 C 接口，但业务系统可能是任意语言（Rust/Go/Python/Java 等），C 接口对非 C 业务系统不友好。

代码生成器方案工作量太大（~4-6 周），MVP 阶段不可行。改为：**定义语言无关的契约格式 `contract.yaml` → `blessstar spec --lang=X` 生成目标语言的落地建议文档 → LLM 消费落地建议生成对应语言的代码 + 测试**。

## 2. 三大候选方案对比

| 维度 | 方案A (极简 Markdown) | 方案B (结构化契约 + blessstar spec) ⬅️ **已选中** | 方案C (DSL + 管线引擎) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 空目录 + 手写模板 README.md，`blessstar spec` 做简单变量替换 | 定义 `contract.yaml` Schema + `blessstar spec` CLI 输出 JSON 注入 Mustache 模板 | 定义完整 DSL（contract + rule + effect 三文件），`blessstar spec` 含阶段编排和 LLM 集成 |
| **契约格式** | 自由 Markdown 模板，无结构约束 | `contract.yaml`（JSON Schema 校验）+ 三个模板层级 | 多文件 DSL |
| **落地建议输出** | 纯文本 Markdown | 结构化 JSON 上下文 + Mustache 渲染的 Markdown 指南 | 可执行 Pipeline 描述 + LLM 可直接消费的 IR |
| **LLM 集成度** | 无 | 生成 Markdown 文档，推荐人工喂给 LLM | 内置 LLM 调用管线 |
| **模板灵活性** | 低 | 中（Mustache 条件/循环 + 段落拼接） | 高 |
| **优点** | 0 代码量 | 工作量适中（~3 天），合同即文档；LLM 出代码远快于手写 | 自动化最高 |
| **缺点** | 无校验易出错 | 仍需人工将落地建议给 LLM（半自动） | 开发管线需 4-6 周 |
| **实现门槛** | 1 人天 | 3 人天 | 6 周 |
| **维护成本** | ⭐（0 维护） | ⭐⭐（维护模板 + Schema） | ⭐⭐⭐⭐（维护 DSL 解析 + 管线 + 模板） |
| **新人上手成本** | 中 | 低（contract.yaml + 落地建议，LLM 生成测试） | 最低 |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案B — 结构化契约 + blessstar spec
>
> * **契约格式**：`contract.yaml`（JSON Schema 校验，覆盖 biz_id/fields/dependencies/gates/effects 五段）—— **语言无关**，任意语言的业务系统共用同一格式
>
> * **落地建议输出**：`IMPLEMENTATION_GUIDE.md`（Mustache 模板渲染）—— **按目标语言输出不同模板**，通过 `--lang` 参数选择（默认 `--lang=c`）
>
> * **LLM 消费方式**：将 `IMPLEMENTATION_GUIDE.md` 喂给 LLM → 生成目标语言的代码 + 测试 → 人工 review gate 核心逻辑
>
> * **blessstar spec 职责**：仅做契约校验 + 模板渲染，不调用 LLM，不写业务代码
>
> * **核心分层**：`contract.yaml` 是语言无关的"事实层"；`--lang` 参数选择"适配层"的模板；BlessStar C kernel 是基础设施（不要求业务系统用 C 写）
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-03

### 📐 架构拓扑图（ASCII必填）

```
                        blessstar 契约开发工作流

  ┌────────────────────────────────────────────────────────────┐
  │  1. 编写契约 (开发者, ~5分钟)                                │
  │                                                             │
  │  livedesign.danmaku/                                        │
  │    └── contract.yaml          ← YAML 格式, 仅声明不编码     │
  │         · biz_id / display_name / version                   │
  │         · fields[] (key/type/default/desc/required)         │
  │         · dependencies[] (外部只读依赖)                     │
  │         · gates[] (约束规则声明, 不实现)                   │
  │         · effects[] (副作用声明, 不实现)                   │
  └────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
  ┌────────────────────────────────────────────────────────────┐
  │  2. blessstar spec (CLI 工具, ~1秒)                         │
  │                                                             │
  │  ┌─────────────────────┐                                    │
  │  │ contract.yaml       │                                    │
  │  │ JSON Schema 校验    │── 不合规 → 报错中断                 │
  │  │ Mustache 模板引擎   │                                     │
  │  └─────────┬───────────┘                                    │
  │            │                                                 │
  │            ▼ IMPLEMENTATION_GUIDE.md                         │
  │  ┌──────────────────────────────────────────────────────┐   │
  │  │ 落地建议文档 (结构化 Markdown)                        │   │
  │  │ · 已有基础设施引用（可复用的头文件/类型/宏）          │   │
  │  │ · 待实现的代码段落（字段声明 / gate 骨架 / effect 骨架）│   │
  │  │ · 测试段落（每个 gate 的验证范围）                    │   │
  │  │ · LLM 上下文约束（签名/宏/路径要求）                  │   │
  │  └──────────────────────────────────────────────────────┘   │
  └────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
  ┌────────────────────────────────────────────────────────────┐
  │  3. LLM 生成代码 (开发者将指南喂给 LLM, ~几分钟)              │
  │                                                             │
  │  IMPLEMENTATION_GUIDE.md (含 --lang 标记)                   │
  │       │                                                     │
  │       ▼  按目标语言产出:                                    │
  │                                                             │
  │  ┌──────C──────┐  ┌──────Rust─────┐  ┌──────Go──────┐    │
  │  │ gates.c     │  │ gates.rs      │  │ gates.go     │    │
  │  │ effects.c   │  │ effects.rs    │  │ effects.go   │    │
  │  │ register.c  │  │ lib.rs        │  │ register.go  │    │
  │  │ test_*.cpp  │  │ tests/        │  │ *_test.go    │    │
  │  └──────┬──────┘  └──────┬────────┘  └──────┬───────┘    │
  │         │                │                   │             │
  │         ▼                ▼                   ▼             │
  │  ┌────────────────────────────────────────────────────┐   │
  │  │ Python / Java / C# / ... (通过 --lang 扩展)       │   │
  │  └────────────────────────────────────────────────────┘   │
  │                                                             │
  └────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
  ┌────────────────────────────────────────────────────────────┐
  │  4. 编译验证 (开发者, ~30秒)                                 │
  │                                                             │
  │  C:     cmake --build && ctest                              │
  │  Rust:  cargo build && cargo test                           │
  │  Go:    go build ./... && go test ./...                     │
  │  Java:  mvn package                                         │
  │  C#:    dotnet build && dotnet test                         │
  │  TS:    npm run build && npm test                           │
  │  Python: pytest (无编译步骤)                                │
  │       │                                                     │
  │       ├── 编译通过 → 测试通过 → ✅ PR ready                 │
  │       └── 失败 → 修正 LLM 输出或人工修补                    │
  └────────────────────────────────────────────────────────────┘


                         运行时视图

  ┌───────────────────────────────────────────────┐
  │            BlessStar 运行时 (C kernel)          │
  │                                                │
  │  业务系统注册 (通过 manifest.json 编译产物):     │
  │    ├── bs_config_declare(fields) ← contract    │
  │    ├── add_gate_fn(gate_rules)   ← contract    │
  │    ├── runtime_sync(effects)     ← contract    │
  │    └── bs_biz_registry_register()← contract    │
  │                                                │
  │  Commit 管线:                                   │
  │    READ → GATE → EXECUTE → PERSIST             │
  │                │                                │
  │                ├── 通过 → 副作用触发            │
  │                └── 拒绝 → 处方笺生成            │
  │                                                │
  │  业务系统语言:                                  │
  │    C/Rust/Go/Python 等通过 FFI/shim 注册       │
  │    或只通过 manifest.json 做元数据注册          │
  └───────────────────────────────────────────────┘
```

### ⚙️ 架构不变量（审查红线）

1. **契约文件即真相**：`contract.yaml` 是业务系统的唯一元数据入口。`manifest.json` 应为 `contract.yaml` 的编译产物（由 `blessstar spec` 顺带生成），**禁止手写 manifest.json**
2. **落地建议只建议不代劳**：`blessstar spec` 永远不可直接修改业务代码（不写任何语言的源文件）。落地建议只是文档，代码必须由 LLM + 人工 review 生成
3. **Gate 逻辑不可完全自动生成**：`contract.yaml` 的 `gates[].rule` 只作描述性声明，具体 gate 函数核心判断逻辑必须手写。LLM 生成的 gate 骨架可通过，但 `out->accepted` 判断逻辑必须人工审核
4. **LLM 输出可编译可验证**：LLM 输出的代码必须能通过目标语言的构建系统验证（C→cmake、Rust→cargo、Go→go build、Python→pytest 等）。违反此条直接打回
5. **契约格式向前兼容**：`contract.yaml` 新增字段必须 optional（带 default），禁止删除已有字段，禁止更改已有字段的类型
6. **业务系统语言不绑定基础设施语言**：BlessStar C kernel 是基础设施，业务系统可选用任何语言实现 gate/effect 逻辑。连接方式可以是 FFI shim、子进程调用、或只通过 manifest.json 做元数据注册（gate/effect 可选延后实现）

### 📋 落地建议（给 subtask-b 的指令）

#### 契约格式模板（contract.yaml）

```yaml
# ============================================================
# contract.yaml — BlessStar 业务系统契约定义
# 版本: 1.0.0
# Schema: https://blessstar.dev/schemas/contract-v1.json
# ============================================================

# ── 元信息 ──
biz_id: "livedesign.danmaku"          # 唯一标识，对应 manifest.biz_id[64]
display_name: "弹幕业务系统"           # 显示名，对应 manifest.display_name[128]
version: "1.2.0"                       # 业务系统自身版本
sdk_version: ">=1.0.0"                 # SDK 版本约束，传给 bs_version_compatible()
description: "弹幕渲染与交互配置"       # 描述，对应 manifest.ai_data.domain_shards[].domain_description

# ── 配置字段声明 ──
# 每个字段对应 bs_config_declare() 的一次调用
fields:
  - key: "livedesign.danmaku.font_size"
    type: int32                         # 可选: int32/int64/string/double/bool/file
    default: "14"                       # 默认值字符串，传给 bs_field_decl_t.default_str
    description: "弹幕字号(像素)"       # 语义描述，传给 bs_field_decl_t.description (ai_hint)
    required: true                      # 必填，传给 bs_field_decl_t.required

  - key: "livedesign.danmaku.opacity"
    type: double
    default: "0.8"
    description: "弹幕透明度(0.0~1.0)"
    required: false

  - key: "livedesign.danmaku.live_playback"
    type: bool
    default: "true"
    description: "是否实时弹幕回放"
    required: false

  - key: "livedesign.danmaku.max_count"
    type: int32
    default: "50"
    description: "同时最多弹幕数"
    required: false

# ── 外部依赖 ──
# 只读依赖：声明本业务系统读取但不由本系统写入的配置字段
# 验证时检查依赖字段的 key 前缀是否存在已注册的业务系统
dependencies:
  - "livedesign.room.layout"            # 依赖 room 业务系统的 layout 配置
  - "livedesign.room.theme_color"       # 依赖 room 业务系统的 theme_color 配置

# ── Gate 规则 ──
# 约束规则声明，只描述"要做什么"和"约束条件"
# 不描述"怎么做"——具体 gate 函数逻辑由 LLM 根据本声明生成
# 每个 gate 对应一个 gate_fn，注册到 reload_batch_controller
gates:
  - id: "font_size_range"
    description: "字号必须在 12-72 之间"
    field: "livedesign.danmaku.font_size"
    rule: "value in [12, 72]"               # 描述性规则，非可执行 DSL
    layer: "default"                         # default / policy / custom
    sub_category: "threshold"                # threshold / alert / approval / enum_check / format
    scenario: "font_size"                    # 匹配 matcher 的场景标签

  - id: "opacity_range"
    description: "透明度必须在 0.0-1.0 之间"
    field: "livedesign.danmaku.opacity"
    rule: "value in [0.0, 1.0]"
    layer: "default"
    sub_category: "threshold"

  - id: "max_count_positive"
    description: "弹幕数必须为正整数"
    field: "livedesign.danmaku.max_count"
    rule: "value > 0"
    layer: "default"
    sub_category: "threshold"

  - id: "dependency_layout_exists"
    description: "弹幕业务依赖 livedesign.room 存在"
    rule: "dependency livedesign.room.layout exists"
    layer: "policy"
    sub_category: "approval"
    dependencies: ["livedesign.room"]

# ── 副作用 ──
# 配置变化通过 Gate 校验后触发的外部动作
# 每个 effect 对应一个 runtime_sync 回调或事件发射
effects:
  - trigger: "livedesign.danmaku.font_size"
    action: "renderer.updateFontSize"         # 通知 Renderer 更新字号
    async: false                               # 同步阻塞直到完成

  - trigger: "livedesign.danmaku.opacity"
    action: "renderer.updateOpacity"
    async: false

  - trigger: "livedesign.danmaku.max_count"
    action: "danmaku.reflow"
    async: true                                # 异步，不阻塞 Commit

  - trigger: "livedesign.danmaku.*"
    action: "danmaku.reflow"                   # 任意字段变化都触发重排
    async: true
```

#### 落地建议模板（IMPLEMENTATION_GUIDE.md）— `--lang=c` 变体

> 以下展示 `--lang=c`（默认变体）的模板样式，面向需要与 BlessStar C kernel 直接 FFI 集成的业务系统。
> `--lang=rust` / `--lang=go` / `--lang=python` 变体同结构，但文件扩展名、构建命令、语言模式不同。

```markdown
# 落地建议：{{biz_id}} / {{display_name}}

> 本指南由 `blessstar spec` 根据 `contract.yaml` 自动生成（`--lang={{lang}}`）。
> 将其喂给 LLM，由 LLM 生成 `{{lang}}` 代码 + 测试。
> 人工 review gate 核心逻辑后合并。

---

## 一、项目上下文（供 LLM 参考）

### 1.1 项目路径
```
{{project_root}}
```

### 1.2 业务系统配置
| 字段 | 值 |
|------|-----|
| biz_id | `{{biz_id}}` |
| version | `{{version}}` |
| sdk_version | `{{sdk_version}}` |

### 1.3 可复用的头文件
```c
// 字段声明
#include "bs/app/sdk/config_declare.h"         // bs_field_decl_t, bs_field_type_t, BS_FIELD, BS_FIELD_REQ
#include "bs/app/sdk/config_api.h"              // bs_config_read, bs_config_write

// Gate 注册
#include "bs/adapter/orchestration/reload_batch_controller.h"
//   → ReloadPathGateFn: int(void* ctx, const char* uri, const IoReadResult* read_result, BsReloadGateDetail* detail_out)
//   → BsReloadGateDetail: char buf[256]

// 副作用注册
#include "bs/adapter/persistence/attach_epoch.h"
//   → bs_runtime_sync_fn: int(void* ctx, const char* key, const uint8_t* data, size_t len, uint64_t epoch)

// manifest 注册
#include "bs/adapter/business/manifest.h"      // bs_manifest_t, bs_manifest_field_t
#include "bs/adapter/business/biz_registry.h"  // bs_biz_registry_register
```

### 1.4 可复用的宏和辅助

```c
// 简便宏 (config_declare.h)
BS_FIELD(key, type, defval_str, desc)        // 声明非必填字段
BS_FIELD_REQ(key, type, defval_str, desc)    // 声明必填字段
BS_CONFIG_FIELDS(...)                        // 包裹字段列表并调用 bs_config_declare()

// 测试宏 (已有 test_*.cpp 中使用)
ASSERT_EQ(a, b)      // 相等断言
ASSERT_NE(a, b)      // 不等断言
ASSERT_TRUE(cond)    // 条件为真
ASSERT_FALSE(cond)   // 条件为假
TEST(name)           // 测试用例开始
PASS()               // 标记通过
FAIL(msg)            // 标记失败并输出信息
```

### 1.5 现有测试入口模板

```cpp
#include "bs_test_util.hpp"  // ASSERT_EQ, TEST, PASS, FAIL 等

static int pass_count = 0;
static int test_count = 0;

// ... 测试用例 ...

int main() {
    // ... 测试执行 ...
    printf("%d/%d passed\\n", pass_count, test_count);
    return (pass_count == test_count) ? 0 : 1;
}
```

---

## 二、待实现的 C 代码

### 2.1 字段声明数组

生成文件：`{{module_dir}}/src/fields.c`

```c
// 期望输出：一个 bs_field_decl_t 数组
// 用 BS_FIELD / BS_FIELD_REQ 宏声明
// 数组通过 BS_CONFIG_FIELDS 宏注册
{{#fields}}
// {{description}}
{{#required}}BS_FIELD_REQ{{/required}}{{^required}}BS_FIELD{{/required}}("{{key}}", BS_TYPE_{{type_upper}}, "{{default}}", "{{description}}"),
{{/fields}}
```

**需要验证的边界**：
{{#fields}}
- `{{key}}` ({{type}})：默认值 `{{default}}`{{#required}}，必填{{/required}}{{^required}}，非必填{{/required}}
{{/fields}}

### 2.2 Gate 函数

生成文件：`{{module_dir}}/src/gates.c`

> 每个 gate 对应一个 `ReloadPathGateFn` 函数。
> `rule` 字段是描述性声明，具体的判断逻辑需要手写/LLM 生成。

{{#gates}}
**gate[{{id}}]** — `{{description}}`
- **规则**：`{{rule}}`
- **层**：`{{layer}}`
- **子分类**：`{{sub_category}}`
- **函数签名**：
```c
static int gate_{{id}}(void* user_ctx, const char* uri,
                       const IoReadResult* read_result,
                       BsReloadGateDetail* detail_out) {
    // --- 实现要点 ---
    // 1. 从 read_result 中提取 {{field}} 的值
    // 2. 判断值是否满足规则 {{rule}}
    // 3. 通过 → return BS_RELOAD_GATE_OK
    // 4. 拒绝 → 填充 detail_out->buf 为错误描述
    //         → return BS_RELOAD_GATE_IR_REJECT
}
```
{{#dependencies}}
- **前置依赖**：{{.}}（需检查注册表中是否存在）
{{/dependencies}}

---

{{/gates}}

### 2.3 副作用函数

生成文件：`{{module_dir}}/src/effects.c`

> 每个 effect 对应一个 `bs_runtime_sync_fn` 回调。
> 触发时机：Gate 通过 → Commit → Persist → runtime_sync。

{{#effects}}
**effect[{{trigger}}]** → `{{action}}`
- **同步/异步**：{{#async}}异步{{/async}}{{^async}}同步{{/async}}
- **函数签名**：
```c
static int effect_{{trigger_dot_to_underscore}}(void* ctx, const char* key,
                                                  const uint8_t* data, size_t len,
                                                  uint64_t epoch) {
    // --- 实现要点 ---
    // 1. {{trigger}} 变化时通知 {{action}}
    // 2. data/len 是变更后的值（UTF-8 JSON 片段）
    // 3. epoch 是当前版本号，可用于排序
    // 4. 返回 0 表示成功
    return 0;
}
```

{{/effects}}

### 2.4 注册入口

生成文件：`{{module_dir}}/src/register.c`

```c
#include "bs/adapter/business/biz_registry.h"   // bs_biz_registry_register
#include "bs/adapter/business/manifest.h"        // bs_manifest_t
#include "bs/app/sdk/config_declare.h"           // bs_config_declare
#include "bs/adapter/persistence/attach_epoch.h" // bs_adapter_attach_set_runtime_sync_fn
// gate 头文件
{{#gates}}
#include "gates.h"
{{/gates}}
// effect 头文件
{{#effects}}
#include "effects.h"
{{/effects}}

// 字段声明 extern
extern bs_field_decl_t g_{{biz_id_underscore}}_fields[];
extern size_t g_{{biz_id_underscore}}_fields_count;

int {{biz_id_underscore}}_module_init(void* adapter_ctx) {
    // Step 1: 声明配置字段
    bs_config_declare(g_{{biz_id_underscore}}_fields, g_{{biz_id_underscore}}_fields_count);

    // Step 2: 注册 Gate
    // 示例：通过 reload_batch_controller 注册 gate_fn
    // add_gate_fn(controller, gate_font_size_range, NULL, 0);

    // Step 3: 注册副作用
    // bs_adapter_attach_set_runtime_sync_fn(adapter_ctx, effect_font_size, ctx);

    // Step 4: 注册 manifest
    bs_manifest_t manifest = {0};
    strncpy(manifest.biz_id, "{{biz_id}}", sizeof(manifest.biz_id) - 1);
    strncpy(manifest.display_name, "{{display_name}}", sizeof(manifest.display_name) - 1);
    strncpy(manifest.sdk_version, "{{sdk_version}}", sizeof(manifest.sdk_version) - 1);
    // ... 填充 fields 和 ai_data ...
    bs_biz_registry_register(&manifest);

    return 0;
}
```

---

## 三、待实现的测试

### 3.1 字段声明测试

| 用例 | 断言 |
|------|------|
| 读取 `{{fields.[0].key}}` 返回默认值 `{{fields.[0].default}}` | `ASSERT_EQ(val, "{{fields.[0].default}}")` |
| 读取 `{{fields.[1].key}}` 返回默认值 `{{fields.[1].default}}` | `ASSERT_EQ(val, "{{fields.[1].default}}")` |
| 读取不存在的 key 返回 NULL | `ASSERT_EQ(val, nullptr)` |

### 3.2 Gate 测试

{{#gates}}
**gate[{{id}}]** — `{{description}}`

| 输入 | 期望结果 |
|------|---------|
| {{rule}} → 合法边界值 | pass |
| {{rule}} → 非法值 | reject |
| {{#dependencies}}依赖 {{.}} 不存在 → reject{{/dependencies}} |

{{/gates}}

### 3.3 副作用测试

{{#effects}}
**effect[{{trigger}}]**
- 模拟 `{{trigger}}` 变更 → runtime_sync 回调被触发
- 验证回调参数 `key == "{{trigger}}"`、`epoch > 0`

{{/effects}}

---

## 四、编译与验证

```bash
# 编译
cd {{project_root}}
cmake --build build --target {{cmake_target}}

# 运行测试
cd build
ctest -R {{test_filter}} -V
```

---

## 五、LLM 输出约束（`--lang=c` 变体）

> 不同 `--lang` 变体的输出约束不同。以下是 C 变体的约束，其他语言在对应模板中定义。

1. 生成的 `.c` 文件放在 `adapter/business/{{biz_id}}/src/`
2. 生成的 `.h` 文件放在 `adapter/business/{{biz_id}}/include/`
3. 生成的测试文件放在 `adapter/test/test_{{biz_id}}.cpp`
4. 所有字符串比较使用 `strcmp`/`strncmp`，不使用 C++ `std::string::operator==`
5. Gate 函数返回 `BS_RELOAD_GATE_OK` 表示通过，`BS_RELOAD_GATE_IR_REJECT` 表示拒绝
6. 运行时 `bs_config_read` 返回 `char*`（malloc 分配），调用者负责 `free`
7. 测试入口返回 `(pass_count == test_count) ? 0 : 1`

> 对于 `--lang=rust`：文件后缀 `.rs`，使用 `cargo test`，通过 FFI 调用 `extern "C"` 包装的 kernel 函数。
> 对于 `--lang=go`：文件后缀 `.go`，使用 `go test`，通过 `cgo` 调用 C kernel。
> 对于 `--lang=python`：文件后缀 `.py`，使用 `pytest`，通过 `ctypes`/`cffi` 调用 C kernel。
```

#### blessstar spec CLI 命令设计

```bash
# 检查 contract.yaml 格式
blessstar spec check livedesign.danmaku/contract.yaml

# 生成落地建议文档 (默认 --lang=c，面向 C kernel 直连)
blessstar spec generate livedesign.danmaku/contract.yaml
# → 输出 livedesign.danmaku/IMPLEMENTATION_GUIDE.md

# 生成 Rust 业务系统的落地建议
blessstar spec generate livedesign.danmaku/contract.yaml --lang=rust
# → 输出 livedesign.danmaku/IMPLEMENTATION_GUIDE.md (Rust 模板)

# 生成 Go 业务系统的落地建议
blessstar spec generate livedesign.danmaku/contract.yaml --lang=go

# 生成 Python 业务系统的落地建议
blessstar spec generate livedesign.danmaku/contract.yaml --lang=python

# 生成 Java 业务系统的落地建议 (JNI)
blessstar spec generate livedesign.danmaku/contract.yaml --lang=java

# 生成 C# 业务系统的落地建议 (P/Invoke)
blessstar spec generate livedesign.danmaku/contract.yaml --lang=cs

# 生成 TypeScript 业务系统的落地建议 (napi-rs)
blessstar spec generate livedesign.danmaku/contract.yaml --lang=ts

# --lang 可扩展: --lang=swift, --lang=zig, --lang=kotlin ...
# 只需 templates/ffi_shim/{lang}/ 下有对应模板文件即可生效

# 生成落地建议 + 同时输出 manifest.json
blessstar spec generate livedesign.danmaku/contract.yaml --manifest
# → 输出 livedesign.danmaku/IMPLEMENTATION_GUIDE.md
# → 输出 livedesign.danmaku/manifest.json (语言无关的编译产物)

# 使用自定义模板目录
blessstar spec generate livedesign.danmaku/contract.yaml --template my-templates/
```

#### 模块拆分

- **新增 CLI 工具**：`tools/blessstar/` 下实现 `blessstar spec` 子命令
  - `tools/blessstar/src/spec.c` — 入口：解析命令行（含 `--lang` 参数），调用 spec 管线
  - `tools/blessstar/src/contract_parser.c` — YAML 解析（初期用简单 key-value 解析，后续换 libyaml）
  - `tools/blessstar/src/contract_validator.c` — 字段存在性 / 类型合法性校验
  - `tools/blessstar/src/spec_renderer.c` — Mustache 模板引擎（自带简易实现，不依赖第三方）
  - `tools/blessstar/templates/` — 模板目录
    - `IMPLEMENTATION_GUIDE.md.mustache` — 默认 (lang=c) 落地建议主模板
    - `IMPLEMENTATION_GUIDE_rust.md.mustache` — Rust 变体模板
    - `IMPLEMENTATION_GUIDE_go.md.mustache` — Go 变体模板
    - `IMPLEMENTATION_GUIDE_python.md.mustache` — Python 变体模板
    - `IMPLEMENTATION_GUIDE_java.md.mustache` — Java 变体模板
    - `IMPLEMENTATION_GUIDE_cs.md.mustache` — C# 变体模板
    - `IMPLEMENTATION_GUIDE_ts.md.mustache` — TypeScript 变体模板
    - `manifest.json.mustache` — manifest 编译产物模板（语言无关）
    - `ffi_shim/` — FFI shim 模板子目录（见下节）

- **涉及改动文件清单**：
  - `CMakeLists.txt` — 新增 `tools/blessstar/` 子目录构建
  - `tools/blessstar/CMakeLists.txt` — CLI 工具构建配置
  - `tools/blessstar/src/spec.c` — 新文件（含 `--lang` 参数解析）
  - `tools/blessstar/src/contract_parser.c` — 新文件
  - `tools/blessstar/src/contract_validator.c` — 新文件
  - `tools/blessstar/src/spec_renderer.c` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE.md.mustache` — 新文件 (lang=c 默认)
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_rust.md.mustache` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_go.md.mustache` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_python.md.mustache` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_java.md.mustache` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_cs.md.mustache` — 新文件
  - `tools/blessstar/templates/IMPLEMENTATION_GUIDE_ts.md.mustache` — 新文件
  - `tools/blessstar/templates/manifest.json.mustache` — 新文件

#### 测试要求

- **单元测试**：
  - `contract_parser`：解析完整 contract.yaml → 正确提取所有字段
  - `contract_validator`：缺少必填字段 → 报错；类型不合法 → 报错；重复 key → 报错
  - `spec_renderer`：模板变量替换正确；条件分支正确；循环展开正确
- **集成测试**：
  - `blessstar spec check` 对合法 contract.yaml 返回 0
  - `blessstar spec check` 对非法 contract.yaml 返回非 0 并输出错误信息
  - `blessstar spec generate` 产出 IMPLEMENTATION_GUIDE.md，包含所有字段/gate/effect 段落
- **端到端测试（与 biz_* 测试联动）**：
  - 用 `blessstar spec generate` 输出落地建议 → 喂给 LLM → LLM 产出代码 → 编译 → ctest 通过

---

# 🏛️ 架构方案选择记录 (ADR) — blessstar 多语言业务系统集成方案

## 1. 需求背景

`contract.yaml` 是语言无关的，但运行时的 gate 校验和副作用回调是 C 函数指针（`ReloadPathGateFn`、`bs_runtime_sync_fn`）。Python/Go/Rust/Java 等语言的业务系统无法直接注册这些 C 回调。需要定义一套多语言集成方案，让非 C 业务系统也能参与 Commit 管线的 gate/effect 生命周期。

## 2. 三大候选方案对比

| 维度 | 方案A (纯元数据) | 方案B (Hybrid FFI) ⬅️ **已选中** | 方案C (IPC 桥接) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 业务系统只提交 contract.yaml，简单 gate（range/enum/format）由 kernel DAG 编译器自动处理。不提供自定义 gate/effect 能力 | **分层集成：简单 gate → kernel DAG 自动处理；复杂 gate/effect → FFI shim 包装目标语言函数。`blessstar spec --lang=x` 生成目标语言特定的 FFI shim 骨架 + 适配器代码** | 业务系统作为独立进程运行，通过 protobuf/gRPC 与 C kernel 交互。gate 校验和副作用执行走 IPC 调用 |
| **业务系统工作量** | 只写 contract.yaml（5 分钟），0 行运行时代码 | **简单场景：0 行代码。复杂场景：写业务语言的门禁逻辑 + 少量 FFI 绑定（由模板生成）** | 写业务语言的 gRPC client + protobuf schema + 进程管理 |
| **Custom gate 支持** | ❌ 不支持（仅限 kernel 内置规则） | **✅ 支持（FFI shim 回调目标语言）** | ✅ 支持（IPC 调用目标语言进程） |
| **Effect 支持** | ❌ 不支持 | **✅ 支持（FFI shim，适合轻量级副作用）** | ✅ 支持（IPC，适合重量级副作用） |
| **Gate 延迟** | <1μs（纯 C DAG） | **<10μs（FFI 跨语言调用栈）** | >1ms（IPC 序列化 + 进程切换） |
| **部署复杂度** | ⭐ 最低 | **⭐⭐（需编译 FFI 共享库）** | ⭐⭐⭐⭐（需管理业务系统进程） |
| **适用场景** | 纯声明型业务（仅 range 校验） | **中等复杂度，需自定义校验逻辑** | 门禁逻辑重、有独立部署诉求 |
| **实现成本** | 0 新增 | **5 人天（FFI shim 模板 + C kernel 侧注册 API 扩展）** | 4 周（IPC 协议 + 连接管理） |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案B — Hybrid FFI（分层集成：简单规则 → kernel DAG 自动处理；复杂逻辑 → FFI shim 包装目标语言函数）
>
> * **核心分层结构**：
>   - **L0 — kernel DAG 自动编译**：`sub_category=threshold|format|enum_check` 的简单规则由 `bs_gate_ast_compile` 从 contract.yaml 自动编译为 DAG 节点，业务系统不需要写任何 gate 代码
>   - **L1 — contract.yaml 注册声明**：业务系统通过 `contract.yaml` 声明字段、依赖、gate 规则和 effect 意图，`blessstar spec` 编译出 `manifest.json`，由 C kernel 在启动时加载并注册
>   - **L2 — FFI shim 桥接**：`sub_category=alert|approval|custom` 的复杂 gate/effect 通过 FFI shim 回调目标语言。`blessstar spec generate --lang=x` 自动生成 shim 骨架
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-03

### 📐 架构拓扑图（ASCII必填）

```
                       多语言集成分层架构

  ┌──────────────────────────────────────────────────────────────────┐
  │              contract.yaml (语言无关的事实层)                      │
  │  fields / dependencies / gates[].rule / effects[].trigger        │
  │                                                                  │
  │  每个 gate 的 sub_category 决定集成层级：                          │
  │    threshold|format|enum_check → L0 (kernel DAG, 纯C)            │
  │    alert|approval|custom      → L2 (FFI shim, 目标语言)          │
  └──────────────────────────┬───────────────────────────────────────┘
                             │ blessstar spec generate --manifest
                             ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │                    manifest.json (编译产物)                        │
  │  biz_id, fields[], gates[] (含 layer/sub_category), effects[]    │
  └──────────────────────────┬───────────────────────────────────────┘
                             │ BlessStar 启动时加载
                             ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │            BlessStar C Kernel (基础设施层)                        │
  │                                                                  │
  │  ┌──────────────────────────────────────────────────────────┐    │
  │  │ 启动注册管线                                               │    │
  │  │                                                           │    │
  │  │  ┌──────────┐   ┌──────────┐   ┌──────────┐             │    │
  │  │  │ L0:      │   │ L1:      │   │ L2:      │             │    │
  │  │  │ DAG 编译 │   │ manifest │   │ FFI shim │             │    │
  │  │  │ 自动注册  │   │ 注册     │   │ dlopen   │             │    │
  │  │  └────┬─────┘   └────┬─────┘   └────┬─────┘             │    │
  │  │       │               │               │                   │    │
  │  │       ▼               ▼               ▼                   │    │
  │  │  ┌──────────────────────────────────────────┐             │    │
  │  │  │                 gate_chain                │             │    │
  │  │  │   DAG 节点 (内置) + FFI 节点 (动态加载)    │             │    │
  │  │  └──────────────────────────────────────────┘             │    │
  │  └──────────────────────────────────────────────────────────┘    │
  │                                                                  │
  │  Commit 管线:                                                    │
  │    READ → GATE → EXECUTE → PERSIST                              │
  │            │                                                     │
  │            ├── 通过 → 副作用触发                                  │
  │            │          ├── L0: 无副作用                            │
  │            │          ├── L1: manifest 注册的 effect 枚举         │
  │            │          └── L2: FFI shim 回调目标语言 effect 函数   │
  │            └── 拒绝 → 处方笺生成                                  │
  └──────────────────────┬────────────────────────────────────────────┘
                         │ FFI 边界
                         ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │              业务系统 (目标语言 — 任意语言)                       │
   │                                                                  │
   │  ┌──────────────────────────────────────────────────┐            │
   │  │  ffi_shim.{c/rs/go/py/java/cs/ts/...}           │            │
   │  │  (由 blessstar spec --lang=x 生成)              │            │
   │  │                                                  │            │
   │  │  // 暴露约定的符号给 kernel dlopen:               │            │
   │  │  // C:     extern "C" int gate_alert_fn(...)     │            │
   │  │  // Rust:  #[no_mangle] pub extern "C" fn       │            │
   │  │  // Go:    //export func                         │            │
   │  │  // Java:  JNIEXPORT jint JNICALL Java_...       │            │
   │  │  // C#:    [UnmanagedCallersOnly]                │            │
   │  │  // TS:    napi_create_function(...)             │            │
   │  │  // 内部实现调用目标语言的业务逻辑                │            │
   │  └──────────────────────────────────────────────────┘            │
  │                                                                  │
  │  ┌──────────────────────────────────────────────────┐            │
  │  │  业务核心逻辑 (目标语言原生实现)                    │            │
  │  │  // Rust: fn validate_threshold(val: i32) -> bool│            │
  │  │  // Go:    func ValidateThreshold(val int) bool  │            │
  │  │  // Python: def validate_threshold(val): bool    │            │
  │  └──────────────────────────────────────────────────┘            │
  └──────────────────────────────────────────────────────────────────┘


                       运行时的层级判定流程

  配置变更请求
       │
       ▼
  ┌──────────────────┐
  │ 遍历 gates[]     │
  │ 逐 gate 判定层级 │
  └──────┬───────────┘
         │
    ┌────┴────┐
    │sub_cat= │
    │threshold│  ←── L0 — kernel DAG 自动编译
    │format   │
    │enum_chk │       bs_gate_ast_compile() 从 contract.yaml 的
    └────┬────┘       rule 描述直接构建 DAG 节点，零代码
         │
         ▼
  ┌──────────────────┐   通过
  │ DAG 节点评估     │──────→ CONTINUE
  │ (纯C, <1μs)     │
  │ 失败             │
  └──────┬───────────┘
         │
         ▼ reject → 处方案 + 停止管线
         │
    ┌────┴────┐
    │sub_cat= │
    │alert    │  ←── L2 — FFI shim
    │approval │
    │custom   │       kernel 通过 dlopen 加载 shim 库
    └────┬────┘       调用 shim 中 extern "C" 函数
         │            shim 内部转发到目标语言实现
         ▼
  ┌──────────────────┐   通过
  │ FFI shim 调用    │──────→ CONTINUE
  │ (<10μs 跨语言)   │
  │ 失败             │
  └──────┬───────────┘
         │
         ▼ reject → 处方案 + 停止管线


                        模板生成的代码结构

  livedesign.danmaku/
    ├── contract.yaml              # 开发者手写 (5分钟)
    ├── manifest.json              # blessstar spec --manifest 生成
    ├── ffi_shim.c                 # blessstar spec --lang=c 生成
    ├── gates.h                    # blessstar spec --lang=c 生成
    ├── effects.h                  # blessstar spec --lang=c 生成
    ├── src/
    │   ├── ffi_shim.c             # ← 开发者填充核心逻辑
    │   └── biz_logic.c           # ← 开发者实现（也可用其他语言）
    └── tests/
        └── test_gates.c           # blessstar spec 生成测试骨架

  --lang=rust 时 ffi_shim.rs 的模板结构:
    livedesign.danmaku/
      ├── contract.yaml            # 不变
      ├── ffi_shim.rs              # 由 --lang=rust 生成
      │   #[no_mangle]
      │   pub extern "C" fn gate_{{id}}(...) -> i32 {
      │       // 调用 Rust 业务逻辑
      │       biz::validate_threshold(val)
      │   }
      ├── Cargo.toml               # 生成 cargo 项目骨架
      └── src/
          └── lib.rs               # Rust 业务逻辑

  --lang=python 时 ffi_shim.py 的模板结构:
    livedesign.danmaku/
      ├── contract.yaml            # 不变
      ├── ffi_shim.py              # 由 --lang=python 生成
      │   from ctypes import CFUNCTYPE, c_int, c_char_p
      │   @CFUNCTYPE(c_int, c_char_p, c_int, c_char_p)
      │   def gate_{{id}}(key, old_val, new_val):
      │       # 调用 Python 业务逻辑
      │       return 0 if biz.validate(..., ...) else -1
      └── biz_logic.py             # Python 业务逻辑

  --lang=java 时 — JNI 模式:
    livedesign.danmaku/
      ├── contract.yaml
      ├── ffi_shim.java            # 由 --lang=java 生成
      │   public class FfiShim {
      │       // JNI native 方法, kernel 通过 dlopen 加载 .so
      │       public static native int gateAlert(String key, String val);
      │   }
      ├── ffi_shim_jni.c           # JNI 桥接 (也由模板生成)
      │   JNIEXPORT jint JNICALL Java_FfiShim_gateAlert(
      │       JNIEnv* env, jclass cls, jstring key, jstring val) {
      │       return biz_java_validate(key, val);
      │   }
      ├── pom.xml                  # Maven 骨架
      └── src/main/java/           # Java 业务逻辑

  --lang=cs 时 — P/Invoke + UnmanagedCallersOnly 模式:
    livedesign.danmaku/
      ├── contract.yaml
      ├── ffi_shim.cs              # 由 --lang=cs 生成
      │   class FfiShim {
      │       [UnmanagedCallersOnly(CallConvs = new[]{typeof(CallConvCdecl)})]
      │       public static int GateAlert(IntPtr keyPtr, IntPtr valPtr) {
      │           string key = Marshal.PtrToStringUTF8(keyPtr);
      │           return Biz.Validate(key, ...) ? 0 : -1;
      │       }
      │   }
      ├── FfiShim.csproj           # 项目文件
      └── BizLogic.cs              # C# 业务逻辑

  --lang=ts 时 — Node.js N-API 模式:
    livedesign.danmaku/
      ├── contract.yaml
      ├── ffi_shim.ts              # 由 --lang=ts 生成
      │   import { napi } from '@napi-rs/core';
      │   // 编译为 .node 文件, kernel 通过 dlopen 加载
      │   export function gateAlert(key: string, val: string): number {
      │       return biz.validate(key, val) ? 0 : -1;
      │   }
      ├── package.json             # npm 项目
      ├── Cargo.toml               # napi-rs 构建配置
      └── src/biz.ts               # TypeScript 业务逻辑
```

### ⚙️ 架构不变量（审查红线）

1. **L0 gate 不可为 L2 写代码**：`sub_category=threshold|format|enum_check` 的 gate 规则由 kernel `bs_gate_ast_compile` 自动从 `contract.yaml` 编译为 DAG 节点。业务系统**不允许也不需要在 ffi_shim 中重复实现这些规则**
2. **FFI shim 由模板生成，不手工写**：FFI 绑定代码（`extern "C"`、`#[no_mangle]`、`ctypes.CFUNCTYPE`、`cgo` 等）由 `blessstar spec generate --lang=x` 自动输出。开发者**只允许修改 shim 内部调用的业务逻辑函数**，不允许修改 FFI 边界签名
3. **Shim 生命周期绑定 kernel**：FFI shim 编译为 `.dll/.so/.dylib`，由 C kernel 在 `module_init` 时通过 `dlopen`/`LoadLibrary` 动态加载。业务系统不需要链接到 BlessStar 二进制，kernel 崩溃不影响 shim 进程（事实上 shim 在 kernel 进程内）
4. **解释型语言允许无编译步骤**：Python 等解释型语言的 FFI shim 模板使用 `ctypes.CDLL`/`cffi.FFI` 直接加载 kernel 导出符号，不需要编译 C 扩展，只需要运行时解释器。编译型语言（Rust/Go/Java C#/TS napi）需要目标语言的编译器工具链，模板会同时生成对应语言的构建配置（`Cargo.toml`/`go.mod`/`pom.xml`/`.csproj`/`package.json`）
5. **manifest.json 是唯一注册凭证**：`blessstar spec` 产出的 `manifest.json` 包含所有 L0/L1 注册信息（不含 L2 shim 路径，shim 路径由 `blessstar spec` 另行生成注册配置）。启动时 kernel 扫描 `biz/*/manifest.json` 完成注册，不依赖业务系统进程是否存活

### 📋 落地建议（给 subtask-b 的指令）

- **C kernel 侧新增**：
  - `kernel/gate_chain/src/gate_dag_compiler.c` — 新增模块：从 contract.yaml 的 `gates[].rule` 描述自动编译为 DAG 节点（`bs_gate_ast_compile_condition` 的 wrapper）。支持 `value in [a, b]`、`value > N`、`value < N`、`value == X`、`value != X` 等常见模式
  - `kernel/gate_chain/include/bs/kernel/gate_chain/gate_dag_compiler.h` — 公开 `bs_gate_dag_compile_from_yaml_rule()`
  - `adapter/orchestration/src/ffi_loader.c` — 新增模块：`dlopen`/`LoadLibrary` 加载 FFI shim 共享库，按 `contract.yaml` 中 `gates[].id` 查找 `extern "C"` 符号并注册到 `reload_batch_controller`
  - `adapter/orchestration/include/bs/adapter/orchestration/ffi_loader.h` — 公开 `bs_ffi_loader_load_shim()` / `bs_ffi_loader_unload_shim()`

- **业务系统侧模板（由 blessstar spec 生成）**：
  - `tools/blessstar/templates/ffi_shim/` — FFI shim 模板目录，按语言名组织：
    - `ffi_shim.c.mustache` — C（默认，`--lang=c`）
    - `ffi_shim/rust/ffi_shim.rs.mustache` — Rust（`--lang=rust`，`#[no_mangle] pub extern "C" fn`）
    - `ffi_shim/go/ffi_shim.go.mustache` — Go（`--lang=go`，`//export` + `import "C"`）
    - `ffi_shim/python/ffi_shim.py.mustache` — Python（`--lang=python`，`ctypes.CFUNCTYPE` + `CDLL`）
    - `ffi_shim/java/ffi_shim.java.mustache` — Java（`--lang=java`，JNI `extern "C"` + `System.loadLibrary`）
    - `ffi_shim/cs/ffi_shim.cs.mustache` — C#（`--lang=cs`，`[DllImport]` + `P/Invoke`）
    - `ffi_shim/ts/ffi_shim.ts.mustache` — TypeScript（`--lang=ts`，Node.js `napi-rs` 或 `node-addon-api`）
    - 其他语言按需添加：`--lang=swift` → `ffi_shim/swift/`，`--lang=zig` → `ffi_shim/zig/` 等
  - **添加新语言的流程**：在 `ffi_shim/` 下新建 `{lang}/` 目录 + `ffi_shim.{ext}.mustache` 文件 + 在 `spec_renderer` 注册扩展名映射表，**不修改 CLI 代码**
  - 以上模板与已有的 `IMPLEMENTATION_GUIDE_*.md.mustache` 位于同一 `templates/` 父目录

- **contract.yaml gate 规则扩展**：
  - 对 `sub_category=threshold|format|enum_check` 的 gate，`rule` 字段必须使用 **kernel DAG 可编译的有限子集**（`in [a, b]`、`> N`、`< N`、`== X`、`!= X`、`regex: pattern`），否则 `blessstar spec check` 报错
  - 对 `sub_category=alert|approval|custom` 的 gate，`rule` 字段仅作描述性文档，kernel 不编译，由 ffi_shim 实现

- **涉及改动文件清单**：
  - `kernel/gate_chain/src/gate_dag_compiler.c` — 新文件
  - `kernel/gate_chain/include/bs/kernel/gate_chain/gate_dag_compiler.h` — 新文件
  - `kernel/gate_chain/include/bs/kernel/gate_chain/gate_chain_types.h` — 增加 `sub_category` 枚举值检查
  - `kernel/gate_chain/CMakeLists.txt` — 添加 `gate_dag_compiler.c`
  - `adapter/orchestration/src/ffi_loader.c` — 新文件
  - `adapter/orchestration/include/bs/adapter/orchestration/ffi_loader.h` — 新文件
  - `adapter/orchestration/CMakeLists.txt` — 添加 `ffi_loader.c`
  - `tools/blessstar/templates/ffi_shim/ffi_shim.c.mustache` — C 默认
  - `tools/blessstar/templates/ffi_shim/rust/ffi_shim.rs.mustache` — Rust
  - `tools/blessstar/templates/ffi_shim/go/ffi_shim.go.mustache` — Go
  - `tools/blessstar/templates/ffi_shim/python/ffi_shim.py.mustache` — Python
  - `tools/blessstar/templates/ffi_shim/java/ffi_shim.java.mustache` — Java (JNI)
  - `tools/blessstar/templates/ffi_shim/cs/ffi_shim.cs.mustache` — C# (P/Invoke)
  - `tools/blessstar/templates/ffi_shim/ts/ffi_shim.ts.mustache` — TypeScript (napi-rs)

- **测试要求**：
  - 单元：`gate_dag_compiler` 对合法/非法 rule 的编译结果
  - 单元：`ffi_loader` 加载/卸载/符号查找/符号缺失处理
  - 集成：mock 一个 Rust shim → 编译为 `.dll` → kernel `dlopen` → gate 调用 → 返回结果
  - 集成：mock 一个 Python shim → `ctypes.cdll` 加载 kernel → gate 调用 → 返回结果
  - 回归：`sub_category=threshold` 的 gate 不被误编为 FFI 调用，`sub_category=custom` 的 gate 不被 kernel DAG 自动编译

---

# 🏛️ 架构方案选择记录 (ADR) — CI编译器扫描仪设计

## 1. 需求背景

当前 BlessStar 的 CI 流水线已有契约门禁框架（`contract_gate_runner` + lock plan），但**不消费 CDD Skill 产出的 `gate_rule_def`**，无法在 PR 合并前拦截违反配置契约的变更。需要设计一个 CI 扫描组件，读取 `scenario` 含 `_COMPILE_TIME` 后缀的门禁规则，对 PR 中的配置值做离线校验，输出 pass/fail 报告并支持阻断合并。

## 2. 三大候选方案对比

| 维度 | 方案A (极简运维) | 方案B (性能优先) ⬅️ **已选中** | 方案C (弹性扩展) |
|:---|:---|:---|:---|
| **核心思路** | Python 独立脚本，单文件实现 | **扩展现有 `contract_gate_runner`，gate_rule_def 作为新契约类型接入 lock plan** | 独立编译型二进制（Rust/Go）+ 增量缓存引擎 |
| **实现路径** | 新增 `tools/scripts/contracts/compile_time_scanner.py`，直接读取 `gate_rule_def.json`，逐个校验 | **改造 `contract_compile.py` 支持 gate_rule_def 输入源；扩展 lock plan schema 增加 `_COMPILE_TIME` 阶段** | 新建 `tools/scanner/` 目录，编译为独立 exe；增量模式只扫描 PR 变更文件 |
| **输入** | `gate_rule_def.json`（文件路径参数） | **`gate_rule_def.json` 作为新 `contract_type` 注册到 lock plan** | `gate_rule_def.json` + PR diff（git diff） |
| **输出** | exit code + stdout/stderr | **lock plan 报告格式（JSON + Markdown）** | 结构化报告（JSON）+ SARIF 格式（GitHub code scanning 集成） |
| **CI 集成** | 在 `ci.yml` 中加一个 `run:` 步骤 | **注册为 `stage: ci` 的 gate，复用现有 fail-fast 和报告机制** | 作为独立 job，支持 `continue-on-error` 模式 |
| **优点** | 零依赖，开发成本低（1-2天），直接接入 | **复用已有调度、报告、fail-fast 机制，学习成本低，与现有 CI 门禁体系一致** | 性能最优（增量秒级），集成 GitHub code scanning，类型安全 |
| **缺点** | 无增量和缓存能力，大仓库全量扫描慢；无结构化报告 | **需要改 lock plan schema，与现有 D-scheme 耦合** | 开发成本高（1-2周），需要维护编译产物 |
| **预估单次扫描耗时** | 全量 1000 条规则 ~ 5s | **全量 1000 条规则 ~ 3s（复用锁计划缓存）** | 增量 10 条规则 < 200ms，全量 < 1s |
| **维护成本** | ⭐（单文件） | ⭐⭐（改两个脚本） | ⭐⭐⭐（独立模块 + CI 缓存管理） |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案B — 扩展现有 `contract_gate_runner`，gate_rule_def 作为新契约类型接入 lock plan
>
> * **核心设计**：
>   - 改造 `contract_compile.py`，使其支持将 CDD Skill 产出的 `gate_rule_def.json` 编译为 lock plan 中的 `contracts[]` 条目
>   - 在 lock plan 的 `stage_order` 中增加 `compile_time` 阶段
>   - 新增 `contract_type: "gate_rule_def"`，其 `gate_refs` 指向一个内置的 `_COMPILE_TIME` 校验 gate
>   - 该内置 gate 筛选 `scenario` 含 `_COMPILE_TIME` 后缀的规则，对 PR 变更的配置值做离线校验
>   - 校验逻辑：range 检查（op=range）、枚举检查（op=in）、阈值比较（op=gt/lt/gte/lte）
>   - 依赖检查（${...} 动态引用）在 CI 阶段降级为 warn——因为关联配置值不一定在本次 PR 中
>
> * **确认人**：用户
>
> * **确认日期**：2026-07-04

### 📐 架构拓扑图（ASCII 必填）

```
                        CI 编译期配置扫描工作流

  ┌─────────────────────────────────────────────────────────────────────┐
  │                          GitHub PR                                  │
  │  修改了 config.yaml 中 payment.fee.rate = 0.008                     │
  └────────────────────────┬────────────────────────────────────────────┘
                           │ CI 触发
                           ▼
  ┌─────────────────────────────────────────────────────────────────────┐
  │  ci.yml — cmake job                                                 │
  │                                                                     │
  │  步骤 1: Contract Compile (contract_compile.py)                     │
  │    ├── 输入: gate_rule_def.json (CDD Skill 产出)                    │
  │    ├── 筛选: scenario 含 _COMPILE_TIME 后缀的规则                     │
  │    ├── 输出: contract_plan.lock.json (含 compile_time stage)        │
  │    └── 注册内置 gate: compile_time_scanner (shell: python ...)      │
  │                                                                     │
  │  步骤 2: Contract Gate Runner (contract_gate_runner.py)             │
  │    ├── 读取 lock plan                                               │
  │    ├── 按 stage_order 执行:                                         │
  │    │    stage:  ci        → 常规 gate（格式、命名、边界检查）        │
  │    │    stage:  compile_time → compile_time_scanner (新增)          │
  │    │    stage:  staging   → ...                                     │
  │    │    stage:  prod_ops  → ...                                     │
  │    ├── fail-fast: blocking 契约失败即阻断                            │
  │    └── 输出: contract-gate-report.json + .md                        │
  │                                                                     │
  │  步骤 3: 报告上传                                                   │
  │    ├── 失败 → 在 PR 上标记 ❌ checks failed                          │
  │    └── 成功 → ✅ checks passed                                      │
  └─────────────────────────────────────────────────────────────────────┘


                    compile_time_scanner 内部逻辑

  ┌─────────────────────────────────────────────────────────────────────┐
  │  compile_time_scanner.py (新增)                                     │
  │                                                                     │
  │  输入:                                                              │
  │    ├── gate_rule_def.json (全量规则)                                │
  │    ├── git diff HEAD~1 (PR 变更文件)                                │
  │    └── config 当前值快照 (可选的, 用于 ${...} 动态引用兜底)           │
  │                                                                     │
  │  处理流程:                                                          │
  │    ┌───────────┐                                                    │
  │    │ 步骤 1    │ 筛选 scenario 含 _COMPILE_TIME 的规则              │
  │    │ 过滤      │ 忽略其他规则                                        │
  │    └─────┬─────┘                                                    │
  │          ▼                                                          │
  │    ┌───────────┐                                                    │
  │    │ 步骤 2    │ 解析 git diff, 提取变更文件的 config_key → new_val  │
  │    │ 提取变更  │ 格式: { "payment.fee.rate": "0.008", ... }         │
  │    └─────┬─────┘                                                    │
  │          ▼                                                          │
  │    ┌───────────┐                                                    │
  │    │ 步骤 3    │ 对每条匹配的规则, 用 new_val 做校验:                │
  │    │ 规则校验  │                                                     │
  │    │           │  op=range  → new_val ∈ [min, max]?                 │
  │    │           │  op=in     → new_val ∈ enum_values?                │
  │    │           │  op=gt/lte → new_val >/<= threshold?               │
  │    │           │  op=gte/lt → new_val >=/< threshold?               │
  │    │           │  ${...}    → warn (运行时 gate 负责)               │
  │    └─────┬─────┘                                                    │
  │          ▼                                                          │
  │    ┌───────────┐                                                    │
  │    │ 步骤 4    │ 全部通过 → exit 0                                  │
  │    │ 输出      │ 有失败  → exit 非0, 输出 JSON 报告:                │
  │    │           │ { violated_rules: [                                │
  │    │           │     { file, line, config_key,                      │
  │    │           │       expected, actual, error_hint }               │
  │    │           │   ], passed: N, failed: M }                        │
  │    └───────────┘                                                    │
  └─────────────────────────────────────────────────────────────────────┘


                 gate_rule_def → lock plan 映射

  CDD Skill 产出                              lock plan 条目
  ──────────────────                          ──────────────
  {                                            {
    "field_key": "payment.fee.rate",              "id": "payment.fee.rate_COMPILE",
    "op": "range",                                "type": "gate_rule_def",
    "value": "[0.001,0.006]",                     "status": "active",
    "scenario": "..._COMPILE_TIME",               "blocking": true,
    "error_hint": "..."                            "stage": "compile_time",
  }                                                "gate_refs": ["compile_time_scanner"],
  }                                                "rule": { "op": "range", "value": "[0.001,0.006]" }
                                                   "error_hint": "..."
                                                 }
```

### ⚙️ 架构不变量（审查红线）

1. **输入隔离**：扫描器必须只消费 `scenario` 含 `_COMPILE_TIME` 后缀的 `gate_rule_def`，禁止扫描运行时规则（运行时规则应由 `ConfigReloadSession` 处理，两者互不干扰）。
2. **增量优先**：CI 触发时必须优先使用 `git diff` 只扫描变更文件涉及的配置 key，全量扫描仅作为兜底策略（如首次接入或缓存丢失）。这是性能不变量。
3. **动态引用降级**：对于 `${config_key}` 动态引用的依赖规则，若关联配置不在本次 PR 变更范围内，扫描器不得报错——应跳过该条规则并记录 warn，运行时由 gate_chain 实际求值。
4. **输出兼容**：扫描报告必须同时输出机器可读格式（JSON）和人类可读格式（Markdown），JSON 格式需包含 `file_path`、`line`、`config_key`、`expected`、`actual`、`error_hint` 字段。

### 📋 落地建议（给 subtask-b 的指令）

- **新增文件**：
  - `tools/scripts/contracts/compile_time_scanner.py` — 编译期扫描器入口，含规则筛选、git diff 解析、值校验、报告输出
  - `tools/scripts/contracts/compile_time_scanner_test.py` — 扫描器单元/集成测试

- **修改文件**：
  - `tools/scripts/contracts/contract_compile.py` — 扩展 `contract_type` 支持 `gate_rule_def`；新增 `compile_time` stage 到 `stage_order`；筛选 `_COMPILE_TIME` 规则生成 lock plan 条目
  - `tools/scripts/lib/contract_schema.py` — 新增 `gate_rule_def` 契约类型的 schema 定义和校验器
  - `.github/workflows/ci.yml` — 在 cmake job 中增加 gate_rule_def.json 的生成步骤（作为 CDD Skill 产物的输入）

- **测试要求**：
  - 单元：`compile_time_scanner` 对 range/in/gt/lte 操作符的正确校验
  - 单元：`contract_compile.py` 对 gate_rule_def 输入的正确编译
  - 集成：mock 一个含 `_COMPILE_TIME` 规则的 gate_rule_def.json → 编译为 lock plan → 扫描器运行 → 校验报告内容
  - 回归：`_COMPILE_TIME` 规则不会被 `ConfigReloadSession` 的双重校验（运行时门禁只处理非 `_COMPILE_TIME` 规则）

***

# 🏛️ 架构方案选择记录 (ADR) — BlessStar 配置消费端口-适配器升级

## 1. 需求背景

当前 BlessStar 的配置消费模式是 **SDK 直调**：业务代码直接调用 `blessStar.GetDuration("/config/payment/timeout")` 或通过 AI 管线的 `read_config_value` 工具读取。这种模式导致：

1. 业务代码与 BlessStar SDK 紧耦合 — 单元测试必须 mock SDK
2. 配置 Key 散落在业务代码各处 — 无类型安全，无编译期校验
3. 业务语义缺失 — 代码中充斥 `/config/payment/timeout` 这样的路径字符串，而非 `paymentConfig.GetTimeout()` 这样的业务语义

升级目标：引入**端口-适配器模式**，在不拆除现有统一注册体系的前提下，在业务系统代码侧增加一层接口膜。

同时发现几个生产级缺陷，一并修复：
- **无容灾降级** — BlessStar 宕机 → `GetTimeout()` 返回 0 → 业务系统灾难性故障
- **无链路追踪** — 方法签名缺少 `context.Context`，审计排障断裂
- **依赖注入手写维护成本高** — 服务增多时 main.go 膨胀

## 2. 三大候选方案对比

| 维度 | 方案A (极简：SDK 封装) | 方案B (均衡：代码生成适配器) | 方案C (完整：运行时动态代理) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 在 BlessStar SDK 上层提供类型安全的 ConfigReader 包装类 | 从 `manifest.json`/`config_metadata.json` 自动生成 Port 接口 + Adapter 桩代码 | 运行时注册 Port 接口 → 动态代理自动桥接 BlessStar Registry |
| **容灾机制** | ❌ 无内置 | ✅ Last Known Good Value 缓存 + 硬编码默认值 | ✅ 同方案B |
| **方法签名** | ❌ 无 ctx | ✅ 强制 context.Context 第一参数 | ✅ 同方案B |
| **DI 代码** | 手写 | ✅ 自动生成 ProvideBlessStarAdapters() | 自动生成 |
| **优点** | 改动最小，1-2人周 | 类型安全 + 可测试 + 可审计 + 容灾 | 零适配代码 |
| **缺点** | 无语义隔离，无类型安全 | 需要代码生成管线 | 动态代理调试难，运行时开销 |
| **可测试性** | ⭐⭐ | ⭐⭐⭐ | ⭐ |
| **维护成本** | ⭐ | ⭐⭐ | ⭐⭐⭐ |

## 3. 用户最终确认草案

> **确认选择**：方案B（代码生成适配器）
> **确认人**：用户，时间：2026-07-05

### 📐 架构拓扑图

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Business System                               │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    domain/ (端口层)                          │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │ AuthConfig │  │PaymentConf │  │ OrderConf  │  ← 接口    │   │
│  │  │ (port)     │  │ig (port)   │  │ig (port)   │    (只声明) │   │
│  │  │ Get...     │  │GetTimeout  │  │GetStatus   │            │   │
│  │  │ (ctx)      │  │(ctx)       │  │(ctx)       │            │   │
│  │  └──────┬─────┘  └──────┬─────┘  └──────┬─────┘            │   │
│  │         │               │               │                   │   │
│  │         └───────────────┼───────────────┘                   │   │
│  │                         ▼                                   │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │               adapter/ (适配器层)                    │   │   │
│  │  │  ┌─────────────────────────────────────────────┐    │   │   │
│  │  │  │          blessstar-adapter/                 │    │   │   │
│  │  │  │  ┌─ AuthAdapter ───────────────────────┐    │    │   │   │
│  │  │  │  │  TokenExpirySeconds(ctx) {          │    │    │   │   │
│  │  │  │  │    1. BlessStar SHM 实时读取        │    │    │   │   │
│  │  │  │  │    2. 成功 → 写入 lastKnownCache    │    │    │   │   │
│  │  │  │  │    3. 失败 → 读 lastKnownCache      │    │    │   │   │
│  │  │  │  │    4. 缓存也无 → 硬编码默认值       │    │    │   │   │
│  │  │  │  │  }                                  │    │    │   │   │
│  │  │  │  └─────────────────────────────────────┘    │    │   │   │
│  │  │  │  PaymentAdapter  OrderAdapter  ...          │    │   │   │
│  │  │  └─────────────────┬───────────────────────────┘    │   │   │
│  │  │  ┌─────────────────────────────────────────────┐    │   │   │
│  │  │  │      mock-adapter/                          │    │   │   │
│  │  │  │  AuthMock  PaymentMock  OrderMock           │    │   │   │
│  │  │  │  (固定返回值，供单元测试使用)                │    │   │   │
│  │  │  └─────────────────────────────────────────────┘    │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  │                                                              │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │  provider/ (自动生成)                                │   │   │
│  │  │  ProvideBlessStarAdapters(client) *Adapters          │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                         │                                           │
│                         ▼ (依赖注入)                                 │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │       service/ (业务层，只依赖 Port 接口)                   │   │
│  │  PaymentService  OrderService   UserService                  │   │
│  │  (不感知 BlessStar，不感知容灾逻辑)                          │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
                              │ (BlessStar SDK)
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        BlessStar 平台                                │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ Registry │  │ Gate Chain│  │ Pipeline │  │ AI 配置管理管线   │   │
│  │ Schema   │  │ 门禁校验   │  │ IR 执行  │  │ 自然语言→配置     │   │
│  └──────────┘  └───────────┘  └──────────┘  └──────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  config_consumer (SHM 共享内存读取，延迟 ~1μs)              │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

### ⚙️ 架构不变量（审查红线）

1. **禁止**业务代码直接 import BlessStar SDK — 只能通过生成的 Port 接口调用
2. **方法签名强制 `ctx context.Context`** — 所有 Port 方法的第一个参数必须是 `context.Context`，用于链路追踪和超时控制
3. **容灾三阶段降级（强制）** — 每个 Adapter 方法必须实现：SHM 实时读取 → 成功写入 lastKnownCache → 失败读缓存 → 缓存未命中读硬编码默认值。不得返回零值，不得 panic，不得向上抛异常
4. **`lastKnownCache` 必须是进程内内存缓存**（如 Go `sync.Map`），不得依赖 Redis/外部存储
5. **DI 代码自动生成** — `blessstar_provider.go` 由代码生成器一并产出
6. **每个 Port 必须配套生成 Mock 实现** — 供业务系统单元测试使用
7. **代码生成产物全部纳入版本控制** — CI 检测 manifest.json 变更时自动触发重新生成

### 📋 落地建议（给 subtask-b 的指令）

- **新建模块**：`tools/blessstar-codegen/` CLI 工具
  - 子包: `parser/`（JSON → IR Model）、`types/`（类型映射）、`backend/`（语言后端接口）、`backend/go/`、`backend/java/`、`backend/python/`、`backend/c/`
  - CLI 签名：`blessstar-codegen --lang go --manifest biz-registry/douyin-mall/manifest.json --output biz-adapters/`

- **Adapter 代码生成模板核心结构**：
  ```
  生成的 Adapter 结构体包含:
    client             *blessstar.Client
    lastKnownCache     sync.Map          ← 容灾降级
    hardcodedDefaults  map[string]any    ← 极冷启动默认值

  每个方法的三段式实现:
    1. SHM 实时查询 → 成功写入缓存 → 返回
    2. 查询失败 → lastKnownCache 命中 → 返回缓存值
    3. 缓存未命中 → 返回 hardcodedDefaults 中的值
  ```

- **自动生成 provider/blessstar_provider.go**：
  ```
  type Adapters struct { Auth, Payment, Order, ... }
  func ProvideBlessStarAdapters(client) *Adapters  // 一行实例化所有 Adapter
  ```

- **CI/CD 集成**：`.github/workflows/codegen.yml` — `paths: ['biz-registry/**/manifest.json']`

- **测试要求**：
  - 代码生成器单元测试（Parser、TypeResolver、每个 Language Backend）
  - 集成测试：生成完整 Go Adapter → `go build` 编译验证
  - **容灾测试**：模拟 BlessStar 断连 → 验证降级到 lastKnownCache → 验证降级到硬编码默认值
  - 回归测试：生成产物与 golden file 比对

- **涉及改动文件清单**：
  - 新增：`tools/blessstar-codegen/main.go`
  - 新增：`tools/blessstar-codegen/parser/parser.go`
  - 新增：`tools/blessstar-codegen/backend/go/go_backend.go`
  - 新增：`tools/blessstar-codegen/backend/java/java_backend.go`
  - 新增：`tools/blessstar-codegen/backend/python/py_backend.go`
  - 新增：`tools/blessstar-codegen/backend/c/c_backend.go`
  - 修改：`biz-registry/douyin-mall/manifest.json`（补充 config_domains）
  - 修改：`biz-registry/douyin-mall/config_metadata.json`（补充 ai_hint）
  - 新增：`.github/workflows/codegen.yml`

---

# 🏛️ 架构方案选择记录 (ADR) - 移除 blessstar-sdk-go 外部依赖，采用 ConfigReader 接口注入

## 1. 需求背景

当前 blessstar-codegen 生成的 Go 适配器代码依赖 `blessstar-sdk-go` 外部包，该包目前仅作为空桩存在（`Client.Get()` 返回 `(nil, nil)`），无运行时价值。同时，生成的 Port 接口直接引用 `*blessstar.Client` 类型，违反了"Port 不得依赖外部 SDK"的架构不变量。

核心问题：Go 业务系统如何读取 BlessStar C++ 共享内存中的配置？需要一个**零外部依赖、配置源可插拔、生成代码即开即用**的方案。

## 2. 三大候选方案对比

| 维度 | 方案A (极简运维) | 方案B (高性能) | 方案C (高扩展) |
| :--- | :--- | :--- | :--- |
| **核心思路** | `ports/` 中定义 `ConfigReader` 接口，去掉外部 SDK 依赖；业务方自行决定配置源实现 | cgo 封装 BlessStar C++ SHM 读出端，Go 直接读共享内存 | BlessStar 启动 sidecar 进程暴露 gRPC ConfigService |
| **优点** | 零外部依赖，生成代码即开即用；配置源可插拔 | 延迟 ~1µs，性能最优 | 多语言互通；Watch API 原生推送；独立生命周期 |
| **缺点** | 配置变更需轮询（CachedReader + time.Ticker 秒级解决） | cgo 交叉编译灾难；需暴露 C++ C API | 引入进程间通信 ~100µs；运维成本 +1 |
| **预估读取延迟** | ~1µs (缓存命中) / ~1ms (HTTP 穿透) | ~1µs (SHM 零拷贝) | ~100µs (gRPC 回环) |
| **编译复杂度** | ⭐（纯 Go） | ⭐⭐⭐（cgo + 原生库） | ⭐⭐（sidecar 进程管理） |
| **Watch 实时推送** | ✅ time.Ticker 秒级轮询 | ❌ 需自行轮询 | ✅ gRPC SSE |

## 3. 用户最终确认草案（标注日期）
> **确认选择**：方案A — ConfigReader 接口注入 + CachedReader 可选包装器 （确认人：用户/Commander，时间：2026-07-05）

### 📐 架构拓扑图（ASCII必填）

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         业务系统（Go 进程）                               │
│  ╔═══════════════════════════════════════════════════════════════════╗   │
│  ║            pkg/ports/  (顶级目录，禁止放在 adapter 子目录下)       ║   │
│  ║  ┌────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  AuthConfig 接口  │  UserConfig 接口  │  ConfigReader 接口  │  ║   │
│  ║  │  TokenExpiry()    │  RoleValues()     │  Get(ctx, path)    │  ║   │
│  ║  │  PasswordCost()   │  StatusValues()   │     ↑string,error  │  ║   │
│  ║  └────────────────────────────────────────────────────────────┘  ║   │
│  ║  业务代码(service/)仅依赖此包，不感知底层配置源                     ║   │
│  ╚═══════════════════════════════════════════════════════════════════╝   │
│                          │ 依赖 ports.ConfigReader                       │
│                          ▼                                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │                   adapter_blessstar/ 包                           │    │
│  │                                                                  │    │
│  │  AuthConfigAdapter          UserConfigAdapter                    │    │
│  │  ┌────────────────────┐    ┌────────────────────┐               │    │
│  │  │ New(reader)         │    │ New(reader)         │              │    │
│  │  │ 阶段1: reader.Get() │    │ 阶段1: reader.Get() │              │    │
│  │  │ 阶段2: cache.Load() │    │ 阶段2: cache.Load() │              │    │
│  │  │ 阶段3: hardcoded    │    │ 阶段3: hardcoded    │              │    │
│  │  └────────────────────┘    └────────────────────┘               │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                          │                                               │
│                          ▼                                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  CachedReader (可选装饰，由 main.go 注入)                        │    │
│  │  - sync.Map 缓存 + time.Ticker 秒级刷新                         │    │
│  │  - 命中直接返回，穿透调 inner.Get()                              │    │
│  │  - 纯 Go 标准库，零外部依赖                                      │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                          │                                               │
│                          ▼                                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  ConfigReader 实现 (业务方按需选择，可绕过 CachedReader 直连)     │    │
│  │                                                                  │    │
│  │  ┌─────────────────┐  ┌──────────────┐  ┌──────────────────┐    │    │
│  │  │  HTTPReader       │  │  FileReader   │  │  EnvReader       │    │    │
│  │  │  调 Electron HTTP │  │  读本地 JSON   │  │  os.Getenv()     │    │    │
│  │  │  (依赖环境变量     │  │  文件          │  │  适用于容器化    │    │    │
│  │  │  BLESSSTAR_       │  │               │  │  场景           │    │    │
│  │  │  ENDPOINT)        │  │               │  │                  │    │    │
│  │  └─────────────────┘  └──────────────┘  └──────────────────┘    │    │
│  │  ┌──────────────────────────────────────────────────────────┐   │    │
│  │  │  SHMReader (未来预留) — cgo 封装 C++ SHM，极致性能         │   │    │
│  │  └──────────────────────────────────────────────────────────┘   │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  adapter_mock/ 包 (单元测试用)                                    │    │
│  │  NewAuthConfigMock() → 固定返回值, 不依赖 ConfigReader           │    │
│  └──────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘
                              │ 仅 HTTPReader 按需发起 HTTP 请求
                              ▼
              ┌─────────────────────────────────────────┐
              │  Electron 主进程                         │
              │  (BlessStar C++ SHM 持有者)               │
              │  监听 ${BLESSSTAR_ENDPOINT:-localhost:8080}│
              │  暴露 HTTP API:                          │
              │  GET /config/{biz}/{path}                │
              │  返回: {"value": "...", "found": true}   │
              └─────────────────────────────────────────┘

━━ 启动时序补充 ━━
1. main.go 初始化:
   reader = NewHTTPReader()  // 读取 BLESSSTAR_ENDPOINT
   └── 即使 HTTP 连不上 → 不阻塞，仅 log.Warn
   adapters = provider.ProvideBlessStarAdapters(reader)
   // ↑ 所有 Get() 请求降级到阶段2(缓存)/阶段3(默认值)
2. 业务方如需"必须连上才能启动":
   for { _, err := reader.Get(ctx, "health"); if err == nil { break }; retry }
   // 由业务方自行实现，框架不强制

```

### ⚙️ 架构不变量（审查红线）

1. **Port 接口零外部依赖** — `pkg/ports/` 包只依赖 Go 标准库，不得 import 任何 BlessStar 或其他第三方包
2. **`ConfigReader` 接口定义在 `pkg/ports/` 包中** — 作为 Go 标准库级别的抽象，不绑定任何实现
3. **Adapters 构造函数接收 `pkg/ports.ConfigReader`，而非具体实现** — 依赖倒置原则
4. **`pkg/ports/` 必须放在项目顶级目录**，禁止放在 `adapter/` 或 `infrastructure/` 子目录下，防止反向污染
5. **Mock 不依赖 ConfigReader** — `NewMock()` 返回固定值，无需配置源
6. **CachedReader 是可选包装器，非强制** — 业务方可以选择直接传入 `ConfigReader` 实现跳过缓存
7. **CachedReader 仅依赖 Go 标准库** — `sync.Map` + `time.Ticker`，零外部依赖
8. **三阶段降级不受影响** — adapter 内部三段式降级在 `ConfigReader` 返回 `("", nil)` 时仍正常工作
9. **ConfigReader 实现通过环境变量 `BLESSSTAR_ENDPOINT` 获取 Electron 地址** — 符合 12-Factor App，不在代码中硬编码
10. **非阻塞启动** — HTTPReader 初始化时即使连接失败，系统正常启动，仅日志 Warn。业务方如需强制等待，由业务方自行在 `main.go` 中重试

### 📋 落地建议（给 subtask-b 的指令）

- **模块变更**：
  1. **删除** Go backend 中 `GenerateBlessStarSDKStub` 相关代码和 `blessstar-sdk-go` 引用
  2. **修改** Port 模板：`ConfigReader` 接口生成到 `pkg/ports/config_reader.go`（顶级目录，非 adapter 子目录）
  3. **修改** Adapter 模板：构造函数签名从 `func NewXxx(client *blessstar.Client)` → `func NewXxx(reader ports.ConfigReader)`
  4. **修改** Provider 模板：`ProvideBlessStarAdapters(reader ports.ConfigReader)` 而非 `(client *blessstar.Client)`
  5. **修改** go.mod 生成：移除 `blessstar-sdk-go` require 和 replace 指令
  6. **新增** CachedReader 生成到 `provider/cached_reader.go`（可选装饰器，非强制）
  7. **修改** Mock 生成模板：不变，mock 不依赖 ConfigReader

- **接口设计**（代码生成器自动生成到 `pkg/ports/config_reader.go`）：
  ```go
  package ports
  import "context"
  // ConfigReader 是配置读取接口。
  // 实现类通过环境变量 BLESSSTAR_ENDPOINT 获取 Electron 地址（HTTPReader 场景）。
  // 初始化失败时不得阻塞进程，由 adapter 的三阶段降级兜底。
  type ConfigReader interface {
      Get(ctx context.Context, path string) (string, error)
  }
  ```

- **CachedReader 设计要点**（代码生成器可选生成）：
  - 构造函数 `NewCachedReader(inner ports.ConfigReader, refreshInterval time.Duration) *CachedReader`
  - 后台 goroutine 使用 `time.Ticker` 定时调用 `inner.Get()` 刷新全量缓存
  - 实现 `ports.ConfigReader` 接口，可作为 adapter 的配置源透明注入
  - 零外部依赖，仅使用 Go 标准库

- **HTTPReader 设计要点**（业务方自行实现，不在代码生成器范围内）：
  - 构造函数读取 `os.Getenv("BLESSSTAR_ENDPOINT")`，默认值 `localhost:8080`
  - 初始化时不发起连接探测，首次 Get() 才真正发起 HTTP 请求
  - 连接失败时返回 error，由 adapter 的三阶段降级处理
  - 请求路径格式：`http://{endpoint}/config/{biz_id}/{config_path}`

- **测试要求**：
  - 单元测试：验证 adapter 构造函数接受 `ports.ConfigReader` 接口（传入 mock reader）
  - 单元测试：验证 `CachedReader` 命中/穿透/刷新行为
  - 单元测试：验证 `CachedReader` 的 time.Ticker 刷新后最新值生效
  - 集成测试：生成代码的 `go build ./...` 编译验证（零外部依赖）
  - 回归测试：现有 19 个测试用例全部通过

- **改动文件清单**：
  - 修改：`tools/blessstar-codegen/backend/backend.go`（删除 `GenerateBlessStarSDKStub`）
  - 修改：`tools/blessstar-codegen/backend/go/go_backend.go`（adapter/provider/port 模板变更）
  - 修改：`tools/blessstar-codegen/main.go`（删除 SDK stub 生成逻辑）
  - 修改：`tools/blessstar-codegen/backend/go/go_backend_test.go`
  - 修改：`backend/java/java_backend.go`, `python/py_backend.go`, `c/c_backend.go`（删除 SDK stub 方法）
  - 新增（生成）：`pkg/ports/config_reader.go`
  - 新增（生成）：`provider/cached_reader.go`

---

---

# 🏛️ 架构方案选择记录 (ADR) — Schema-First 架构升级（配置即宪法）

## 1. 需求背景

当前的 BlessStar 生态中，配置定义分散在多个冗余文件中：
- `biz-registry/{biz_id}/config_metadata.json`（字段元数据，含 UI/契约/AI 信息）
- `manifest.json`（代码生成器入口清单）
- `gate_rule_def`（门禁规则定义，需手动维护与 metadata 对齐）

这三份文件的字段含义重叠，变更时需人工同步，CI 仅能做事后拦截而非事前预防。

**核心痛点**：缺乏一份**自描述的、独立于代码的、可被机器推导的单一真理源（SSOT）**。

**升级目标**：引入 `config-schema.yaml` 作为唯一的配置宪法，将 BlessStar、codegen、业务系统三者完全解耦：
- **配置文件本身** → 管"定义"（Schema 与契约）
- **BlessStar** → 管"容器"（配置值的存储与变更安全）
- **业务系统** → 管"使用"（通过端口拿值）

## 2. 三大候选方案对比

| 维度 | 方案A (极简主义) | 方案B (性能优先) ⬅️ **已选中** | 方案C (弹性扩展) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 保留现有 codegen 管线，将 dual JSON 合为单 YAML，编译时静态推导 | **BlessStar 运行时动态加载 YAML 自省 + codegen 保留 IR 生成职责** | 引入 Schema Registry 中心服务，事件驱动全链路派生 |
| **BlessStar 定位** | 不感知 YAML 变更 | 运行时动态解析 YAML 的 `contract`/`ui_meta` 段，热自省渲染 UI + 门禁 | 通过 Registry Webhook 接收 Schema 变更事件 |
| **codegen 定位** | 保留现有管线，仅替换输入源 | 保留"IR 生成"职责：读取 YAML → 生成 Ports/Adapters/Mock/Provider | 拆分出 schema-deriver 微服务 |
| **CI/CD 定位** | 不变 | 新增 `codegen --check` PR 校验，确保 YAML 与产物一致 | 新增 Schema 合规性 CI |
| **优点** | 改动最小，现有 19 个测试全回归 | 三者各司其职互不侵入；YAML 一改 UI/门禁即变（热加载无编译延迟）；原子性切换机制规避文件监听风险 | 跨项目天然解耦，Schema 版本可追溯 |
| **缺点** | YAML 变更需重新运行 codegen；BlessStar 不感知 YAML | BlessStar 需新增 `SchemaLoader` 模块（原子性热加载 + 完整性校验） | 引入中心服务，运维成本 +1，网络延迟增加 |
| **预估吞吐** | 5000 变更/小时（CI 瓶颈） | **30000 变更/小时**（运行时热加载，无 CI 等待） | 10000 变更/小时（网络 I/O 瓶颈） |
| **维护成本** | ⭐ | ⭐⭐ | ⭐⭐⭐ |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案 B — BlessStar 运行时动态自省 + codegen 保留 IR 生成职责
>
> **架构三权分立**：
> - **BlessStar 服务端**：运行时门禁执行 + UI 动态渲染，动态读取 YAML，不生成业务系统代码
> - **codegen CLI 工具**：编译时代码派生，读取 YAML → 生成 Ports/Adapters/Mock/Provider → 提交业务系统仓库
> - **CI/CD**：PR 时运行 `codegen --check`，确保 YAML 与生成代码一致
>
> **SchemaLoader 原子性切换机制**（规避文件写入一半的风险）：
> 1. 监听文件系统事件（fsnotify），检测 `config-schema.yaml` 变更
> 2. 比对 md5/mtime，跳过无变化事件
> 3. 原子性加载：解析到临时内存 → 完整性校验（version、dependencies 语法等）→ 校验通过则一次性替换 `activeSchema` 指针 + 同步更新门禁引擎
> 4. 校验失败保留旧版本，记录错误日志并告警
> 5. 启动时找不到 YAML → 内置空规则集（只允许写入，不拦截任何变更）+ Warn 日志
>
> **确认人**：用户
>
> **确认日期**：2026-07-07

---

### 3.1 架构补充设计决议

#### 决议一：config-schema.yaml 单个配置的推导物全景

YAML 中每个配置字段不仅驱动当前 codegen + 门禁链路，还应具备向以下方向的推导潜能（按优先级排列）：

| YAML 字段 | 推导产物 | 消费方 |
| :--- | :--- | :--- |
| `contract.slo_impact: "支付成功率"` | Prometheus alerting rule + Grafana panel 定义 | SRE/运维平台 |
| `dependencies` + `impact_scope` | 配置依赖拓扑 DAG（变更影响面分析用） | BlessStar Admin 影响分析面板 |
| `env_overrides: { production: { range: [30,120] } }` | 多环境门禁阈值裁剪 | BlessStar 门禁引擎 |
| `range: [10,120]` + `dependencies` | 9 条边界值测试用例（codegen --gen-tests） | CI 测试套件 |
| `business_desc` + `search_keywords` + `ai_hint` | LLM RAG 检索上下文片段 | 编辑器 AI 管线 |

> **原则**：YAML 中每个字段都在定义下游某个产物的"参数"而非"实现"——推导是机械的、可自动化、可验证的。

#### 决议二：SchemaLoader 专职"金标准过滤式解析器"

SchemaLoader 不做通用 YAML 导入，而是对照内核内置的 `bs_schema_gold_standard` 做**过滤式解析**：

```
config-schema.yaml
  │
  ▼
SchemaLoader
  ├─ 对照 bs_schema_gold_standard (内核固化的字段白名单)
  ├─ 校验: 金标准标为 required 的字段若缺失 → 视为非法，拒绝加载
  ├─ 校验: 金标准有默认值的字段若缺失 → 补入默认值
  ├─ 过滤: 未列入金标准的 YAML 字段 → 静默忽略（不报错、不阻塞）
  └─ 输出: struct bs_schema*（只含 BlessStar 认可的纯净字段集）
```

金标准在 kernel 内固化为编译期常量元定义：

```c
// kernel/schema/include/bs/kernel/schema/gold_standard.h
// BlessStar 金标准 v1：定义 SchemaLoader 认可的字段集合
struct bs_gold_field_def {
    const char* qualified_name;   // 如 "key", "type", "contract.range"
    enum bs_field_type type;
    bool required;                // true=缺失视为非法, false=可选
    const char* default_value;    // required=false 时的默认值（NULL=无默认值）
};

// 金标准 v1 表（编译期固化）
extern const struct bs_gold_field_def BS_GOLD_STANDARD_V1[];
extern const size_t BS_GOLD_STANDARD_V1_COUNT;
```

三个质变：
1. **前向兼容**：YAML 中未来新增的扩展字段自动被忽略，老版本 BlessStar 不会崩溃
2. **扩展权归还业务团队**：业务方可在 YAML 中添加 BlessStar 无关的元数据（如 Wiki 链接），SchemaLoader 视而不见
3. **金标准自版本化**：`BS_GOLD_STANDARD_V1` / `V2` 随 kernel 版本演进，SchemaLoader 根据 YAML 的 `version` 字段匹配合适的金标准版本

#### 决议三：YAML Gate 字段 → GateFactory 构造参数（丝滑门禁注入）

将 `contract` 段从"YAML 中的描述性注解"提升为"Gate 实例的构造参数声明"，实现零代码门禁注册：

```
YAML fields[].contract
  ├─ range: [10, 120]          →  GateFactory 创建 RangeGate(params={min:10, max:120})
  ├─ dependencies: ["A > B*2"] →  GateFactory 创建 DependencyGate(params={expr: "A > B*2"})
  ├─ approval_required: true   →  GateFactory 创建 ApprovalGate(params={required: true})
  └─ slo_impact: "支付成功率"   →  GateFactory 创建 SLOWarningGate(params={metric: "payment_success_rate"})
```

核心设计：

```c
// codegen 从 YAML contract 段推导出的门禁配置结构
struct gate_config {
    const char* field_key;       // 如 "payment.timeout"
    enum gate_type type;         // RANGE, DEPENDENCY, APPROVAL, SLO_WARNING
    struct gate_params params;   // 类型相关的参数联合体
};

// GateFactory 批量注册
// 在 SchemaLoader 的 on_switch 回调中触发
int gate_factory_batch_register(struct gate_factory* factory,
                                const struct gate_config* configs,
                                size_t count);
int gate_factory_reset(struct gate_factory* factory);  // 原子性全量替换
```

注册链路：
```
config-schema.yaml 变更
  → SchemaLoader 原子性加载 → on_switch 回调
  → GateFactory::reset() + batch_register(gate_configs)
  → 门禁引擎规则集热更新（读写锁保护）
```

> **红线**：GateFactory 是整个 BlessStar 中 gate 实例的**唯一创建者**，禁止在其他模块中手工创建 gate 实例。

---

### 📐 架构拓扑图（ASCII 必填）（含补充决议后的完整版）

```
                                  ┌─────────────────────────────────────┐
                                  │         config-schema.yaml          │
                                  │         (唯一真理源 SSOT)            │
                                  │  ┌─────────────────────────────┐    │
                                  │  │ domain, version, fields[]   │    │
                                  │  │ ┌─────────────────────────┐│    │
                                  │  │ │ key, type, default       ││    │
                                  │  │ │ contract: {range, dep,   ││    │
                                  │  │ │   slo_impact, approval}  ││    │
                                  │  │ │ env_overrides            ││    │
                                  │  │ │ ui_meta, business_desc,  ││    │
                                  │  │ │ impact_scope             ││    │
                                  │  │ └─────────────────────────┘│    │
                                  │  └─────────────────────────────┘    │
                                  └──────────┬──────────────────────────┘
                                             │ Git Push
                    ┌────────────────────────┼────────────────────────────┐
                    │                        │                            │
                    ▼                        ▼                            ▼
    ┌───────────────────────────┐  ┌──────────────────────────────────────┐
    │   codegen CLI (CI)        │  │  BlessStar Admin (运行时)             │
    │                           │  │                                      │
    │ blessstar-codegen         │  │  SchemaLoader (金标准过滤式解析器)      │
    │  ├─ 读取 YAML             │  │  ├─ fsnotify 监听                    │
    │  ├─ 对照金标准过滤        │  │  ├─ md5/mtime 比对                   │
    │  ├─ 生成 ports/*.go       │  │  ├─ gold_standard 过滤 ↓             │
    │  ├─ 生成 adapters/*.go    │  │  │  (只提取 BlessStar 认可字段)       │
    │  ├─ 生成 mock/*.go        │  │  ├─ 原子性加载(临时→校验→替换指针)     │
    │  ├─ 生成 provider/*.go    │  │  └─ 失败→回滚旧版 / 启动→空规则集     │
    │  ├─ 生成 gate_configs.h   │  │              │                       │
    │  ├─ 生成 test_cases/      │  │              ▼ (on_switch 回调)       │
    │  ├─ 生成 observability/   │  │  ┌──────────────────────────────┐    │
    │  └─ 提交 PR 到业务仓库    │  │  │  GateFactory                  │    │
    └───────────────────────────┘  │  │  ├─ reset() 全量替换          │    │
                                   │  │  └─ batch_register(gate_configs) │
                    ┌──────────────────┤  │  (从 YAML contract 段提取参数) │    │
                    │  CI: --check     │  └──────────────┬───────────────┘    │
                    │  比对生成产物    └─────────────────┼────────────────────┘
                    │  与仓库代码一致                     │
                    └──────────────────┐                 │
                                       │                 │
                                       ▼                 ▼
                  ┌───────────────────────────────────────────────┐
                  │           BlessStar 内核 (Kernel)              │
                  │                                                │
                  │  ┌────────────────┐  ┌────────────────────┐   │
                  │  │ Gate Engine    │  │ Schema Validator    │   │
                  │  │ (运行时门禁执行) │  │ (金标准 + 完整性校验) │   │
                  │  │  range/dep/    │  │                    │   │
                  │  │  approval/slo  │  │  gold_standard.h   │   │
                  │  └────────────────┘  │  编译期固化字段表    │   │
                  │  ┌────────────────┐  └────────────────────┘   │
                  │  │ UI Map         │                           │
                  │  │ (schema→UIDL)  │                           │
                  │  │  动态渲染表单   │                           │
                  │  └────────────────┘                           │
                  └────────────────────┬──────────────────────────┘
                                       │ ConfigReader HTTP API
                                       ▼
                  ┌───────────────────────────────────────────────┐
                  │         业务系统 (运行时进程)                   │
                  │                                                │
                  │  ports/ (接口层，无外部依赖)                     │
                  │   ├─ config_reader.go: ConfigReader 接口       │
                  │   ├─ payment.go: GetTimeout() Duration         │
                  │   └─ user.go: GetMaxRetry() int32              │
                  │                                                │
                  │  adapters/blessstar/ (实现层，依赖 ConfigReader)│
                  │   ├─ payment_adapter.go: 三阶段降级             │
                  │   └─ (ConfigReader → Cache → Default)          │
                  │                                                │
                  │  adapters/mock/ (测试双打，固定返回值)           │
                  │   ├─ payment_mock.go                           │
                  │   └─ user_mock.go                              │
                  │                                                │
                  │  test_cases/ (边界值测试, codegen --gen-tests)   │
                  │  observability/ (告警规则, codegen --gen-obs)   │
                  └───────────────────────────────────────────────┘
```

### ⚙️ 架构不变量（审查红线）

1. **SSOT 原则不可违**：`config-schema.yaml` 是唯一的真理源，`manifest.json`、`config_metadata.json`、`gate_rule_def` 均由此推导，**禁止**人工维护冗余文件。

2. **Schema 与运行时解耦**：业务系统的运行时进程**禁止**直接读取 `config-schema.yaml`——只能通过 `ports.ConfigReader` 接口取配置值；Schema 唯二合法消费方是：codegen（编译时）和 BlessStar Admin（运行时）。

3. **YAML 版本化契约**：`config-schema.yaml` 每次变更必须提交 Git，且 `version` 字段必须遵循 SemVer（`vMAJOR.MINOR.PATCH`），codegen 与 BlessStar 按需实现版本兼容。

4. **`contract.dependencies` 可计算性**：所有跨字段依赖关系必须表示为闭式数学表达式（如 `"payment.timeout > payment.max_retry * 2"`），**禁止**依赖隐式业务逻辑或外部 API。表达式语法沿用 Go `govaluate` 标准。

5. **门禁规则不可绕过**：BlessStar Admin 对配置值的写入**必须**通过 `contract` 中的 `range`/`dependencies` 校验，不允许跳过门禁直接写存储层。校验失败时保留旧值、记录错误日志并告警。

6. **codegen 产物纳入版本控制**：由 `config-schema.yaml` 推导出的 Ports + Adapters + Mock + Provider 文件**必须**提交到业务系统的源码仓库，PR 时运行 `codegen --check` 校验一致性。

7. **SchemaLoader 原子性加载**：BlessStar 的 SchemaLoader 模块必须实现"临时结构解析 → 完整性校验 → 原子性指针替换"三阶段，不允许直接覆盖读取文件。启动时 YAML 缺失则使用空规则集。

8. **三权分立职责边界**：BlessStar（运行时门禁/UI）、codegen（编译时 IR 生成）、CI/CD（一致性校验）三者职责互不侵入，代码仓库间无循环依赖。

9. **SchemaLoader 金标准过滤式解析**：SchemaLoader 不得直接导入完整 YAML，必须对照内核内置的 `bs_schema_gold_standard` 做过滤式解析。只提取金标准认可的字段子集输出 `struct bs_schema*`，未列入金标准的 YAML 字段静默忽略。对于金标准中定义的字段（如 `key`、`type`、`default`、`contract.range`），如果 YAML 中缺失，BlessStar 应使用金标准中定义的默认值或视为非法加载，不得默许跳过。

10. **GateFactory 唯一创建者**：GateFactory 是整个 BlessStar 中 gate 实例的唯一创建者。codegen 从 YAML `contract` 段生成 `gate_config` 数组，SchemaLoader 的 `on_switch` 回调触发 `GateFactory::reset() + batch_register()` 完成门禁规则热更新。禁止在其他模块中手工创建 gate 实例。

11. **YAML 扁平化原则**：SchemaLoader 是 YAML 树→扁平 KV 映射表的唯一执行者。libyaml 解析完整树后，SchemaLoader 递归遍历、展平所有叶子节点路径为 qualified_name（如 `contract.range`），再交付金标准过滤器。上层业务（GateFactory、UI、codegen）只认扁平 Key，禁止直接接触 YAML 原始嵌套结构。SchemaLoader 的扁平化层与金标准过滤器之间通过唯一的 Flat KV Map 接口通信。

### 📋 落地建议（给 subtask-b 的指令）

- **模块拆分**：
  - 新增 `kernel/schema_loader/` 模块（BlessStar 内核侧）：实现 SchemaLoader 组件，含 file watcher / yaml parser / 金标准过滤式解析 / 原子性切换 / 完整性校验 / 空规则集兜底
  - 新增 `kernel/schema/gold_standard.h`（BlessStar 内核侧，编译期固化）：金标准字段白名单表 `BS_GOLD_STANDARD_V1[]`，定义每个字段的 `qualified_name` / `type` / `required` / `default_value`
  - 新增 `adapter/schema_watcher/` 模块（BlessStar 适配器侧）：提供无阻塞的 `bs_schema_watcher_check_and_reload()` 单次检查函数（mtime 比对 + 触发 reload），由主机语言（Go/Java）的协程调度器定时轮询调用。C 层不做线程/异步操作，保持纯净无状态。标记 `TODO: fsnotify/libuv` 作为未来生产级事件驱动替代。
  - 修改 `tools/blessstar-codegen/` 模块：将输入源从 `manifest.json` + `config_metadata.json` 双输入改为单一 `config-schema.yaml` 输入；扩展派生产物：`gate_configs.h`、`test_cases/`、`observability/`
  - 修改 `tools/blessstar-codegen/main.go`：新增 `--check` 模式（一致性校验）、`--gen-tests`（边界测试生成）、`--gen-obs`（可观测性规则生成）

- **架构关键设计代码**：
  - **金标准定义**（kernel 编译期固化）：
    ```c
    // kernel/schema/include/bs/kernel/schema/gold_standard.h
    // BlessStar 金标准 v1：SchemaLoader 据此做过滤式解析
    // 不在本表中的 YAML 字段 → 静默忽略
    // required=true 且 YAML 缺失 → 视为非法，拒绝加载
    // required=false 且 YAML 缺失 → 使用 default_value 补入
    struct bs_gold_field_def {
        const char* qualified_name;   // 如 "key", "type", "contract.range"
        enum bs_field_type type;
        bool required;
        const char* default_value;    // NULL = 无默认值
    };

    extern const struct bs_gold_field_def BS_GOLD_STANDARD_V1[];
    extern const size_t BS_GOLD_STANDARD_V1_COUNT;
    ```

  - **SchemaLoader 核心接口**（C kernel 层，非阻塞设计）：
    ```c
    // schema_loader.h — SchemaLoader 生命周期（金标准过滤式）
    struct bs_schema_loader* bs_schema_loader_create(const char* yaml_path);
    void bs_schema_loader_destroy(struct bs_schema_loader* loader);

    // 金标准过滤式解析入口：内部对照 BS_GOLD_STANDARD_V1 做字段匹配
    int bs_schema_loader_reload(struct bs_schema_loader* loader);

    // 原子性切换：返回当前 active schema 的只读指针（仅含金标准认可字段）
    const struct bs_schema* bs_schema_loader_get_active(struct bs_schema_loader* loader);

    // 非阻塞单次检查：比较 mtime，有变更则触发 reload，否则立即返回
    // 返回：0=无变更, 1=已重载, -1=错误
    int bs_schema_watcher_check_and_reload(struct bs_schema_watcher* watcher);

    // 注册门禁引擎的回调，schema 切换时自动触发 GateFactory 批量更新
    void bs_schema_loader_on_switch(
        struct bs_schema_loader* loader,
        void (*callback)(const struct bs_schema* new_schema, void* userdata),
        void* userdata);
    ```

  - **SchemaYAMLParser 三层升级路径**（Flat KV 转换器设计）：

    **阶段 1（MVP 维持）**：手动行解析，仅提取顶层标量（key/type/default）和通过简单分割提取数组字符串（如 dependencies）。qualified_name 展平匹配（如 `contract.range` 通过递归展平为扁平 key 后与金标准比对）。数组元素不含复杂嵌套对象时可用。

    **阶段 2（libyaml 接入）**：在 SchemaLoader 内部封装 "YAML 树 → 扁平化 KV 映射表（Hash Table）" 转换器。libyaml 解析出完整树，SchemaLoader 递归遍历树，将所有叶子节点路径展平，然后丢给金标准过滤器过滤。**上层业务（GateFactory、UI）依然只认扁平 Key，内核无需感知 YAML 结构变化。**

    ```
    libyaml 解析树               SchemaLoader 内部展平               金标准过滤器
    ┌──────────────┐            ┌──────────────────┐            ┌─────────────┐
    │ field         │            │ flatten to       │            │ match       │
    │  ├─ key       │───────────→│ key: timeout      │───────────→│ BS_V1[key]  │
    │  ├─ contract  │            │ contract.range:   │            │ BS_V1[contract.
    │  │   ├─ range │           │   [10,120]         │            │   range]    │
    │  │   └─ deps  │            │ contract.dep:     │            │ BS_V1[...]  │
    │  └─ ui_meta   │            │   "A > B*2"       │            └─────────────┘
    │    ├─ label   │            │ ui_meta.label:    │
    │    └─ order   │            │   "超时时间"        │
    └──────────────┘            └──────────────────┘
    ```

    **阶段 3（金标准升级）**：如果未来金标准需要支持嵌套结构（如 `ui_meta: { label: "xxx" }`），只需修改金标准定义 Schema 和过滤器逻辑，扁平化层和上层业务均无需改动。

    核心约束：**SchemaLoader 是 YAML 扁平化的唯一执行者，上层业务禁止直接接触 YAML 原始结构。**

  - **SchemaWatcher 非阻塞设计**（跨平台调度）：

    不在 C 层管理线程生命周期，而是将 SchemaWatcher 拆分为无阻塞的内核接口 + 主机语言调度的组合：

    ```c
    // a) C 内核层 — 纯函数、无阻塞、无状态
    // 单次检查：比较 mtime，有变更则触发 reload，否则立即返回
    // 返回：0=无变更, 1=已重载, -1=错误
    int bs_schema_watcher_check_and_reload(struct bs_schema_watcher* watcher);
    ```

    ```go
    // b) Go 主机层 — 负责调度
    // main.go 中启动 goroutine，每 2s 调用 C.bs_schema_watcher_check_and_reload()
    go func() {
        ticker := time.NewTicker(2 * time.Second)
        defer ticker.Stop()
        for range ticker.C {
            if ret := C.bs_schema_watcher_check_and_reload(watcher); ret < 0 {
                log.Warn("schema reload failed")
            }
        }
    }()
    ```

    设计原则：
    - C 层保持纯净，不做线程/异步操作
    - 阻塞负担交给 Go/Java 的协程调度器
    - 可在单测中直接同步调用 `check_and_reload()` 验证，无需模拟线程
    - 符合 "端口-适配器" 架构：业务系统（Go 层）自行决定配置源的轮询策略和频率

    **替代方案（纯 C 事件驱动）**：改用 inotify(Linux)/kqueue(BSD)/libuv(跨平台)。在 MVP 阶段不值得引入编译依赖，标记为 `TODO: fsnotify/libuv`。

  - **GateFactory + YAML contract 对接**：
    ```c
    // gate_factory.h — codegen 从 YAML contract 段生成的 gate_config
    struct gate_config {
        const char* field_key;       // 如 "payment.timeout"
        enum gate_type type;         // RANGE, DEPENDENCY, APPROVAL, SLO_WARNING
        struct gate_params {
            enum gate_param_type param_type;
            union {
                struct { int64_t min; int64_t max; } range;           // RANGE
                struct { const char* expr; } dependency;              // DEPENDENCY
                struct { bool required; } approval;                   // APPROVAL
                struct { const char* metric; } slo_warning;           // SLO_WARNING
            };
        } params;
    };

    // SchemaLoader on_switch 回调内部调用：
    int gate_factory_reset(struct gate_factory* factory);
    int gate_factory_batch_register(struct gate_factory* factory,
                                    const struct gate_config* configs,
                                    size_t count);
    ```

  - **config-schema.yaml 格式定义**（含扩展字段，放置于业务系统仓库根目录）：
    ```yaml
    # config-schema.yaml — 配置宪法（唯一真理源）
    domain: payment
    version: v1.0.0
    fields:
      - key: payment.timeout
        type: duration
        default: "30s"
        business_desc: "支付等待超时，超过则自动取消订单"
        impact_scope: ["订单状态", "用户通知"]
        contract:
          range: [10, 120]
          dependencies:
            - "payment.timeout > payment.max_retry * 2"
          slo_impact: "支付成功率"
          approval_required: true
        env_overrides:
          production:
            range: [30, 90]
        ui_meta:
          label: "超时时间"
          order: 1
        search_keywords: ["超时", "支付超时", "timeout"]
        ai_hint: "支付等待超时，默认30秒，需大于重试次数*2"
      - key: payment.max_retry
        type: int
        default: 3
        contract:
          range: [1, 5]
    ```

  - **codegen --check 校验流程**：
    ```
    blessstar-codegen --schema config-schema.yaml --lang go --output /tmp/gen_check
    diff -r /tmp/gen_check/ports/  ./ports/
    diff -r /tmp/gen_check/adapters/ ./adapters/
    diff -r /tmp/gen_check/gate_configs.h ./gate_configs.h
    → 若有差异，exit 1 + 打印差异文件列表
    ```

  - **dependencies 表达式语法**（沿用 govaluate 标准，支持运算符：`+`, `-`, `*`, `/`, `>`, `<`, `>=`, `<=`, `==`, `!=`, `&&`, `||`, `in`, `()` 分组）

- **测试要求**：
  - **单元测试**：金标准过滤式解析（required 字段缺失→拒绝、可选字段缺失→补默认值、未注册字段→静默忽略）
  - **单元测试**：SchemaLoader 原子性切换（正常加载 / 校验失败回滚 / 空规则集兜底）
  - **单元测试**：dependencies 表达式解析与求值（5+ 边界用例）
  - **单元测试**：config-schema.yaml → codegen IR 映射正确性（domain/fields/type/default 字段全覆盖）
  - **单元测试**：YAML contract → gate_config 映射（RANGE/DEPENDENCY/APPROVAL/SLO_WARNING 四种类型全覆盖）
  - **集成测试**：`blessstar-codegen --check` 一致性检测（一致通过 / 不一致拒绝）
  - **集成测试**：`check_and_reload()` 非阻塞单次检查（mtime 无变更→0、有变更→1、文件不存在→-1）
  - **集成测试**：Go goroutine 定时轮询 `check_and_reload()` → 触发 SchemaLoader 金标准过滤 → on_switch → GateFactory batch_register → 门禁引擎热更新
  - **集成测试**：YAML 变更触发 codegen --check 失败 → CI 拦截 + 错误日志
  - **回归测试**：现有 codegen 19 个测试全量回归通过
  - **编译验证**：`go build ./...` / `cmake --build` 零错误

- **涉及改动文件清单**：
  - 新增：`kernel/schema/include/bs/kernel/schema/gold_standard.h`（金标准字段白名单表）
  - 新增：`kernel/schema/src/gold_standard.c`（金标准 v1 常量定义）
  - 新增：`kernel/schema_loader/include/bs/kernel/schema_loader/schema_loader.h`
  - 新增：`kernel/schema_loader/src/schema_loader.c`
  - 新增：`kernel/schema_loader/src/schema_yaml_parser.c`
  - 新增：`kernel/schema_loader/test/test_gold_standard_filter.c`
  - 新增：`kernel/schema_loader/test/test_schema_loader_atomic_switch.c`
  - 新增：`kernel/schema_loader/test/test_dependency_eval.c`
  - 新增：`adapter/schema_watcher/include/bs/adapter/schema_watcher/schema_watcher.h`
  - 新增：`adapter/schema_watcher/src/schema_watcher.c`（非阻塞 `check_and_reload` 实现）
  - 新增：`kernel/gate/include/bs/kernel/gate/gate_factory.h`（GateFactory batch_register/reset 接口）
  - 修改：`tools/blessstar-codegen/main.go`（新增 --check / --gen-tests / --gen-obs 模式 + 单 YAML 输入）
  - 修改：`tools/blessstar-codegen/types/types.go`（适配 YAML 输入结构 + gate_config 输出）
  - 修改：`tools/blessstar-codegen/backend/go/go_backend.go`（扩展 gate_configs.h / test_cases / observability 生成）
  - 修改：`tools/blessstar-codegen/backend/go/go_backend_test.go`（新增 --check 测试）
  - 样例新增：`business system/douyin-mall/config-schema.yaml`（首个 Schema-First 落地项目）

---

# 🏛️ 架构方案选择记录 (ADR) — config-schema.yaml 多文件拆分（SSOT 目录化）

## 1. 需求背景

当前 `config-schema.yaml` 将字段定义、契约规则、多领域配置（叙事/人格/口头禅等）混在单文件中。随着业务系统增长（如设计助手需独立管理情绪、人格、语言风格等多个配置领域），单文件模式存在以下问题：

- **领域边界模糊**：narrative / persona / catchphrase 等不同领域的配置字段堆在同一文件中，无物理分隔
- **Git 冲突风险集中**：多个开发者同时修改同一文件的不同领域时，极易产生合并冲突
- **不支持独立版本化**：所有配置字段共享同一个 `version` 字段，无法按领域独立演进
- **无法按文件控制权限**：code review、文件所有者等 Git 级别的权限控制只能到文件级，无法细分

**升级目标**：将单文件 `config-schema.yaml` 拆分为目录化多文件 SSOT + 编译期聚合为 bundled 缓存的模式，同时保持 BlessStar 统一管理配置的能力不变。

---

## 2. 三大候选方案对比

| 维度 | 方案A (极简主义) | **方案B (性能优先 ⬅️ 已选中)** | 方案C (弹性扩展) |
| :--- | :--- | :--- | :--- |
| **核心思路** | 单文件 YAML 多文档段 (`---`) 逻辑分区 | **`config-schema.d/` 目录化 SSOT + 编译期聚合为 `bundled.yaml`** | 运行时 Schema Registry + 动态合并 |
| **拆分方式** | 同一文件内 `# --- narrative ---` 注释分区 | `config-schema.d/{persona,narrative,catchphrase,emotion}.yaml` | 双层文件：`*.schema.yaml`（纯字段定义）+ `*.contract.yaml`（纯契约规则） |
| **聚合时机** | 无需聚合（单文件） | **编译期：`blessstar schema bundle`** | 运行时：Schema Registry 动态加载合并 |
| **聚合产物** | 即源文件本身 | **`config-schema.bundled.yaml`（仅含默认值 + 契约规则，无 UI/搜索辅助字段）** | Schema IR（中间表示），无独立缓存文件 |
| **运行时加载目标** | 直接加载单文件 | **加载 `bundled.yaml`（轻量、契约聚焦）** | 运行时轮询 Schema Registry API |
| **SSOT 变更入口** | 直接编辑单文件 | **直接编辑 `config-schema.d/*.yaml` → `git commit` → `blessstar schema bundle`** | 直接编辑或通过 Registry API |
| **运行时调参入口** | Admin UI → WAL 持久化（不改 SSOT） | Admin UI → WAL 持久化（不改 SSOT）| Admin UI → WAL 持久化（不改 SSOT） |
| **优点** | 改动最小，现有解析器完全兼容 | 领域边界清晰；Git 文件级权限控制；bundled 缓存轻量（无 ui_meta 等冗余）；编译期可校验完整性；WAL 与 SSOT 职责分离 | 契约与字段定义可独立版本化；运行时热更新粒度按文件 |
| **缺点** | 单文件膨胀不可逆；Git 冲突集中；不支持独立版本化 | 需新增编译期聚合器工具；现有 C 解析器需改造支持目录扫描 + 合并 | 架构复杂度最高；引入 Schema Registry 服务；运维成本 +2 |
| **预估吞吐** | 10000 fields/min | **8000 fields/min** | 6000 fields/min |
| **维护成本** | ⭐ | **⭐⭐** | ⭐⭐⭐ |
| **解析器改造量** | 无 | **中等**：新增 `blessstar schema bundle` 聚合器（Go/Python）+ C 端 `schema_loader` 支持读 bundled 格式 | 大：新增 Schema Registry、合并引擎、版本解析 |

---

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案 B — `config-schema.d/` 目录化 SSOT + 编译期聚合为 `bundled.yaml`
>
> **核心设计原则**：
> - SSOT = `config-schema.d/*.yaml`，直接人类编辑 / Git 管理，不存在从运行时"回写"SSOT 的路径
> - `config-schema.bundled.yaml` 是编译产物，仅含默认值 + 契约规则，不含 `ui_meta` / `search_keywords` / `ai_hint` 等 UI/搜索辅助字段
> - 运行时调参走 WAL 持久化（覆盖值优先），不修改 SSOT
> - Schema 演进（改契约、增字段）走直接编辑 SSOT → `git commit` → `blessstar schema bundle`
> - Gold Standard 双重校验：每个 `config-schema.d/*.yaml` 独立校验 + 聚合后整体校验
> - 聚合器不可逆：`config-schema.d/*.yaml` → `bundled.yaml` 是单向编译，禁止在 bundled.yaml 上手动编辑后回写
>
> **确认人**：用户
>
> **确认日期**：2026-07-11

---

### 📐 架构拓扑图（ASCII 必填）

```
                                   编译阶段 (Human + Codegen)
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                                                                              │
 │   Git 仓库 / 业务系统项目根目录                                                │
 │   ┌─────────────────────────────────────────┐                               │
 │   │ config-schema.d/         ← SSOT (人类编辑 / Git 管理)                    │
 │   │   ├── persona.yaml       ← 助手人格配置                                  │
 │   │   ├── narrative.yaml     ← 叙事与量化配置                                │
 │   │   ├── catchphrase.yaml   ← 口头禅配置                                    │
 │   │   └── emotion.yaml       ← 情绪配置                                      │
 │   └─────────────────────────┬───────────────────────────────┘               │
 │                             │                                               │
 │                             │ blessstar schema bundle (编译期聚合器)         │
 │                             ▼                                               │
 │   ┌─────────────────────────────────────────┐                               │
 │   │ config-schema.bundled.yaml  ← 编译产物   │                               │
 │   │ (仅含: domain / version / fields[]       │                               │
 │   │  其中 fields[] 仅含: key / type /        │                               │
 │   │  default / contract)                     │                               │
 │   └──────┬──────────────────────┬───────────┘                               │
 │          │                      │                                            │
 │          ▼                      ▼                                            │
 │   ┌──────────────┐    ┌──────────────────┐                                  │
 │   │   codegen     │    │  BlessStar       │                                  │
 │   │   (编译时)    │    │  SchemaLoader    │                                  │
 │   │               │    │  (启动时加载)    │                                  │
 │   │ 生成 Ports /  │    │                  │                                  │
 │   │  Adapters /   │    │  金标准过滤 →    │                                  │
 │   │  Mock / Gate  │    │  on_switch →     │                                  │
 │   │  Configs /    │    │  GateFactory     │                                  │
 │   │  Test Cases   │    │  batch_register  │                                  │
 │   └───────────────┘    └────────┬─────────┘                                  │
 └──────────────────────────────────┼───────────────────────────────────────────┘
                                    │
                                    ▼
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                            运行阶段 (BlessStar + 业务系统)                    │
 │                                                                              │
 │  ┌──────────────────────────────────────────────────────────────────────┐   │
 │  │                    BlessStar Admin (运行时)                            │   │
 │  │                                                                       │   │
 │  │  用户通过 Admin UI 修改配置值                                          │   │
 │  │       │                                                               │   │
 │  │       ▼                                                               │   │
 │  │  门禁引擎校验 ← 对照 bundled.yaml 中的 contract 段                     │   │
 │  │       │                                                               │   │
 │  │       ▼                                                               │   │
 │  │  WAL 持久化 (记录 key + value + timestamp + operator)                  │   │
 │  │       │                                                               │   │
 │  │       ▼                                                               │   │
 │  │  BlessStar 存储层 (数据库 / 文件 / 共享内存)                           │   │
 │  └──────────────────────────────────────────────────────────────────────┘   │
 │                                                                              │
 │  ┌──────────────────────────────────────────────────────────────────────┐   │
 │  │                    业务系统进程 (运行时)                               │   │
 │  │                                                                       │   │
 │  │  读取配置值:                                                          │   │
 │  │    if key in WAL → 返回 WAL 覆盖值                                    │   │
 │  │    else          → 返回 bundled.yaml 默认值                           │   │
 │  │                                                                       │   │
 │  │  ports/ (接口层)                                                      │   │
 │  │  adapters/ (实现层: ConfigReader → WAL → Bundled)                     │   │
 │  └──────────────────────────────────────────────────────────────────────┘   │
 └─────────────────────────────────────────────────────────────────────────────┘
```

---

### ⚙️ 架构不变量（审查红线）

1. **SSOT 目录不可违**：每个业务系统下必须且仅有一个 `config-schema.d/` 目录，所有配置字段的 SSOT 必须定义在该目录下的 `*.yaml` 文件中。**禁止**在目录外定义配置字段。

2. **bundled.yaml 精简原则**：`config-schema.bundled.yaml` 是编译产物，**只允许**包含 `domain`、`version`、`fields[].{key, type, default, contract}`，**不得包含** `ui_meta`、`search_keywords`、`ai_hint`、`impact_scope`、`env_overrides` 等非运行时所需字段。

3. **聚合器单向不可逆**：`blessstar schema bundle` 是**单向编译**（`config-schema.d/*.yaml` → `bundled.yaml`），禁止在 `bundled.yaml` 上手动编辑后回写 SSOT。`bundled.yaml` 应在 `.gitignore` 中标记为生成文件，或在 CI 中确保与 SSOT 一致。

4. **WAL 不修改 SSOT**：运行时通过 Admin UI 修改配置值**仅写入 WAL 持久化层**，不得直接修改 `config-schema.d/*.yaml` 或 `bundled.yaml`。运行时配置读取优先级为：**WAL 覆盖值 > bundled.yaml 默认值**。

5. **Schema 演进必须走 SSOT**：任何涉及字段新增、删除、类型变更、契约修改、默认值变更的 Schema 演进，必须通过直接编辑 `config-schema.d/*.yaml` → `git commit` → `blessstar schema bundle` 完成，**禁止**通过运行时工具直接修改 bundled 或 WAL 来模拟 Schema 变更。

6. **跨文件契约依赖的编译期解析**：当文件 A 的 `contract.dependencies` 引用文件 B 的字段时，聚合器必须在编译期完成跨文件依赖解析和校验。运行时 bundled.yaml 中的 dependencies 表达式已经是解析后的完整形态，**禁止**运行时跨文件查找依赖定义。

7. **Gold Standard 双重校验**：每个 `config-schema.d/*.yaml` 在聚合前独立校验 gold standard（required 字段缺失→拒绝、可选字段缺失→补默认值、未注册字段→静默忽略），聚合后再做一次整体校验确保无冲突。

8. **SSOT 领域版本化**：每个 `config-schema.d/*.yaml` 文件可声明独立的 `x-version` 元数据字段（可选），编译期聚合器将这些元信息归入 bundled.yaml 的 `x-extensions` 段中，仅用于追踪和审计，不影响运行时。

---

### 📋 落地建议（给 subtask-b 的指令）

- **模块拆分**：
  - 新增 `tools/schema-bundler/` 模块（Go CLI 工具）：实现 `blessstar schema bundle` 命令，完成 `config-schema.d/*.yaml` 的目录扫描 → 逐文件解析 → 跨文件依赖解析 → Gold Standard 校验 → 合并输出 `config-schema.bundled.yaml`
  - 修改 `kernel/schema_loader/src/schema_loader.c`：扩展入参支持加载 `bundled.yaml`（简化格式），复用现有金标准过滤式解析逻辑
  - 修改 `tools/blessstar-codegen/parser/schema_yaml.go`：扩展支持读取 `bundled.yaml` 作为 codegen 输入源
  - 修改 `adapter/src/attach_config.cpp`：新增 WAL 覆盖值读取优先级逻辑（WAL > bundled）

- **架构关键设计代码**：
  - **聚合器核心接口**（`tools/schema-bundler/main.go`）：
    ```go
    // blessstar schema bundle 命令入口
    // 扫描 config-schema.d/ → 逐文件解析 → 跨文件依赖解析
    // → 金标准校验 → 合并 → 输出 config-schema.bundled.yaml
    type BundleCmd struct {
        SchemaDir string   // config-schema.d/ 路径
        Output    string   // 输出路径 (默认 config-schema.bundled.yaml)
        CheckOnly bool     // 仅校验不输出 (CI 模式)
    }
    ```
  - **bundled.yaml 精简格式定义**：
    ```yaml
    # config-schema.bundled.yaml — 编译期聚合缓存（自动生成，禁止手动编辑）
    domain: livedesign
    version: v1.0.0
    generated_at: "2026-07-11T12:00:00+08:00"
    generated_from:
      - config-schema.d/persona.yaml
      - config-schema.d/narrative.yaml
      - config-schema.d/catchphrase.yaml
      - config-schema.d/emotion.yaml
    fields:
      - key: persona.core.warmth
        type: F64
        default: "0.8"
        contract:
          range: [0.5, 1.0]
          slo_impact: "对话情感基调"
          approval_required: true
          immutable: true
      - key: narrative.quantify.schedule
        type: STR
        default: "0 */6 * * *"
        contract:
          slo_impact: "量化指标时效性"
      # ... (无 ui_meta / search_keywords / ai_hint 等字段)
    ```
  - **WAL 读取优先级逻辑**（`adapter/src/attach_config.cpp`）：
    ```c
    // 读取配置值时优先查 WAL，未命中则回退到 bundled 默认值
    const char* bs_config_read(const char* key) {
        // 1. 查 WAL 覆盖值
        const char* wal_value = bs_wal_get(key);
        if (wal_value) return wal_value;
        // 2. 回退 bundled 默认值
        return bs_bundled_get_default(key);
    }
    ```

- **测试要求**：
  - **单元测试**：聚合器逐文件 Gold Standard 校验（required 缺失→拒绝、未注册字段→静默忽略）
  - **单元测试**：跨文件 dependencies 解析正确性（文件 A 引用文件 B 的字段）
  - **单元测试**：聚合后整体校验（是否存在同名 key 冲突、类型不一致）
  - **单元测试**：WAL 读取优先级（WAL 有值→返回 WAL、WAL 无值→返回 bundled 默认值）
  - **集成测试**：`blessstar schema bundle --check` CI 模式（一致通过 / 不一致拒绝）
  - **集成测试**：完整端到端流程（SSOT 编辑 → bundle → codegen → SchemaLoader 加载 → WAL 覆盖 → 业务系统读取）
  - **回归测试**：现有 SchemaLoader 全部测试 + codegen 全部测试回归通过

- **涉及改动文件清单**：
  - 新增：`tools/schema-bundler/main.go`（`blessstar schema bundle` 命令入口）
  - 新增：`tools/schema-bundler/bundler.go`（目录扫描 + 逐文件解析 + 跨文件依赖解析 + 合并输出）
  - 新增：`tools/schema-bundler/validator.go`（Gold Standard 双重校验器）
  - 新增：`tools/schema-bundler/test/bundler_test.go`
  - 修改：`kernel/schema_loader/src/schema_loader.c`（支持加载 bundled.yaml 精简格式）
  - 修改：`kernel/schema_loader/src/schema_yaml_parser.c`（适配精简格式解析）
  - 修改：`tools/blessstar-codegen/parser/schema_yaml.go`（支持读取 bundled.yaml）
  - 修改：`adapter/src/attach_config.cpp`（新增 WAL 优先读取逻辑）
  - 新增：`business system/livedesign/config-schema.d/persona.yaml`（首个拆分解耦示范）
  - 新增：`business system/livedesign/config-schema.d/narrative.yaml`
  - 新增：`business system/livedesign/config-schema.d/catchphrase.yaml`
  - 新增：`business system/livedesign/config-schema.d/emotion.yaml`
  - 新增：`business system/livedesign/config-schema.bundled.yaml`（自动生成）






# 🏛️ 架构方案选择记录 (ADR) — biz-adapters 端口-适配器架构修复（Module 化拆分）

## 1. 需求背景

当前 `biz-adapters/` 混装了 **Port 接口** 和 **BlessStar 适配器实现** 两类职责，导致三个架构缺陷：

1. **Port 接口归属错位** — `biz-adapters/go/douyin-mall/ports/` 定义在 biz-adapters 目录下，但 Port 接口作为业务系统的配置注入契约，应属于业务系统自有资产（`{biz_id}/pkg/ports/`），而非平台目录
2. **多配置源扩展受阻** — Port 接口与 BlessStar 绑定在同一个 module 中，新增 DB/HTTP/Env 配置源实现时需侵入 biz-adapters 的 ports 目录，无法实现配置源自由插拔
3. **architect-pro 与 codegen 产出冲突** — architect-pro 生成 Port 接口到业务系统目录，codegen 也生成 Port 接口到 biz-adapters，两份 Port 定义不一致时无编译期校验，运行时静默出错

## 2. 三大候选方案对比

| 维度 | 方案A (极简：原地拆分) | 方案B (性能：module 化) ⬅️ **已选中** | 方案C (扩展：Registry 中心化) |
| :--- | :--- | :--- | :--- |
| **核心思路** | biz-adapters 中去掉 ports/，只留 adapters/ + provider/；Ports 由 architect-pro 生成到业务系统仓库 | biz-adapters 拆为三层 module：`readers/`(低阶实现) → `{biz_id}/adapters/`(适配器) → 业务系统引用；Ports 归业务系统 | biz-adapters 改为「适配器注册中心」，通过 `AdapterRegistry` 运行时注册，业务系统通过 SPI 发现实现 |
| **Port 归属** | 业务系统 `pkg/ports/` | 业务系统 `pkg/ports/`，业务系统作为 Go module 发布 | 统一 `central-ports-registry` 中心仓库 |
| **多源扩展方式** | biz-adapters/{biz_id}/adapters/{source}/ 目录共存 | biz-adapters/{biz_id}/ 作为独立 module，`readers/` 共享低阶实现供所有 biz 复用 | `AdapterRegistry.register("blessstar", factory)` 运行时注册 |
| **低阶实现复用** | ⭐（reader 分散在各 biz 目录） | ⭐⭐⭐（readers/ 统一目录，所有 biz 共享） | ⭐⭐（registry 分发但有网络开销） |
| **编译期校验** | ✅ 业务系统直接 import 同名 Ports | ✅ 业务系统 import 自己的 Ports | ❌ 运行时通过反射匹配签名 |
| **CI/CD 复杂度** | ⭐（只改 codegen 输出路径） | ⭐⭐（需管理 module 间版本和 replace） | ⭐⭐⭐（引入注册中心维护） |
| **维护成本** | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |

## 3. 用户最终确认草案（标注日期）

> **确认选择**：方案 B — Module 化三层拆分，Ports 归业务系统
>
> **核心设计原则**：
> - Port 接口唯一：`biz-adapters/` 中禁止出现 `ports/` 目录，所有 Port 接口定义在业务系统的 `pkg/ports/` 下
> - biz-adapters 三层 module：`readers/`（共享低阶 ConfigReader 实现）→ `{biz_id}/`（适配器实现，独立 go.mod）→ 业务系统 `pkg/ports/`（Port 定义）
> - codegen 输出行为修改：`--output` 只生成 `adapters/blessstar/` + `adapters/mock/` + `provider/blessstar_provider.go`，不再生成 `ports/`
> - 非 BlessStar 适配器（db/http/env）手写放入 `adapters/{source}/`，与 blessstar 平级
>
> **确认人**：用户
>
> **确认日期**：2026-07-13

---

### 📐 架构拓扑图（ASCII 必填）

```
                                        修复前（当前混装）
 ┌────────────────────────────────────────────────────────────────────────────┐
 │  biz-adapters/go/douyin-mall/         douyin-mall/                        │
 │  ├── ports/          ← 不应该在这       ├── ports/    ← architect-pro也生成 │
 │  ├── adapters/                          │  (两份不一致时无校验)              │
 │  │   ├── blessstar/                     └── pkg/service/                   │
 │  │   └── mock/                                                             │
 │  └── provider/                                                             │
 │                                                                             │
 │  ★ 问题：port 定义两处存在，biz-adapters 内无法扩展非 BlessStar 适配器       │
 └────────────────────────────────────────────────────────────────────────────┘

                                    ▼ 修复后（方案B）

 ┌────────────────────────────────────────────────────────────────────────────┐
 │  业务系统仓库 (douyin-mall)                                                  │
 │  ┌──────────────────────────────────────────────┐                          │
 │  │  pkg/ports/            ← architect-pro 生成   │                          │
 │  │  ├── config_reader.go   (零外部依赖)           │                          │
 │  │  ├── auth.go            (AuthConfig 接口)     │                          │
 │  │  ├── order.go           (OrderConfig 接口)    │                          │
 │  │  └── ...                                      │                          │
 │  │  internal/service/     ← 只依赖 pkg/ports/   │                          │
 │  │  go.mod ← module github.com/xxx/douyin-mall   │                          │
 │  └──────────────────────────────────────────────┘                          │
 │                                                                             │
 │  biz-adapters/ (适配器市场 - 平台侧)                                         │
 │  ┌──────────────────────────────────────────────┐                          │
 │  │  go/readers/              ← 共享低阶实现     │                          │
 │  │  ├── httpreader/                              │                          │
 │  │  ├── filereader/                              │                          │
 │  │  ├── envreader/                               │                          │
 │  │  └── go.mod ← module biz-adapters/readers     │                          │
 │  ├──────────────────────────────────────────────┤                          │
 │  │  go/douyin-mall/          ← 按 biz_id 分组    │                          │
 │  │  ├── adapters/                                │                          │
 │  │  │   ├── blessstar/       ← codegen 生成      │                          │
 │  │  │   ├── mock/            ← codegen 生成      │                          │
 │  │  │   ├── db/              ← 手写              │                          │
 │  │  │   └── http/            ← 手写              │                          │
 │  │  ├── provider/                                │                          │
 │  │  │   ├── blessstar_provider.go ← codegen 生成 │                          │
 │  │  │   ├── db_provider.go       ← 手写          │                          │
 │  │  │   └── http_provider.go     ← 手写          │                          │
 │  │  ├── tests/                                    │                          │
 │  │  └── go.mod ← module biz-adapters/go/douyin-mall│                         │
 │  │              require github.com/xxx/douyin-mall │                          │
 │  │              replace → ../../{biz_repo_path}   │                          │
 │  └──────────────────────────────────────────────┘                          │
 └────────────────────────────────────────────────────────────────────────────┘

 编译期模块依赖关系：
 ┌──────────────┐     import     ┌──────────────────┐     import     ┌──────────────┐
 │ biz-adapters │ ────────────→  │ 业务系统          │ ────────────→  │ biz-adapters │
 │ /go/readers/  │  (低阶 reader) │ /pkg/ports/       │  (Port 接口)   │ /go/douyin-  │
 │              │               │                   │               │ mall/        │
 └──────────────┘               └──────────────────┘               └──────────────┘

 运行时业务系统 main.go 消费链路：
 main()
   → reader = httpreader.NewHTTPReader()              // biz-adapters/readers/
   → adapters = provider.ProvideBlessStarAdapters(reader)  // biz-adapters/douyin-mall/provider/
   → svc = service.NewAuthService(adapters.AuthConfig)    // 依赖 douyin-mall/pkg/ports/
```

---

### ⚙️ 架构不变量（审查红线）

1. **Port 接口唯一原则** — `biz-adapters/` 中**禁止**出现 `ports/` 目录。所有 Port 接口只能定义在业务系统的 `pkg/ports/` 下，由 architect-pro 生成和管理。

2. **biz-adapters 适配器必须 import 业务系统 Port 包** — 所有 `adapters/blessstar/` 中的实现文件必须 `import "github.com/xxx/{biz_id}/pkg/ports"`，禁止在 biz-adapters 内重新定义或拷贝接口定义。

3. **ConfigReader 接口定义在业务系统 `pkg/ports/config_reader.go`** — 作为 Go 标准库级别的抽象，零外部依赖（仅依赖 `context`、`time`），不绑定任何配置源实现。

4. **ConfigReader 低阶实现统一放到 `biz-adapters/go/readers/` 共享目录** — `httpreader/`、`filereader/`、`envreader/` 作为独立 Go module，所有业务系统通过 go.mod replace 共享引用，避免重复实现。

5. **非 BlessStar 适配器按源分类放入 `adapters/{source}/`** — db/http/env 等适配器与 blessstar 平级，各自独立的目录和 provider，禁止混入 blessstar 目录。

6. **biz-adapters 中每个 `{biz_id}` 独立 go.mod** — 通过 `replace` 指令将 Port 引用映射到业务系统仓库本地路径，CI 时由 codegen 自动配置；`readers/` 也是独立 go.mod。

7. **codegen 输出行为修改** — `--output` 只生成 `adapters/blessstar/` + `adapters/mock/` + `provider/blessstar_provider.go`，**不再生成 `ports/` 目录**。新增 `--biz-repo-path` 参数用于 go.mod replace 指向。

8. **provider 按配置源拆分** — `provider/blessstar_provider.go` 由 codegen 自动生成；`provider/db_provider.go` 和 `provider/http_provider.go` 手写维护，各自独立的 Provide 函数签名。

---

### 📋 落地建议（给 subtask-b 的指令）

- **模块拆分**：
  - **新增** `biz-adapters/go/readers/` 共享目录，含 `httpreader/`（HTTP 调用 Electron API）、`filereader/`（读本地 JSON/YAML）、`envreader/`（os.Getenv）三个标准低阶 ConfigReader 实现
  - **新增** `biz-adapters/go/readers/go.mod`：`module biz-adapters/go/readers`
  - **修改** `tools/blessstar-codegen/main.go`：新增 `--biz-repo-path` 参数，修改 `--output` 行为不再生成 `ports/`
  - **修改** `tools/blessstar-codegen/backend/go/go_backend.go`：import path 从 `{output_dir}/ports` 改为指向 `{biz_repo}/pkg/ports`
  - **修改** `tools/blessstar-codegen/backend/java/java_backend.go`：同理，import 指向业务系统 Ports
  - **修改** `tools/blessstar-codegen/backend/python/py_backend.go`：同理
  - **修改** `tools/blessstar-codegen/backend/c/c_backend.go`：同理（`#include` 路径指向业务系统）
  - **迁移** 现有 `biz-adapters/go/douyin-mall/ports/*` → `business system/douyin-mall/pkg/ports/`（仅做一次初始化迁移，之后由 architect-pro 管理）
  - **删除** `biz-adapters/go/douyin-mall/ports/` 目录
  - **删除** `biz-adapters/python/douyin-mall/ports/`、`biz-adapters/c/douyin-mall/ports/`、`biz-adapters/c/mongoose/ports/`、`biz-adapters/java/*/ports/` 同理
  - **新增** architect-pro 侧的 Port 生成职责：Ports 写入 `{biz_dir}/pkg/ports/` 而非当前 `ports/` 同级目录

- **架构关键设计代码**：
  - **codegen CLI 变更**（`tools/blessstar-codegen/main.go`）：
    ```go
    type CodegenCmd struct {
        Lang        string   // 目标语言，支持逗号分隔
        Manifest    string   // manifest.json 路径
        Schema      string   // config-schema.yaml 路径 (Schema-First 模式)
        Output      string   // 输出目录 (默认 biz-adapters)，只生成 adapters/ + provider/
        BizRepoPath string   // 业务系统仓库本地路径，用于 go.mod replace
        Check       bool     // CI 模式：diff 校验一致性
    }
    ```
  - **biz-adapters go.mod 自动生成**（`tools/blessstar-codegen/backend/go/go_backend.go`）：
    ```go
    func (b *GoBackend) GenerateGoMod(bizID, bizRepoPath string) string {
        return fmt.Sprintf(`module biz-adapters/go/%s

    go 1.22

    require github.com/xxx/%s v0.0.0

    replace github.com/xxx/%s => %s
    `, bizID, bizID, bizID, bizRepoPath)
    }
    ```
  - **ConfigReader 接口定义**（业务系统 `pkg/ports/config_reader.go`）：
    ```go
    package ports

    import "context"

    // ConfigReader 是配置读取接口，零外部依赖。
    // 实现类通过环境变量 BLESSSTAR_ENDPOINT 获取 Electron 地址（HTTPReader 场景）。
    // 初始化失败时不得阻塞进程，由 adapter 的三阶段降级兜底。
    type ConfigReader interface {
        Get(ctx context.Context, path string) (interface{}, error)
    }
    ```

- **测试要求**：
  - **回归测试**：现有 19 个 codegen 测试用例全部通过（需修改 import path 断言）
  - **编译测试**：`biz-adapters/go/douyin-mall/` 执行 `go build ./...` 验证能正确编译
  - **编译测试**：`biz-adapters/go/readers/` 执行 `go build ./...` 验证零外部依赖
  - **编译测试**：业务系统 `go build ./pkg/ports/...` 验证 Ports 零外部依赖
  - **集成测试**：`blessstar-codegen --lang go --manifest biz-registry/douyin-mall/manifest.json --biz-repo-path ../../business-system/douyin-mall --output biz-adapters` 完整跑通
  - **集成测试**：验证生成的 go.mod 中 replace 路径正确、go build 不报错
  - **集成测试**：验证 `--check` CI 模式能正确检测 biz-adapters 与业务系统 ports 不一致

- **涉及改动文件清单**：
  - 修改：`tools/blessstar-codegen/main.go`（新增 `--biz-repo-path`，修改 `--output` 行为）
  - 修改：`tools/blessstar-codegen/backend/backend.go`（`GeneratePortInterface` → internal-only 或删除）
  - 修改：`tools/blessstar-codegen/backend/go/go_backend.go`（import path + go.mod replace）
  - 修改：`tools/blessstar-codegen/backend/java/java_backend.go`（import path 指向业务系统）
  - 修改：`tools/blessstar-codegen/backend/python/py_backend.go`（import path 指向业务系统）
  - 修改：`tools/blessstar-codegen/backend/c/c_backend.go`（include path 指向业务系统）
  - 新增：`biz-adapters/go/readers/httpreader/http_reader.go`
  - 新增：`biz-adapters/go/readers/filereader/file_reader.go`
  - 新增：`biz-adapters/go/readers/envreader/env_reader.go`
  - 新增：`biz-adapters/go/readers/go.mod`
  - 移动：`biz-adapters/go/douyin-mall/ports/*` → `business system/douyin-mall/pkg/ports/`
  - 删除：`biz-adapters/go/douyin-mall/ports/`
  - 删除：`biz-adapters/python/douyin-mall/ports/`
  - 删除：`biz-adapters/c/douyin-mall/ports/`
  - 删除：`biz-adapters/c/mongoose/ports/`
  - 删除：`biz-adapters/java/douyin-mall/.../ports/`
  - 删除：`biz-adapters/java/music-website/.../ports/`
  - 新增：`.github/workflows/codegen.yml` CI 中新增 `codegen --check` 步骤

